### 分析夏普比率不一致的原因

1. pytest tests\strategies\test_premium_rate_strategy.py  这个测试文件中夏普率计算结果在 master 分支与 remove-metaprogramming 分支不一致
2. 对比 `master` 与 `remove-metaprogramming` 分支夏普率计算为什么出现差别
3. 切换分支的方法

master 分支: git checkout master
remove-metaprogramming: git checkout remove-metaprogramming

1. 切换分支之后，需要 pip install -U . 安装一下，让该分支的代码生效。
2. 先 debug 并总结为什么会出现不一致，并输出一个说明文档
3. 尝试解决这个问题的 bug
4. 最后需要确保是在 remove-metaprogramming 分支上
