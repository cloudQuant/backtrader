# 迭代35-1：任务清单

状态：IN_PROGRESS（WP-A~E 完成，WP-F 待执行）。关联[迭代计划](迭代计划.md)、[需求](需求文档.md)、[设计](设计文档.md)、[验收](验收文档.md)。
执行约定：每项任务完成后勾选并在备注填写证据（命令/退出码/路径）；验收状态以[验收文档](验收文档.md) §3 矩阵为准。
所有命令在仓库根目录、Anaconda base 环境执行（`/Users/yunjinqi/opt/anaconda3/bin/python`，下述 `python` 均指该解释器）。

## WP351-A：特征工程与样本库（P0，对应 FR351-01~03、07 数据侧）

- [x] A1 创建 `examples/017_fnn_embedding/` 骨架（config.yaml/run.py/fnn_embedding_strategy.py/fnn_autoencoder.py/README.md）｜D351-01
- [x] A2 数据层接入：load_mt5_csv + 启动校验（DATA_INVALID）+ 交易日/时刻索引/对数收益预计算（复用迭代35 结构）｜D351-02
- [x] A3 特征引擎纯函数：12 维特征（D351-03 冻结清单）+ 窗口有效性（NO_WINDOW）｜D351-03
- [x] A4 滚动归一化：norm_window 滚动均值/方差在线维护 + norm_min 拒绝（NO_NORM）｜D351-03
- [x] A5 样本库构建：全历史滑动窗口（stride）+ 待定池/可检索池两阶段入库（标签延迟）+ 特征缓存落盘（features_cache/）｜D351-06.1

验证：`python -m pytest tests/unit/test_fnn_embedding_example.py -q -k "features or normalize or library"`
（AC351-02/03/08 的 oracle 全绿）。

## WP351-B：FNN 自编码器训练管线（P0，对应 FR351-04~06）

- [x] B1 PyTorch AE 定义：encoder/decoder 结构参数化（embed_dim 等）+ 确定性开关（种子/单线程/deterministic）｜D351-04
- [x] B2 训练器：AE 损失/Adam/早停/batch 抽样上限（max_train_samples）｜D351-04
- [x] B3 滚动重训器：retrain_freq 节奏 + 训练集截止护栏 + 权重/元数据落盘（models/step*.pt）+ 库批量重编码绑定版本｜D351-05
- [x] B4 训练质量报告：train_report.jsonl（final_loss/epochs_run/neighborhood_retention/drift_procustes）｜D351-05
- [x] B5 **启动前置诊断**：全库余弦相似度分布探测（P50/P90/P99/P99.9/max 分位表）；若 sim_threshold=0.8 命中 <0.1% 按探测分布校准默认值并记录｜D351-06.3

验证：`python -m pytest tests/unit/test_fnn_embedding_example.py -q -k "autoencoder or train"`
（AC351-04 确定性 oracle 通过；B5 诊断分位表落盘 reports/）。

## WP351-C：检索与统计决策（P0，对应 FR351-07~11、14）

- [x] C1 余弦检索：exclusion zone 剔除 + 向量化矩阵乘 + 相似度门 ｜D351-06.2
- [x] C2 去相关 Top-N：贪心取样（t_end 距离 ≥ dedup_gap）｜D351-06.2
- [x] C3 统计决策链：adj=sign(sim−sim_neutral)×fr → Wilson 下界 + 成本期望门 + 原因码枚举｜D351-07
- [x] C4 判定链接入 Strategy（薄封装）+ 单仓约束状态机｜D351-07、迭代计划 G351-04

验证：`python -m pytest tests/unit/test_fnn_embedding_example.py -q -k "retrieval or wilson or decision"`
（AC351-06/07/09/10/11 oracle 全绿）。

## WP351-D：执行、风控与优化模式（P0，对应 FR351-12/13/15~19）

- [x] D1 执行模型：次 bar 开盘成交/到期平仓/末端强平（继承迭代35 模式）｜D351-08.1
- [x] D2 OCO 止损止盈含下限：m_log = max(3×er, ATR/点差下限)（oracle 双路径）｜D351-08.2
- [x] D3 TradeLogger 装配（feed 显式命名）+ signals.jsonl 扩展字段｜D351-10
- [x] D4 backtest_result.json 扩展 schema + 独立重算脚本｜D351-13
- [x] D5 `--mode optimize`：缓存模型前置检查、网格预算 ≤24 校验、并行/汇总/trial_registry 登记｜D351-11

验证：`python examples/017_fnn_embedding/run.py --fromdate 2021-09-01 --todate 2021-10-31`
端到端退出码 0，产出 backtest_result.json/signals.jsonl/logs/（M1 准出）。

## WP351-E：测试与审计（P0，对应验收 G1/G2）

- [x] E1 单元测试全集：tests/unit/test_fnn_embedding_example.py（设计 D351-14.1 九组 oracle）｜D351-14
- [x] E2 功能回归：tests/functional/strategies/time_based/test_XXXX_fnn_embedding.py（小样本端到端，冻结指标；模型训练一次后冻结入库）｜D351-14.2
- [ ] E3 前视三重审计用例：篡改 t_cut 后数据 → t_cut 前 signals 一致 + 权重 hash 一致｜AC351-14
- [ ] E4 优化模式用例：2×2 微网格（并行=串行、无日志、行数=4、预算拒绝路径）｜AC351-17
- [x] E5 独立性与规范：清空 PYTHONPATH 隔离运行 + 导入图扫描 + `ruff check`/`ruff format --check` 零告警 + 016 目录 hash 不变｜AC351-19

验证：`python -m pytest tests/unit/test_fnn_embedding_example.py tests/functional/strategies/time_based/test_XXXX_fnn_embedding.py -q`
（全绿；全量回归另跑 TradeLogger 既有套件与 016 示例测试，确认零影响）。

## WP351-F：稳定性评估与文档收口（P1→P0 收尾，对应 FR351-20、NFR351-01/02）

- [ ] F1 三分回测：训练 2021-08-12~2023-12-31 / 验证 2024-01-01~2025-03-31 / OOS 2025-04-01~2026-03-24（各带预热；OOS 预注册一次评估）｜D351-11、AC351-21
- [ ] F2 噪声对照：块打乱收益 + 相位随机化 surrogate 两种对照数据重跑全流程，断言零期望/零交易（或差异如实说明）｜AC351-21
- [ ] F3 信号级诊断：IC/ICIR + 事件研究 CAR 曲线（[-15,+60] bar）落盘 reports/｜AC351-21
- [ ] F4 性能计时：首跑（含训练）≤90min / 缓存回跑 ≤20min / 优化 18 组 ≤30min；确定性 diff 两次全流程｜AC351-22
- [ ] F5 README 收口：运行方法/参数表/输出说明/环境版本锁定/局限声明摘要；验收文档 §5 回填与矩阵状态更新

## 依赖与顺序

```text
A1→A2→A3→A4→A5 → B1→B2→B3→B4→B5 → C1→C2→C3→C4 → D1→D2→D3→D4→D5
                ↘ E1（A3/A4、B1/B2、C1~C3 的 oracle 先行，随做随测）→ E2/E3 → E4 → E5
D5 + F1→F2→F3→F4 → F5（G4 收口）→ 迭代完成
```

关键串行约束：B5（相似度分布诊断）必须先于 C1 的默认阈值冻结；E3（前视审计）必须先于任何
参数搜索（迭代计划 §6 风险表约定：噪声对照先于调参，F2 若 FAIL 则停止调参并如实记录）。

## 完成标准

1. 验收文档 G0~G3 全部 PASS（G4 结论如实，允许经济性 FAIL；噪声对照失败同样如实登记，
   不得删除对照结果后继续调参）；
2. 新增文件仅限 `examples/017_fnn_embedding/**` 与 `tests/unit`、`tests/functional/strategies/time_based/`
   下新文件；backtrader 核心零修改；`examples/016_self_similarity/` 逐字节不变；
3. 所有验证命令退出码留痕于验收文档 §5；
4. 试验登记表（trial_registry.md）累计组合数在本迭代内 ≤45（全迭代预算，对齐 L20/MinBTL）。
