# 迭代 29：日志体系完善验收

版本：2.1；修订日期：2026-09-20。依据[需求](./需求文档.md)（1.4）和[设计](./设计文档.md)（1.4）。1.1–1.3 为文档轮修订（布局、spdlog 后端）；**2.0：实现轮完成**，执行记录见第 6 节，处置明细见 `evidence/treatment-records.md`。2.1 的最终再验收以 `v1.4.0` 精确源码为准，状态与命令见[2026-09-20 再验收闭环](./2026-09-20-再验收闭环.md)。

**1.4.0 联合验收补充（2026-09-15，历史快照）：** 本页下表和第 6 节保留原实现轮快照；后续发现并修复的真实 spawn 配置传播、SDK 日志故障隔离、热循环风暴、配置 close 事务清理及扫描口径，以[联合验收与发布记录](./1.4.0联合验收与发布记录.md)为准。最终行系统 logger 修复、`lineiterator` 生命周期能力 guard 和 locator 兼容回退已改变源码，旧两套完整功能、前后配对性能和本地质量结果均为历史，必须重跑后才能用于当前候选；三个 SDK 的发布状态不受此影响。[最终日志修复记录](evidence/release-1.4.0/line-system-logger-remediation.md)绑定 AST 276（64/101/24/87）、80 个恢复 handler、10 个启动 ImportError 排除、locator 两条回退和 59 项定向回归。全仓[豁免清单](evidence/release-1.4.0/logging-exemptions.md)对该四文件范围已被取代，历史定向结果不替代当前完整门禁。2026-09-20 的精确 tag 再验收见[闭环记录](./2026-09-20-再验收闭环.md)，它覆盖本页顶层 Gate 状态而不改写历史执行明细。

**2.1 最终状态（2026-09-20）：`ACCEPTED`。** `v1.4.0` 精确 tag 的功能、真实 spawn/fork、多后端、策略、串行性能、质量和文档门禁均已重验；跨版本性能比较因不具备日志单因子归因条件，按闭环记录第 3 节改为迭代 33 的诊断性治理数据，不再作为本迭代发布门禁。

## 1. Gate 与状态

| Gate | 必要证据 | 本次状态 |
| --- | --- | --- |
| G0 文档合同 | AC29-01：三份文档事实核对、FR/NFR→D→AC 双向追踪、改动范围声明 | `PASS` |
| G1 能力与兼容 | AC29-02～06、AC29-15：按脚本分级分天文件、脚本名探测、双后端（spdlog/stdlib）、滚动、清理、幂等/静默合同、既有兼容 | `PASS`（spdlog 2.0.6 实机验证） |
| G2 覆盖治理 | AC29-07～11：关键节点、except 治理、风暴抑制、级别修正、print 治理 | `PASS` |
| G3 健壮与质量 | AC29-12～14：多进程、写故障、性能与全量回归 | `PARTIAL`：AC29-13 `PASS`；AC29-14 策略回归 `PASS`、性能采样 `NOT_RUN`；AC29-12 真实 spawn `NOT_RUN`（pid 后缀逻辑单测 `PASS`） |

状态定义：PASS=全部必需断言已证实；FAIL=执行后违反断言；BLOCKED=前置依赖/环境不足；NOT_RUN=未执行。零用例、只见退出码、缺失产物均不够判 PASS。任一必需项未通过不得宣告迭代完成；实现中发现的既有缺陷单列，不以"无新增失败"写成"全绿"。

本验收不授予任何交易许可；日志能力与示例准入（examples 012/013 的 shadow/demo 边界）互不影响。

## 2. 基线、环境与结果约束

1. 源码检查与测试在仓库根执行；记录 HEAD、分支、dirty 清单；源码证据绑定文件 hash，不只绑 commit。
2. Python 一律 `/Users/yunjinqi/opt/anaconda3/bin/conda run -n base python ...`；记录实际 `sys.executable` 与依赖。
3. M0 以 AST 脚本冻结三清单（except 处置 / logger 级别 / print 分布）；需求文档第 2 节的 grep 近似口径与 AST 精确口径的差异必须解释，不得倒填。
4. 现有测试禁止改期望值、吞异常、加 skip 迎合改动；`tests/unit/utils/test_logging_config.py` 既有 10 项断言原样通过是 G1 必需项。
5. 日志行为断言不得以"看到控制台输出"代替文件证据；全部用例断言 `log_dir` 产物文件内容。
6. 涉及日期滚动的测试用 monkeypatch 日期源，不依赖真实时钟跨零点，不引入 freezegun 依赖。
7. 所有结果绑定最终源码 hash；阶段间源码变更使旧证据失效时按影响重验。

建议证据目录（当前未生成）：

```text
<evidence>/iter29/<run-id>/
  baseline.json                # checkout、源码/依赖 hash、解释器
  m0-catalogs/                 # excepts.json / logger-calls.json / prints.json（脚本可再生）
  lifecycle-sample/            # 真实回测 log_dir 产物副本（M2）
  treatment-records/           # P0/P1/P2 逐处置置结论 + debug 升级清单 + print 豁免清单
  performance/                 # M5 前后采样样本与脚本 hash
  results.json                 # 逐 case_id 状态
```

## 3. 具名用例

除 AC29-01 外均为实现轮计划，当前 `NOT_RUN`。

| ID / 名称 | 前提与操作 | 必须观察的结果与证据 |
| --- | --- | --- |
| AC29-01 文档合同 | 核对三份文档及初始需求；追踪矩阵、基线事实、链接 | FR29-01..09/NFR29-01..06 每条至少映射一个 D 与一个 AC；反向无孤立条目；初始需求原文不变；无本轮实现声明 |
| AC29-02 分级分天文件 | `configure_logging(level="INFO", log_dir=tmp)`（monkeypatch `sys.argv=["xxx/run.py"]`）后逐级别发日志 | 路径为 `tmp/xxx_run/<今日>/`，含 error/warning/info 三个文件且配置时即创建；脚本名规范化（去后缀、`/`→`_`）正确，`script_name` 显式覆盖优先；路由互不串档；CRITICAL 入 error.log；DEBUG 不落盘（INFO 档） |
| AC29-03 跨午夜滚动 | monkeypatch handler 日期源跨天，继续发日志 | 新日志落同一脚本名下的新日期目录（`YYYY_MM_DD` 下划线格式）；旧目录文件完整保留；无日志的午夜跨越不产生空目录 |
| AC29-04 保留期清理 | 预置超期与非日期格式目录（含其他脚本名目录）后配置/触发滚动 | 仅本脚本目录内超期日期目录删除；其他脚本目录与非日期条目不动；清理异常仅 stderr 告警一次且不外抛 |
| AC29-05 幂等与静默合同 | 重复 configure；不 configure 分别发日志 | 重复配置无重复行；不配置时零输出、零文件、零目录创建（含子目录） |
| AC29-06 既有兼容 | 原样运行 `test_logging_config.py` 10 项；验证旧 `log_file`/`SpdLogManager` 路径 | 全部不改断言通过；`log_dir`+`log_file` 同给报 `ValueError` |
| AC29-07 关键节点覆盖 | 真实小型回测（CSV fixture）配置 `log_dir` 运行 | lifecycle 日志含：run 开始、每 feed 加载条数/范围、策略相位切换、订单提交/成交、run 结束（终值/耗时）；每 run 事件数有下界且无每 bar 级输出 |
| AC29-08 except 静默治理 | 对照 m0-catalogs/excepts.json 逐条核验 P0/P1/P2 处置 | 清单 100% 条目有结论（升级/保留+理由）；P0 两文件的非预期 except 均有可见日志；裸 raise 路径 error.log 可见；`test-fast` 阶段门禁绿 |
| AC29-09 风暴抑制 | 构造循环内同键异常 ≥ 2×every 次 | 首条全量（含堆栈）、摘要条含累计数、总数 = 全量+摘要数之和；`set_throttle(False)` 后逐条输出 |
| AC29-10 级别语义修正 | 对照 m0-catalogs/logger-calls.json 核验升级清单 | 标记为错误路径的原 debug 条目全部升级且行为断言/评审证据齐；保留项有理由记录 |
| AC29-11 print 治理 | 核心引擎路径 print 清单与豁免清单核对 | 核心路径无残留 `print(`（豁免区 bokeh/plot 逐处有理由）；替换点级别符合 D29-04 |
| AC29-12 多进程隔离 | `maxcpus>1` 真实 spawn 优化运行 | 子进程文件带 `.p{pid}` 后缀；任一文件无跨进程交错损坏行；主进程无后缀 |
| AC29-13 写故障隔离 | monkeypatch 写方法抛 OSError 后继续运行回测 | 日志调用不抛出；stderr 告警至多一次；回测正常完成 |
| AC29-14 性能与回归 | `make test-performance` 前后采样；`make test-strategies`；`make test-fast` | 微基准中位数回归 ≤5%（噪声跨阈值按迭代28口径 BLOCKED 复测一次）；策略期望值零变化；全量输出与收集清单落档；spdlog 已安装时两后端分别采样记录（无硬性倍数承诺，spdlog 未安装时记 `NOT_RUN`+探测结论） |
| AC29-15 双后端等价与回退 | spdlog 可用环境下以 `backend` 参数分别配置；模拟不可用（monkeypatch `_detect_spdlog` 返回 None）；检查 `get_logger` 返回类型 | 布局/分流/滚动/清理/幂等在两后端路径逐项一致（行内时间戳格式微差按指南豁免）；`auto` 回退 stdlib 且提示一次；显式 `spdlog` 不可用抛 `ImportError`；`get_logger` 恒返回标准库 `Logger`；spdlog 侧写异常不外抛（AC29-13 同判）；探测结论记录 spdlog 版本/平台/构建方式（2026-09-15 已有 macOS 冒烟记录，Ubuntu 待环境补验）；spdlog 未安装时除探测结论外记 `NOT_RUN`，不得以 stdlib 结果冒充双后端 PASS |

## 4. 执行口径与命令（计划，当前未执行）

```bash
# M0 清单（脚本属本轮交付，入库 scripts/）
conda run -n base python scripts/scan_logging_baseline.py --out <evidence>/m0-catalogs

# 阶段门禁
make test-fast
make test-strategies        # 期望值零变化是硬门禁

# 定向测试（新文件）
conda run -n base python -m pytest tests/unit/utils/test_logging_split_files.py -v
conda run -n base python -m pytest tests/integration/test_logging_lifecycle.py -v

# 性能（M5，串行无 xdist/coverage）
make test-performance

# 质量（逐工具记录，沿用迭代28口径）
conda run -n base python -m black --check backtrader --line-length=100
conda run -n base python -m mypy backtrader --config-file=pyproject.toml
conda run -n base python -m bandit -r backtrader -f json -o <evidence>/bandit.json
conda run -n base python -m ruff check <changed-python-files>
```

既有质量诊断与新增诊断分开列示；`make quality-check` 的实际工具解析按迭代28第 5 节口径记录，不把未运行工具写为通过。

## 5. 追踪矩阵

| 需求 ID | 设计 ID | 验收 ID |
| --- | --- | --- |
| FR29-01 | D29-02、D29-03、D29-13 | AC29-02、AC29-06、AC29-15 |
| FR29-02 | D29-02、D29-03 | AC29-02 |
| FR29-03 | D29-03 | AC29-03、AC29-04 |
| FR29-04 | D29-01、D29-05 | AC29-08 |
| FR29-05 | D29-05 | AC29-09 |
| FR29-06 | D29-01、D29-06 | AC29-07 |
| FR29-07 | D29-04 | AC29-10 |
| FR29-08 | D29-07 | AC29-11 |
| FR29-09 | D29-03、D29-10、D29-13 | AC29-05、AC29-06、AC29-12、AC29-15 |
| NFR29-01 | D29-06、D29-09、D29-13 | AC29-07、AC29-14 |
| NFR29-02 | D29-03 | AC29-05 |
| NFR29-03 | D29-08 | AC29-13 |
| NFR29-04 | D29-08 | AC29-12 |
| NFR29-05 | D29-11 | AC29-14 |
| NFR29-06 | D29-11、D29-12 | AC29-01、AC29-14 |

## 6. 实现轮执行记录（2026-09-15）

环境：`/Users/yunjinqi/opt/anaconda3/bin/conda run -n base python`（Python 3.11.8）；基线 dev `404383b7`；spdlog 2.0.6（site-packages，macOS arm64，sdist 构建）。M0 清单 before/after 落于 `evidence/m0-catalogs{,-after}/`，可由 `scripts/scan_logging_baseline.py` 再生。

### 6.1 逐 AC 判定

| AC | 状态 | 证据 |
| --- | --- | --- |
| AC29-01 | `PASS` | 三份文档追踪矩阵闭合；初始需求原文未改 |
| AC29-02 | `PASS` | `test_logging_split_files.py` stdlib+spdlog 各 8 组参数化通过；布局 `logs/<脚本名>/<YYYY_MM_DD>/`、配置即建、分流互不串档、CRITICAL 入 error、INFO 档无 debug 文件 |
| AC29-03 | `PASS` | `test_midnight_rollover_to_new_dir`（双后端）：新日志落新日期目录、旧文件保留 |
| AC29-04 | `PASS` | `test_retention_cleanup_removes_expired_dirs`（双后端）：仅本脚本超期目录删除，其他脚本目录与非日期条目不动 |
| AC29-05 | `PASS` | 幂等无重复行；不配置零输出零文件零目录（显式断言） |
| AC29-06 | `PASS` | `test_logging_config.py` 既有 10 项不改断言通过；`log_dir`+`log_file` 同给 `ValueError`；`SpdLogManager` 兼容项通过 |
| AC29-07 | `PASS` | `tests/integration/test_logging_lifecycle.py`：真实回测（2006-day-001.txt，SMA 交叉）断言 run starting→data loaded→nextstart→order submitted/executed→stopping→finished 全链，行数下界 7 且 <200（无每 bar 退化），error.log 存在且干净回测下为空；spdlog 后端真实回测冒烟另以独立脚本复核同链路 |
| AC29-08 | `PASS` | AST 口径 before（pass 151/continue 48/raise 86/未 logged）→ after（pass 6/continue 15/raise 38）；插入 229 行（P0 49/P1 89/P2 91），17 文件补 logger；豁免 3 处（理由入处置记录）；多语句体保留原状（已有实质处理）。验证：btapi 1,116 项、unit 3,225 项、`make test-fast` 4,644 项全绿（flaky 单列 6.2） |
| AC29-09 | `PASS` | `test_throttled_error_first_then_summary`：250 次→全量 1 条+摘要，总数 <250；诊断模式 5/5 直通 |
| AC29-10 | `PASS` | 40 处 error-semantic debug→warning（分布入处置记录）；配套 8 项 mock 断言 debug→warning 属 FR29-07 语义合同变更，逐条登记（18 项全绿） |
| AC29-11 | `PASS` | trade_logger 10 处 error-semantic print→logger；豁免 34 处（reporter/btrun CLI、autodict 演示块、Analyzer.print/Strategy.log API） |
| AC29-12 | `NOT_RUN` | 真实 `maxcpus>1` spawn 优化运行未执行；`.p{pid}` 后缀逻辑（`_mp_log_suffix`）单测 `PASS` |
| AC29-13 | `PASS` | `test_write_failure_does_not_raise`：写流抛 OSError 时日志调用不外抛 |
| AC29-14 | `PARTIAL` | **策略回归 `PASS`**：`make test-strategies` 1,271/1,271（最终源码二次执行）；`make test-fast` 4,644 passed；性能采样 `NOT_RUN`（`make test-performance` 未执行——M2 生命周期日志为每 run/每订单级、热循环零新增调用在设计层保证，未做前后采样对比） |
| AC29-15 | `PASS` | spdlog 2.0.6 实机：双后端 8 组等价参数化全过 + spdlog 后端真实回测冒烟（含 error 干净断言）；`auto` 回退+提示一次；显式 `spdlog` 不可用 `ImportError`；`get_logger` 恒返回标准库 `Logger`；spdlog 侧 emit 异常走 handleError 不外抛 |

### 6.2 已知 flaky（非本轮引入，隔离复跑均通过）

- `test_iteration22_ctp_benchmarks.py::test_short_stress_profile_waits_for_deadline_and_is_incomplete`（串行全量下偶发）。
- `test_btapistore_iteration21.py::test_cancel_unknown_query_live_...` 与 `test_btapistore_funding_refresh.py::test_restart_fences_...`（`-n 8` 并行下轮换偶发；AGENTS.md 已记载的并行噪声类别）。

### 6.3 过程中发现并修正的治理误判

P2 批量规则把 4 处 `__getattr__`/`__getitem__` 协议探测的窄异常 raise（order.py:495、autodict.py:134/192、parameters.py:1668）误判为 error——正常订单流的 `order.p` 探测每次触发，污染 error.log 且引入堆栈渲染开销。已降为无 exc_info 的 debug，并在 spdlog 回测冒烟中以"error.log 干净"断言锁定。集成测试同步补该断言。

### 6.4 质量门禁（53 个改动文件）

black（--line-length=100）全过（重排经 HEAD diff 确认仅涉本轮插入行）；ruff：HEAD 基线全绿→本轮引入 155 项（PIE790/F821/B006/E402）全部修复归零；bandit log_message.py 4 处 B110 加 nosec 说明；mypy log_message.py 7 项新增错误修复归零（HEAD 基线 0 错）。

### 6.5 交付清单

代码：`backtrader/utils/log_message.py`（270→~700 行，log_dir/script_name/backend/retention_days + `_DailyLevelFileHandler`/`SpdlogHandler`/`_detect_script_name`/`_detect_spdlog`/`_cleanup_retention`/throttled 系列）；M2 关键节点落点（`_cerebro/execution.py`、`_cerebro/channel.py`、`strategy.py` nextstart/_stop、`brokers/bbroker.py` submit/双 execute/双 reject）；M3/M4 治理 53 文件。测试：`tests/unit/utils/test_logging_split_files.py`（36 项）、`tests/integration/test_logging_lifecycle.py`、logreturns 8 项断言合同变更。脚本：`scripts/scan_logging_baseline.py`。文档：`docs/LOGGING_GUIDELINES.md` 迭代29章节、`AGENTS.md` Logging 节、`evidence/treatment-records.md`。

### 6.6 第二阶段追加治理（同日，用户追加需求）

多语句宽异常回退体补齐：192 处宽捕获无日志 → 插入 184 行（分级 error/debug/warning，
详见处置记录第 7 节），8 处豁免 + 2 处运行时豁免修正（`_safe_log` logger 故障兜底，
由 `test_logger_sink_failure_only_increments_health` 捕获）。终态：未 logged 宽捕获
归零（除声明的豁免）。最终验证（绑定第二阶段后源码）：`make test-fast` 4,645 passed
（exit 0）；`make test-strategies` 1,271/1,271。AC29-08 的覆盖口径由"单语句静默体"
扩展至"全部宽捕获无日志体"，证据链在 `evidence/m0-catalogs-after2/`。
