# 迭代27 执行记录（第一轮：2026-09-12）

执行：JoyinCode 会话（用户授权"完成迭代27"）。本轮完成 T0/T1/T2/T5/T10，实测尝试 T3/T4 并如实记录 BLOCKED；T6-T9 未开始，状态不变。

## 1. 任务处置总览

| 任务 | 状态 | 结果 |
|---|---|---|
| T0 基线快照 | **完成** | 三仓基线即本记录 §2 的提交前状态；本记录连同提交本身构成审计链 |
| T1 backtrader 落库 | **完成** | dev 分支 3 笔提交（见 §2）；提交后树干净；A4 三测试在提交态通过；全量 `tests/unit + tests/integration -n 8` **3804 passed / 1 skipped / 0 failed**（含 live_certification，无 deselect） |
| T2 bt_api_py 落库 | **完成** | 父仓 4 笔 + base/ctp 子仓各 1 笔（见 §2）；`tests/` 全量 **1502 passed / 0 failed**；okx/binance 离线集 **687 passed / 1 failed**（唯一失败 `test_ok_request_bar.py::test_get_history_bar` 为迭代20 §6.4 已归档的实网超时项）；实网 `network/` 集离线环境不跑 |
| T5 旧测试 O2 适配 | **完成** | 45 失败 → **0**（arming 81 + recovery 48 + quality 全绿）；详见 §3 |
| T10 打包清单 | **完成** | `setup.py` 排除 `tests.*/scripts.*/studies.*/docs.*` 子包：66 → **26** 个包，全部 `backtrader.*`；`setup.py --version` 正常 |
| T3 G3 时段探测 | **BLOCKED（实测）** | 2026-09-12T03:17:53Z（周六）TCP 探测第一套 td:10201 / md:10211 均 `TimeoutError` 不可达——符合周末休市预期；待第一套交易时段复跑 preflight 留收据 |
| T4 mechanical_cycle | **诊断完成，修复5项缺陷，推进至 arm 阶段（BLOCKED @ entry-approval）** | 原 `INSTRUMENT_SCAN_EVIDENCE_INCOMPLETE` **不是环境/周末问题**：同日同时刻 engineering_smoke（同扫描）PASS。逐层诊断修复：①扫描前会话语义桥预热（首快照 session_before 恒 disconnected）②结算短路改用 native `settlement_state`（未确认时 verify 回查污染 last_error 反锁确认）③`derive_bundle_preflight` 候选键/session_after 透传笔误 ④bundle proof 哈希绑定最新快照 ⑤授权前重刷 Stage A/B/bundle（30s 新鲜度预算 vs SimNow 慢链）。当前推进至 `arm_execution_from_approval`，余 `ctp_execution_authorization_context_mismatch`（entry-approval 上下文绑定，移交迭代23-25 SDK owner）。**09-11 深夜"connected=false 环境维护"归因作废**——同缺陷所致。修复见 dev `3a8936fa`，单测 43 passed 无回归 |
| T6 FQ3 CP03 / T7 MF-T1 / T8 HF-T1 / T9 O2+U1B14 | **NOT_RUN** | 本轮未开始；合同/返修边界见[任务](任务.md) |

## 2. 提交清单（均未推送；推送/PR 由仓库所有者决定）

| 仓库 | 分支 | 提交 |
|---|---|---|
| backtrader | `dev` | `61ed2678` feat(ctp): barrier/cohort/option-commission 框架层 + O2 entry 集成 + setup.py 打包修复<br>`f42bbc98` feat(examples): 014_1/014_2/015 期权三示例 + SimNow 入场链工具 + 迭代26 整改产物<br>`da1d5c83` docs(iterations): 迭代23-27 文档包 |
| bt_api_py | `codex/iter21-cross-venue-arbitrage` | `ee3a8bc1` feat(execution): 路径预算/入场审批/close plan owner + 子仓指针<br>`44f707ee` test(execution): arming/recovery 套件适配强制 O2 预算门<br>`ae9412b6` docs(plans): iteration-04 运行时契约交付信任计划<br>`1ab54484` test(quality): CTP exchange kwargs 显式结算默认 |
| bt_api/bt_api_base | `codex/iter21-cross-venue-arbitrage` | `74be52d` feat(gateway): 协议模型扩展 |
| bt_api/bt_api_ctp | `codex/ctp-instrument-discovery` | `b371098` feat(ctp): 严格持仓证据/查询生命周期/native 重建 |

提交注意：bt_api_py 配置了 git-lfs 的 post-commit 钩子但本机无 git-lfs（仅告警，提交成功）；该仓 `.pre-commit-config.yaml` 未安装，历史文件（含 HEAD）本就不满足 ruff-format，未做整体重排以免混入纯格式 diff。

## 3. T5 实施细节

**根因**：在途 O2 预算强制（`_begin_invoke` 对已武装 CTP `make_order`/`cancel_order` 一律要求有效预算能力）与旧测试无预算的冲突；45 个失败大多源自夹具 `write_crashed_journal`/`write_bundle_crashed_journal` 造账时的武装下单。

**测试侧**（`test_execution_arming.py` / `test_execution_recovery.py`）：
- 新增 `_budget_evidence`/`_reserve_budget`：构造 `sdk_runtime` 源的最小可写预算证据（6 状态×6 成本字段、`fresh_available_cny`+三项 verified 旗标、单腿 proof 自动补伴生腿满足 2-3 腿 scope）；会话仍按自身 arm proof/写者租约/journal 复核，**未放宽任何 O2 门**；
- 造账夹具、两处 writer 下单、7 处 `arm_recovery_from_preflight`（recovery 模式预留）与普通/撤单 invoke 透传 `budget_capability`；
- journal 断言纳入 `ctp_budget_reservation_started/committed`、`ctp_budget_action_started`；execution summary 断言纳入 `ctp_budget` 块；CTP front 测试期望 `auto_settlement_confirm=False`。

**产品侧（两处必要修复，均在 `_execution_session.py`）**：
1. **跨代际预算上下文规则**（`_budget_bound_context`）：同账户+同 profile 且 `connection_generation` **严格增大**时允许建立新预算上下文（对齐 arming 的"严格更新代际需新 proof"规则）；旧代际预留仍留在账本并计入 active/uncertain 上限，不放宽任何 cap。无此规则则一切重启后 recovery 写被 `budget_context_generation_mismatch` 永久阻断——与 recovery 设计矛盾（10 个失败由此而来）。
2. **缺失导入修复**：`from datetime import UTC, datetime`——否则任何带 `expires_at` 的预留触发 NameError（ruff F821 实证）。

**遗留**：`_execution_session.py` 3 个在途预存 lint 项（I001 import 排序、F401 未用导入、C409）留给 O2 owner，未动他人在途代码。

## 4. 验证命令与结果

```bash
# bt_api_py（T2/T5 验收）
python -m pytest tests -q                                     # 1502 passed / 0 failed
python -m pytest bt_api/bt_api_okx/tests bt_api/bt_api_binance/tests \
   --ignore=**/network -q                                     # 687 passed / 1 failed（已归档网络项）
python -m pytest tests/bt_api_contract/test_execution_recovery.py \
   tests/bt_api_contract/test_execution_arming.py \
   tests/test_bt_api_quality.py -q                            # 141 passed

# backtrader（T1/T10 验收）
python -m pytest tests/unit tests/integration -n 8 -q         # 3804 passed / 1 skipped / 0 failed
python -m pytest tests/unit/stores/test_btapistore_normalized.py::test_owned_sdk_restart... \
   "tests/unit/brokers/test_btapibroker_source_reconciliation.py::test_normalized_ctp..." -q  # 3 passed

# T3 探测（BLOCKED 证据）
# 2026-09-12T03:17:53Z: td 180.168.146.187:10201 / md :10211 → TimeoutError（周六）

# T4（已诊断修复5项缺陷并推进至 arm；余 entry-approval context_mismatch 移交 SDK owner）
python -m examples.ctp_options_simnow_mechanical_operator --env examples/.env \
  --environment second_7x24 --future SA701 --call SA701C1500 --put SA701P1500 \
  --query-timeout 90 --leg-timeout 60
```

## 5. 剩余工作（移交）

1. **T3**：第一套交易时段（工作日 08:55 起）复跑 `013_3 shadow --preflight-only` 留结构化收据 + 60 分钟观察，回写 G3。
2. **T4（更新）**：mechanical 链已修至 arm 阶段；剩余 `ctp_execution_authorization_context_mismatch`（`bt_api.py:6438 _prepare_ctp_entry_arm_authorization` 的 grant↔sealed-context 绑定校验）移交迭代23-25 SDK owner，修复后重跑同命令即可。09-11 深夜环境归因已更正为代码缺陷。
3. **T6-T9**：FQ3 CP03 / MF-T1 六组 / HF-T1 / O2+U1B14（合同均已在推进记录冻结）。
4. **推送/PR**：五仓本地提交均未推送；backtrader 按仓库规范 PR→dev（当前为 dev 直提，与该仓历史迭代提交模式一致，如需走 PR 流程可 cherry-pick 至 feature 分支）。

## 6. 2026-09-21 验收续跑：环境问题登记

| ID | 发现阶段 | 现象与影响 | 初步归因 | 处置与结论 |
|---|---|---|---|---|
| I27-20260921-01 | 迭代22定向回归收集 | `python -m pytest` 在收集/汇总阶段失败：`pytest_asyncio` 对 pytest `Package` 访问不存在的 `.obj`，Hypothesis 的 entry-point 枚举同时因一个 `None` distribution name 抛出 `TypeError`。测试尚未执行，不能据此推断任一产品断言失败。 | 本机全局 pytest 插件与安装元数据组合不兼容；错误栈仅在 `site-packages`，不在 backtrader 或 bt_api_py 源码。 | 后续离线验收以 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest` 执行，并在每份收据中标明该隔离条件；保留原始异常作为环境阻断证据。未修改产品代码或放宽验收断言。 |
| I27-20260921-02 | 迭代30制品治理定向回归 | `test_evidence_reports_the_loaded_workspace_source` 失败：Windows 上 `runtime_artifact_evidence()["module_path"]` 为反斜杠路径，未满足验收所用的可移植 `backtrader/__init__.py` 后缀。其余 210 项通过。 | backtrader 的制品证据直接序列化了平台原生 `Path` 字符串；证据格式未规范化，跨平台消费者会得到不一致输出。 | 先修复运行时代码，将证据路径规范化为 POSIX 表示；保留对真实解析路径和 workspace/source 身份的既有失败关闭校验，随后重跑迭代30完整定向套件。 |
| I27-20260921-03 | 迭代22确定性 replay 定向回归 | `test_native_replay_is_deterministic_real_cerebro_path_without_pnl` 在 Windows 失败：同一 JSON 路径断言一处要求 POSIX 后缀，另一处却以原生反斜杠的 `str(REPO)` 比较前缀；同批其余 460 项通过。 | 验收测试混用了两种路径表示，不能在 Windows 上同时满足；source-component 证据应以跨平台 POSIX 形式序列化。 | 保持运行时证据的 POSIX 序列化，将工作区身份断言也改为 `REPO.as_posix()`；不改变 provenance 哈希、源码载入、零写入或策略执行，随后重跑迭代22定向套件。 |
| I27-20260921-04 | bt_api_py 合同回归收集 | 在 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` 的环境隔离下，`D:/bt_api_py/conftest.py` 无条件添加 `pytest.mark.xdist_group`，因 xdist 插件未加载而在收集阶段退出。未执行任何 SDK 断言。 | bt_api_py 的测试入口依赖 pytest-xdist 标记；与 I27-20260921-01 同属本机插件隔离策略的兼容边界，不是执行、预算或 CTP 产品缺陷。 | 后续 SDK 回归保持自动加载关闭，但显式 `-p xdist` 加载声明的必要插件；报告该运行条件，不修改 SDK 门禁或测试断言。 |
| I27-20260921-05 | 迭代23低频示例 CLI replay | 合法的 `--output reports/iter27-20260921-replay.json` 在确定性 replay 完成后因父目录不存在抛出 `FileNotFoundError`。该问题阻止操作者把报告写入常用的相对结果目录。 | `run.py` 直接调用 `Path.write_text()`，未建立用户显式指定输出文件的父目录。 | 在写报告前以 `mkdir(parents=True, exist_ok=True)` 建立该文件的父目录；不改变输出内容、模式门禁或零网络/零写入语义，并为这一行为补充回归测试。 |
| I27-20260921-06 | 迭代22 G3 只读 shadow 预检 | `--mode shadow --purpose observation --preflight-only` 以 `FAIL_CLOSED` 返回，缺少 `investor_id,password,app_id,auth_code`；未建立 CTP 会话。 | 本机没有受治理的 SimNow 凭据；这是外部运行前置条件，不是策略或 SDK 断言失败。 | 不伪造凭据、不降级 preflight。待凭据由操作者在已忽略的本地环境文件中提供后，使用同一命令重跑，并仅在通过后进入 60 分钟零写观察。 |
| I27-20260921-07 | T6/T7 独立验收 harness 启动 | FQ3 与 MF-T1 将某台 macOS 的 `/Users/yunjinqi/opt/anaconda3/bin/python` 作为唯一合法 base 解释器。FQ3 在测试前直接抛异常；MF-T1 虽返回进程码 0，但收据明确为 `BASE_CONDA_INTERPRETER_REQUIRED`，因此不能当作通过。 | 验收 runner 将单机绝对路径误当作 Conda base 身份条件，并继续把该路径传给子 pytest 进程；这与 Windows/POSIX 上实际 base 语义无关，阻断跨平台当前源码验收。 | 统一改为验证当前解释器为带 `conda-meta` 与 Conda 可执行文件的 base 环境，并以该解释器启动子进程；保留非 Conda、非 base 或解释器解析不一致时的失败关闭。修复后用新的单次输出目录重跑 T6/T7。 |
| I27-20260921-08 | T8 HF-T1 冻结输入验证 | 当前工作树未改动 HF-T1 fixture，但 runner 将 Windows 文本 checkout 的 CRLF 原始字节与冻结记录中的 Git LF SHA-256 比较，五个输入全部失配并在任何 oracle/pytest 前失败。 | 哈希器没有区分受 Git 文本行尾转换影响的冻结 JSON 与二进制/源代码身份材料；历史 provenance 已声明其依据为 LF 规范字节。 | 仅对 HF-T1 的受控 JSON fixture 在哈希时将 `CRLF` 规范为 `LF`；源代码、动态模块、输出和非 fixture 材料仍按原始字节绑定。重新验证 Git blob 与规范化工作树一致后重跑 T8。 |
| I27-20260921-09 | T7 修复后静态校验 | 首次应用 I27-20260921-07 修复时，MF-T1 的 fixture 哈希赋值误多缩进，`py_compile` 在 pytest 前报告 `IndentationError`；其嵌入子 runner 也遗留了同一 macOS `BASE` 常量。 | 修复编辑本身不完整；这不是产品或冻结 oracle 的失败，但会让验收脚本不可执行，且子进程仍可能脱离当前 base 解释器。 | 先登记后修复：恢复正确的循环缩进，子 runner 使用 `sys.executable`；再执行编译与新目录独立验收。 |
| I27-20260921-10 | T6 FQ3 子进程隔离 | 解释器路径修复后，FQ3 目标套件在 `asyncio` 导入 Windows `_overlapped` 时得到 `WinError 10106`，导致 27 个 setup error；同一目标文件在正常受控 pytest 中已通过。 | FQ3 的最小环境 allowlist 删除了 Windows 的 `SYSTEMROOT`/`WINDIR`/`COMSPEC`，破坏 Winsock DLL 装载；这三项是 OS loader 状态，非凭据或任意用户注入。 | 仅在 Windows 将三项 OS loader 变量加入固定 allowlist，继续清除 `.env`、任意 `PYTHONPATH` 和凭据变量，保留 socket 与 `.env` 审计拒绝；随后用新的目录复跑。 |
| I27-20260921-11 | T6 FQ3 冻结目标节点清单 | FQ3 收集到 53 个节点，而冻结 JUnit/collection 清单仍为 52；唯一增量是本轮 I27-20260921-05 修复所新增的 `test_cli_creates_an_explicit_output_parent_directory`。 | 独立验收清单没有随已登记、已通过的低频 CLI 回归增加该唯一节点，导致即使套件通过也会 fail-closed。 | 将该精确节点加入冻结清单；未扩大测试文件范围、未删除旧节点，重跑时仍要求节点集合精确相等。 |
| I27-20260921-12 | T6 FQ3 socket 审计重跑 | I27-20260921-10 后，受控子进程的 Windows 本地 loopback `socket.bind` 被审计拒绝，Conda 激活异常重入并形成子进程级联；先终止验收 root PID 树，随后按精确的 `conda-script.py shell.cmd.exe activate C:\\anaconda3`/`conda.exe ... activate` 特征清理残留。现场最终仅保留原有 CTP service/collector 两个 Python 进程。 | `bind` 本身不建立远端连接、解析或发送数据；将它等同于外部网络 I/O 既不增加本验收的零外联证明，又破坏 Windows 本机运行时。 | 审计继续拒绝 `socket.connect`、`socket.getaddrinfo` 和 `socket.sendto`，但不再拒绝本地 `bind`；收据明确该边界，并以新目录重新执行。 |
| I27-20260921-13 | T6 FQ3 声明的 shadow 子进程来源 | 重跑仍出现 Conda 激活级联。审计事件表明该分支来自测试声明的 `run.py --mode shadow` 子进程；harness 将 `PYTHONPATH` 覆盖成仅含审计目录，因而该子进程不保证从当前工作树导入 `backtrader`。 | 仅把 guard 目录置入 `PYTHONPATH` 破坏了跨 cwd 的 current-source 绑定，使 child 可落入 site-packages/环境启动路径；这同时违背该 runner 的来源验收意图。 | 受控 `PYTHONPATH` 改为 `guard_dir + pathsep + ROOT`，仍由 guard 首先加载，并保证声明的 shadow child 导入当前工作树；再次使用新目录执行。 |
| I27-20260921-14 | T7 MF-T1 冻结 oracle 完整性 | 当前 HEAD 的 `frozen_oracle.py` 规范化 LF SHA-256 为 `580326…7cec`，但 runner 常量为 `28a6b7…3a3a`；另外三份冻结 JSON 均与常量匹配，工作树与 index 一致。 | oracle 在既有提交中已是当前内容，但 runner 的固定常量未随之更新，因此所有平台都会在 oracle/pytest 前 fail-closed。 | 常量更新为通过 `git show HEAD:<path>` 验证的 LF 哈希；只改变错误的 pinned digest，不改变 oracle、场景或负向合同。 |
| I27-20260921-15 | T7 MF-T1 Git source binding | Windows 上 `rel()` 返回反斜杠，`git cat-file --batch-check` 接收 `HEAD:backtrader\\...` 后把所有绑定路径误判为 HEAD 缺失，即使 index/worktree 均一致。 | Git tree path 使用 POSIX `/`；runner 将平台显示字符串直接用于 Git plumbing。 | `rel()` 改为 `Path.as_posix()`，统一收据与 Git 查询路径；仍保留 HEAD/index/worktree 三方一致性检查。 |
| I27-20260921-16 | T7 MF-T1 source isolation 建立 | I27-20260921-14/15 后，runner 到达 source sandbox 建立阶段，但 `os.symlink(ROOT, output/source)` 在当前 Windows 帐户返回 `WinError 1314`（无创建符号链接特权），因此 child oracle 尚未启动。 | 当前主机未启用 Developer Mode，且执行帐户无 SeCreateSymbolicLinkPrivilege；这是受控验收目录的 OS 权限门槛，不是中频策略、fixture 或 SDK 合同失败。 | 保留 `LOCAL_MIDFREQ_TIMING_SUBSET_FAIL` 收据，不以 worktree 结果替代隔离来源验收；待在启用 Developer Mode/具备该权限的 Windows 主机，或由 harness owner 明确接受经审计的 directory-junction 实现后重跑。 |

## 7. 2026-09-21 本轮离线复验与裁决

本轮在 Windows 上以 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` 运行 backtrader 定向测试；
bt_api_py 额外显式加载其声明的 `xdist` 插件（见 I27-20260921-01、04）。该隔离只避免
本机全局插件崩溃，未跳过任何目标测试或放宽产品门禁。

| 范围 | 本轮可复现结果 | 裁决 |
| --- | --- | --- |
| 迭代22 SA / Feed / Store / Broker / idle 集成 | 461 passed；`013_3` `no_signal` replay 为 `PASS_REPLAY_PATH`，0 SDK 写入、无 PnL 字段 | 本地验收通过；G3 仍 `BLOCKED`（I27-20260921-06），G4/R1/R2 不因 replay 改变。 |
| 迭代23 低频 C/P/F | 140 passed（含新增 CLI 输出目录回归）；`eligible` replay 为 `LOCAL_REPLAY_PASS` | 本地回放/时序/adapter 子集通过；真实 CTP、账户、成交和完整 G1/G2 仍未签收。 |
| 迭代24 中频 C/P/F | 128 passed；`no_edge` replay 为 `LOCAL_REPLAY_PASS`，独立 `--timing` 为 `LOCAL_TIMING_REPLAY_PASS` | 本地 FQ2 与 MF-T1 投影通过；G1–G4、R1/R2 继续按合同为 `BLOCKED`/`NOT_RUN`。 |
| 迭代25 / 30 高频与环境适配 | 211 passed；`valid_cohort` replay 为 `LOCAL_REPLAY_PASS`，0 网络/0 写入/0 实际成交 | A1–A5 的本地合同通过；上游执行资格未签发，L2–L4 及 G3 仍阻断，`HFT_STATUS=NOT_ADMITTED`。 |
| 迭代27 SDK 预算/审批/恢复/平仓 | bt_api_py 292 passed（recovery、arming、budget、approval、close-plan 合同） | 本地 O2 相关合同通过；未以此替代真实 CTP 会话、账户或结算证据。 |

本轮修复 I27-20260921-02/03 的跨平台路径证据表示，以及 I27-20260921-05 的低频 CLI
输出目录创建。四个相关示例 README 已补齐策略逻辑、冻结参数、启动命令、输出解释和明确的
证据边界。最终整体结论维持 **INCOMPLETE / NO-GO**：离线可验证部分已验收，但必须在具备
受治理凭据、真实交易时段与上游资格签发后，才可重新执行外部 G3/G4 和研究门；不得把这些
本地 PASS 改写成真实交易、盈利或 HFT 通过。

## 8. 2026-09-21 CTP 长期 Pilot 前置补齐登记

下列项是在用户要求将 CTP 策略先以 SimNow 长期运行、后续再接入期货公司 CTP 时重新核验
发现的事实。按迭代 27 的处置规则，先登记，再补齐可由代码补齐的部分；不以配置伪造账户、
市场或研究证据。

| ID | 发现阶段 | 现象与影响 | 初步归因 | 处置与结论 |
|---|---|---|---|---|
| I27-20260921-17 | Iter22 SimNow 启动前静态核验 | `config.yaml` 绑定的 `state/iter22-czce-2026-calendar-20260910.json` 在当前工作区不存在（`state/` 被忽略）。即使填入正确凭据，首次带合约选择的预检也会以 `BLOCKED_CTP_TRADING_CALENDAR` 失败关闭。 | 受控交易日历是运行时证据制品，未随源码/本地模板迁移；README 的历史描述不能替代 hash 可验证文件。 | 增加操作者就绪检查，明确报告 artifact 缺失或 hash 不匹配；只接受权威 CZCE 来源生成的 JSON 与 SHA-256，不从文档或 fixture 伪造日历。当前可先以当日审阅的实际 SA 合约走手工冻结路径，但仍须真实 TradingDay、ExpireDate 和日历证据。 |
| I27-20260921-18 | Iter22 自然信号准入核验 | 当前候选配置为 `RESEARCH_NOT_ESTABLISHED`，而 `natural_signal` 强制要求 `RESEARCH_ADMITTED` 和冻结的 `signal_preregistration_sha256`。 | 研究样本、成本后表现和独立审阅尚未提供；这不是 `.env` 或运行器缺字段。 | supervisor 与 receipt 工具必须继续拒绝该候选的订单会话；先补齐只读/工程运行能力和证据材料，研究准入经真实数据与审阅完成后才可签发自然信号 receipt。 |
| I27-20260921-19 | 当前主机的 CTP 运行时资格核验 | 已有 G2 记录覆盖 macOS arm64/Anaconda base；本会话运行于 Windows，不能借用该平台的 native/package 身份结论。 | CTP native 扩展、动态库装载和已安装消费者路径均有平台差异。 | 增加 Windows 本地运行前检查和受控证据输出；在本机完成 native probe、安装消费者、只读 SimNow 会话后，才可将 Windows G2/G3 状态签入 receipt。 |
| I27-20260921-20 | SimNow 到期货公司 CTP 的迁移核验 | Iter22 配置只有冻结 SimNow profile；Backtrader runner 与 `bt_api_py` 的 CTP execution approval 都只验证 `demo`，期货公司前置被识别为 unknown/拒绝 arm。直接替换账号或 TD/MD 地址会失败关闭，不能成为生产通道。 | 当前 SDK 的 production identity/trust model、券商前置注册、独立 approval 和生产恢复验收尚未实现。 | 建立 SimNow/production 的隔离 profile 与运维契约骨架，但不把 production 伪装成 demo。生产 CTP 作为 `bt_api_py` 的独立受审功能：需要明确券商资料、账户证明、风险上限、生产 trust root 和端到端验收后才能实现/启用。 |
| I27-20260921-21 | Iter22 长期运行方式核验 | 受控订单 runner 强制每次传入正且有限的 `--run-seconds`；它能安全结束和对账，但没有跨进程的长期重启/停机编排层。 | 有界会话是原有风控设计，不应改成无限、不可审计的订单进程。 | **已补齐** `pilot_supervisor.py`：默认 dry-run，只有显式 `--execute` 才调度子进程；每段最多 3600 秒、最多 24 段。仅在 sealed、已平仓、证据健康、身份与 network-data hash 连续的 manifest 后重启；账户/TradingDay/合约/profile/generation 变化、超时、非零退出、未知 manifest 或内部持久化失败均 fsync 写入 stop-latch 且不自动清除。12 项定向回归通过；它不持有 CTP client，也不扩大订单权限。 |
| I27-20260921-22 | Iter22 admission 运营流程核验 | runner 能严验 HMAC-signed admission receipt，但示例目录只有测试辅助函数，操作者没有受控的 request/draft/签发入口。手写 JSON 容易漏绑 config、code、账户、TradingDay、generation、native 依赖或有效期。 | 准入验证实现早于面向操作者的签发工具；不能以手改 receipt 或让策略进程生成 approval key 作为替代。 | **已补齐**独立 receipt 工具：默认只生成脱敏 request；仅在显式 `--sign`、已有 process-level operator-owned HMAC key、当前 config 准入且完整运行期身份/evidence 字段齐全时签名，发布前复用 runner 验签。它不读取 `.env`、不生成密钥、不建立 CTP 会话、不将凭据写入日志。5 项定向回归通过。 |
| I27-20260921-23 | Windows Iter22 native 前置探测 | 当前 `C:\anaconda3\python.exe` 起初导入的是 site-packages `bt_api_ctp 2.0.2`，但该包没有 `get_ctp_native_diagnostics`；Iter22 `native_probe()` 将它视为 `bt_api_ctp_public_native_diagnostics_missing` 并拒绝任何网络 session。`D:\bt_api_py` 当前源码已有公开诊断接口。 | 当前 Python 环境安装的是未与工作树/Iter22 contract 同步的 CTP package；源码存在不等于运行时会导入该源码。 | 已从 `D:\bt_api_py\bt_api\bt_api_ctp` 重装当前包并运行无网络 probe：`accepted=True`、`native_loaded=True`、package manifest 已验证、1 个 native 文件、组件 identity 完整。此项修复 Windows 本地 native/package 前置；真实 SimNow 会话 G2/G3 仍须另行留证。 |
| I27-20260921-24 | Windows CTP editable 安装 | 首次 `python -m pip install -e D:\bt_api_py\bt_api\bt_api_ctp --no-deps --no-build-isolation` 在 metadata 阶段失败：`importlib.metadata` 枚举 entry points 时得到 `None` distribution name。pip 同时警告旧 `bt_api_ctp-2.0.2-py3.11-win-amd64.egg\EGG-INFO` 的 metadata 无效。 | 确认该旧 egg 的 `EGG-INFO` 只有 `native_libs.txt` / `not-zip-safe`，没有 `PKG-INFO`、`METADATA` 或 `top_level.txt`；这是精确的损坏目标，也解释 I27-20260921-01 的 entry-point 异常。 | 先只读核验后，将唯一损坏 egg 可恢复移动到 `C:\anaconda3\Lib\site-packages\_codex_quarantine\bt_api_ctp-2.0.2-py3.11-win-amd64.egg`；随后同一安装命令完成编译/安装。未删除任何无关包。新解释器已公开 native diagnostics，Iter22 probe 通过。 |
| I27-20260921-25 | Iter22 supervisor 实现复核 | 新增 supervisor 首版虽然逐段验 manifest，但 seal 未保留并比较账户、TradingDay、profile 或 connection generation；因此两个各自“成功”的子会话之间发生身份变化时可能继续启动下一段。stop-latch 写入也缺少显式 `fsync`。 | 跨会话风控要求在实现细节中遗漏；仅验证每个 manifest 自身正确不足以证明连续会话是同一受控账户/连接身份。 | **已修复并验证**：第一段 seal 现在成为账户/TradingDay/合约/profile/generation/network-data hash 基线，任何后续差异立即 fsync 写入 `SESSION_IDENTITY_CHANGED` stop-latch；validator/journal 等内部异常也写 `SUPERVISOR_INTERNAL_FAILURE`。12 项定向回归覆盖两段身份差异与持久化异常。 |
| I27-20260921-26 | Iter22 receipt 工具实现复核 | 新增 receipt 工具首版只要求 `expires_at` 在未来，未限制授权 TTL 或要求当前处于 issued/expires 区间；也只比对 config 中的 calendar hash，未在发起/签发前本地验证 artifact 存在且匹配。 | 原 runner 会在网络边界再次拒绝部分异常，但 operator 工具应在产生可签名材料前防止长寿命或缺失本地日历的错误审批。 | **已修复并验证**：工具要求 `issued_at <= now < expires_at` 且 TTL 不超过两小时；调用 runner 的无网络本地 calendar/hash 验证后才写 request/receipt。负向回归覆盖缺失 calendar、未来签发与超期 TTL，最终 runner 校验继续作为第二道门（receipt 工具 5 项定向回归）。 |
| I27-20260921-27 | Iter22 `.env` 模板/README 一致性复核 | `.env.example` 将第二套 `simnow_second_7x24` 说明为“only for `--api-diagnostic`”，但 runner/README 还允许其用于无写入的 `--engineering-strategy-observation`。操作者可能据此误以为该受限工程路径不可用。 | 模板注释未随已冻结的工程观察例外同步；不是 profile、权限或订单门禁的实现变化。 | 在 README 已有的失败关闭边界下，将模板文字改为同时列出两条只读用途，并明确两者均不能交易；不改变 profile 或 runner 行为。 |
| I27-20260921-28 | Iter22 CZCE 日历补件核验 | 未取得郑商所或期货公司可审计的、逐日机器可读交易日历原件；交易规则/节假日通知可解释一般规则，但不能证明没有临时休市或交易调整。且 auto 选约要求覆盖全部 eligible SA 的 `ExpireDate`，当前候选范围可能延伸至 2027，2026-only 日历即使真实也不能解除 auto 门。 | 将“工作日减节假日”误当交易所逐日确认会违反 runner 的 `never synthesize` 合同；缺少 2027 覆盖则是证据范围不足，不是日历 JSON 格式问题。 | 保持 `BLOCKED_CTP_TRADING_CALENDAR`，不伪造 artifact。仅在取得可追溯原件、覆盖本次手工冻结合约 `ExpireDate` 的情形下，由审核人生成本地 JSON、保存来源/hash/获取时间，再审核更新 config；auto 模式须覆盖所有 eligible expiry。 |
| I27-20260921-29 | Iter22 approval key 模板/运维边界复核 | `.env.example` 为 approval HMAC key 提供了可填写行，而新 receipt README 规定 key 仅由审核方以进程 secret 注入；runner 为兼容性仍会读取本地 `.env`。两种说明并存会诱导操作者把长期审批根与普通 SimNow 凭据放在同一个文件。 | 模板早于独立 receipt 工具；兼容读取不等于推荐的 secret-storage 做法。 | 移除模板中的可填写 approval key 行，改为明确的 process-secret 注释；保留 runner 兼容读取，不破坏既有本地部署。新审批流程使用受管 process environment，且不把 key 写入 request、receipt、报告或仓库。 |
| I27-20260921-30 | Iter22/跨市场长期运行生命周期复核 | `pilot_supervisor.py` 将每个子进程硬限制为最多 3600 秒、最多 24 段，README 又把这一限制用于“长时间运行”。这把第一套 G3 观察窗口/第二套受限工程诊断的时长，与真实运行的生命周期边界混为一谈：CTP 应在权威交易时段与 CTP `TradingDay` 的收盘边界完成受控 drain、对账和次日复核；数字货币为 7×24，健康检查/证据 checkpoint 不能等价为按小时终止交易进程。 | 初版 supervisor 只为 SimNow pilot 的短时证据采集设计，缺少 market-specific lifecycle policy，且不能由 CTP runner 的 `--run-seconds` 语义推导出 crypto 的连续 watchdog。 | **待分轨修复，禁止直接放宽现有 CTP supervisor。** 保留 3600 秒作为 G3 最小观察/工程诊断边界；CTP 新增“按权威 session schedule + CTP TradingDay 边界”编排契约，收盘前 drain、完全对账、写 sealed terminal manifest，下一 TradingDay 重新验账户/日历/准入后才可启动；crypto 另建 7×24 continuous-watchdog 契约，以周期 checkpoint、健康/对账/断线 fail-closed 恢复为边界而非固定杀进程。两者均须有独立的状态 schema、恢复门禁、故障 stop-latch 与回归验收，且不得扩大 SimNow 或生产写权限。 |
| I27-20260921-31 | 012_1/012_2 crypto 长期 shadow 启动文档核验 | 两份 README 均示例 `--mode shadow --duration 86400` “连续观察一天”，但冻结 config 的 `run_timeout_seconds` 分别为 600 和 180，runner 的 `_bounded_requested_duration()` 会在建连前拒绝 86400。该文档命令无法运行，且直接提高普通 `--duration` 上限会把候选绑定的短时 timeout/停机合同误用于 7×24。 | 短时 candidate runner 与长期观测说明未分离；现有 duration 是一次性受控运行参数，不是永久 watchdog 的健康周期。 | **待修复**：保留普通 `--duration` 的既有上限和 demo lease 约束；只给 `shadow` 新增显式、零订单/零成交/零 PnL 的 continuous-watchdog，并以 30–3600 秒 checkpoint 周期写入 append-only 健康记录。README 改用新入口，明确任何 unsafe/stale/stream-conservation/command-health 异常均记录后停止、不自动重启；不得借此启用 paper、demo 或 live。 |
| I27-20260921-32 | examples 实盘架构复核 | 账户级的订单/撤单额度、halt 状态和运行健康分散在 Iter22 `DailyRiskStore`、各策略的局部 risk 对象、SDK CTP execution-budget，以及 `TradeLogger` 的仅告警计数中。多个 strategy/process 共享同一账户时，策略目录内的计数无法构成全局、原子的下单门；现有 `bt_api_py.risk_management`/`monitoring` 也尚未绑定到 `BtApiStore/BtApiBroker` 的 durable intent/reconciliation 写路径。 | 把 candidate 专属经济约束、账户级不可逆写入门、观察报告和服务生命周期混放在 example 层；`TradeLogger.snapshot()` 明确只使用内存/本地 broker cache，不查询 provider、不持久化，不能承担跨进程风险权威或故障恢复。 | **待架构重构，禁止以 TradeLogger 替代风险门。** 在 `bt_api_py` 的 execution-session / broker 边界建立账户、venue、environment、account fingerprint、TradingDay/rolling epoch 和 connection generation 围栏绑定的 `bt_api_risk` 兼容层：所有开仓、撤单和受控平仓先作 durable atomic reservation，再由 SDK 发起/对账/结算；常规开仓、常规撤单与紧急减仓必须使用独立额度。策略只提交 signal/strategy policy，`TradeLogger` 作为只读 telemetry adapter；另建控制面 monitor 消费 durable journal、TradeLogger snapshot 与 health checkpoint，负责告警、freeze/drain/人工介入状态，不能直接产生普通开仓。先迁移 Iter22，再分别接入 CTP TradingDay 与 crypto 7×24 生命周期。 |
| I27-20260921-33 | Backtrader 实盘 bracket/OCO 语义核验 | `BtApiBroker.buy/sell()` 接受 `parent`、`transmit`、`oco` 参数，但每个 framework order 随即走单笔 `submit()`；`BtApiStore` 映射 SDK `OrderRequest` 时不携带 parent/OCO/basket 关联。实盘调用者可能把三笔独立写入误认为 provider 原子 bracket/OCO，出现第一腿成交而保护腿未成功写入的裸露风险。现有 `batch_cancel()` 也只是循环单笔撤单，不能弥补该语义缺失。 | Backtrader 回测 `BackBroker` 的 parent/children/OCO 机制与 `BtApiBroker` 实盘 adapter 的能力混同；当前 SDK 只有单笔 durable intent，未提供统一 parent/child/basket execution contract 或 provider capability matrix。 | **待修复，先 fail-closed。** 在 `bt_api_py` 公共 execution 层建立 durable `ParentExecution`/`ChildIntent`/fill-allocation/recovery 模型，并由 provider 明确声明 native OCO、atomic batch、slice、basket recovery 能力；`BtApiBroker` 仅在该能力已协商且 execution-plan 已创建时投影关联订单，否则拒绝 `parent/oco/transmit` 实盘提交。不得把多次普通提交包装成“原子” bracket/OCO。 |
| I27-20260921-34 | ZMQ forwarding 写入开关核验 | `bt_api_py.forwarding.service.ZmqForwardingRuntime(enable_trading=False)` 曾仅保存 `GatewayConfig`，其 `OrderRouter` 未检查该标志，默认 runtime 仍可成功 `submit_order()`。 | `enable_trading` 未下沉至命令路由/不可逆 provider write 边界；loopback/remote 地址校验不能替代实际写入授权。PUB/SUB、ROUTER/DEALER、SQLite receipt 也尚未构成独立 execution/risk 权威。 | **基础 P0 已修复并复验。** `ForwardingRuntime.write_enabled` 现传入 `OrderRouter`；`place_order`、`cancel_order`、`cancel_all` 及其 ZMQ command 路径在 disabled 时均于 adapter/provider I/O 前拒绝。定向回归以 write-count mock 验证三类命令计数为零，显式 `enable_trading=True` 的写路径仍通过；`25 passed, 1 skipped`（自动加载插件关闭，既有 async 测试跳过）。后续 `bt_api_zmq` 提炼仍为 P0：需补 principal authentication/ACL、cursor/replay/snapshot、跨重启 command/provider reconcile 与 CTP gateway 端到端验收；在这些完成前，不得标为生产网关。 |
| I27-20260921-35 | `bt_api_base` gateway 写入默认门核验 | `bt_api_base.gateway.runtime.GatewayRuntime._handle_command()` 和 `runtime_remote.GatewayRuntime._dispatch()` 对 `place_order` / `cancel_order` 直接调用 adapter；其 `GatewayConfig` 没有 write-enable、server authorization 或 ACL 字段，且不经过 I27-34 的 `OrderRouter`。 | 同一 ZMQ/forwarding 部署存在第二条可绕过已修复 router 的 provider-write 路径；`runtime_remote` 还使用另一套 message protocol，不能将 I27-34 的 forwarding 修复错误外推到全部 gateway。 | **基础 P0 已修复并复验。** `GatewayConfig.enable_trading` 默认 `False`，`gateway_enable_trading` 是 launch-builder 显式 opt-in；两个 runtime 的全部现有 provider 写命令（`place_order`、`cancel_order`）均在 adapter I/O 前拒绝。成功写入 fixture 已改为显式 opt-in。base gateway 全量测试 `15 passed`；依赖该 runtime 的 SDK contract 子集 `273 passed, 2 skipped`。该门不等于 remote authentication/ACL、risk/execution admission 或 CTP 生产资格。 |
| I27-20260921-36 | `bt_api_risk` / `bt_api_monitor` 隔离安装依赖核验 | 两个独立包的源码均 import `bt_api_base`（risk 使用 EventBus/exceptions/logging/mixins；monitor 使用 logging factory），但各自 `pyproject.toml` 的 runtime dependencies 未声明 `bt_api_base`。 | 独立 wheel consumer 可能依赖 ambient editable/base 安装而“碰巧成功”，违背 plugin 的 pin/隔离安装合同；且 base 当前仍把 `pyzmq` 作为硬依赖，后续 ZMQ optionalization 必须避免把该负担误扩散为隐式依赖。 | **metadata P0 已修复并复验。** 两包现在均声明 `bt_api_base>=0.15.4,<0.16`；`pip wheel --no-deps` 成功构建，两个 wheel 的 `METADATA` 都含对应 `Requires-Dist`。完整无 ambient checkout 的安装/导入 consumer 仍由 Iter41 AC41-34 验收；base 的 `pyzmq` optionalization 作为 I27-37 / Iter41-G4 的独立重构，不在 metadata 修复中掩盖。 |
| I27-20260921-37 | ZMQ 协议、导入和 gateway 语义分裂核验 | `bt_api_py.forwarding.schema`（typed DTO）与 `bt_api_base.gateway.protocol` / `runtime_remote.py`（untyped payload）并存；base/runtime 与 forwarding router 的命令集、receipt、UNKNOWN/reconcile 语义不同。`bt_api_py` / forwarding 当前又 eager import ZMQ，`pyzmq` 在 core/base metadata 中为硬依赖。 | 直接创建第三个 ZMQ 包会进一步分裂 wire/OMS 语义；“未启用即不 import/install”也无法成立。不能将 provider-aware router 移入纯 transport 包，否则会让 transport 拥有 provider、账户、risk 或 execution 事实。 | **待 Iter41-G4/P0 收敛。** 先抽取无 ZMQ 的 shared transport contract，令 `bt_api_zmq` 只依赖该 contract 并拥有 sockets/security/relay；`bt_api_py[zmq]` 作为 optional adapter，保留旧 facade。冻结唯一 canonical protocol、capability negotiation/拒绝、compatibility matrix、lazy imports 和 no-PyZMQ core install 验收；在完成前，不支持将 base gateway/CTP 写入声明为 `bt_api_zmq` 生产能力。 |
