# 迭代 24：CTP 期权期货中频套利策略需求

版本：1.1；编写日期：2026-09-10。状态：`PARTIAL_LOCAL_IMPLEMENTATION`。本目录已有本地合成 replay 切片；完整 CTP 链、SimNow 准入和盈利结论均未建立。

本次交付包括需求、设计、验收文档和本目录的本地 replay 策略。原始诉求见[初始需求](./初始需求.md)；三迭代共同的合约资格、现金流、资金、SDK 边界、订单恢复、审批及证据规则统一继承[公共架构与基线](../迭代23-CTP期权期货低频套利策略/公共架构与基线.md)。公共规则优先于本目录的频率参数；不在本例内复制第二套客户端、审批机制、订单账本或账户风控。

> 2026-09-10 的只读实测确认了期货、期权和万元内两腿静态资金候选确实存在，详见[公共基线的结论记录](../迭代23-CTP期权期货低频套利策略/公共架构与基线.md#c01a-2026-09-10-选品实测结论)。本迭代当前 FR 仍是三腿 conversion/reversal 的中频模板；两腿策略应另立候选、模型与验收，不能因资金可行性就视作已通过经济或写入准入。

## 1. 用户目标与边界

构建可供后续策略复用的中频模板：CTP tick 辅助信号，只有已封闭的一分钟 K 线能够触发新的普通开仓、正常平仓或正常调仓决策；使用 Backtrader 原生 `bt.Strategy`、`Cerebro`、`BtApiStore`、`BtApiFeed`、`BtApiBroker`。账户实际权益再高，本策略及账户已冻结义务的累计资金占用也不能绕过人民币 10,000 元上限。

经济候选限定为同一标的期货 F、同到期日/行权价 K 的认购 C 和认沽 P 组成的 conversion/reversal 三腿组合。不是跨品种价差、跨月套利或裸卖期权策略。欧洲式资格允许研究经融资/结算修正的平价关系；美式提前行权、结算和转换风险未验证时，不得将残差称为等式无风险套利。首版不选择或硬编码当前可交易合约，不自动行权、履约、交割或展期，不进入生产账户。

“最好实现盈利”转化为独立研究门槛：在预注册条件下验证费用、滑点、无法成交及退出成本后的样本外表现；不足、失败和没有机会都是合法研究结果，不得降低成本、放宽账户预算或回看 holdout 来制造通过。

## 2. 模式、范围与完成定义

| 模式 | 可连接范围 | 交易写权限 | 结果含义 |
| --- | --- | --- | --- |
| `replay` | 纯本地不可联网 | 任何 SDK/CTP 交易写均为零 | 公式、时序、状态与故障注入；若另开假设成交模型，须显式标记且与真实账本隔离 |
| `shadow` | 审核过的 SimNow 只读会话 | 报单、撤单、行权、自动结算确认均禁止 | 第一套自然行情观察，不能生成真实成交/PnL |
| `simnow` | 通过准入的第一套环境 | 仅 receipt 限定候选、账户、合约、用途、时间及预算范围 | G4 机械验证和 R2 自然信号验证独立报告 |
| `production` | 禁用 | 配置解析阶段拒绝 | 首版无生产交付承诺 |

第二套 7×24 至多用于明确标记的只读 API 诊断，不能替代第一套行情、成交、经济或频率证据。结算确认只能使用公共基线规定的独立显式准备动作，不能混入 shadow/preflight。

`examples/014_2_ctp_options_midfreq/` 已含 `config.yaml`、`run.py`、`ctp_options_midfreq_strategy.py`、`README.md`、`.env.example`，并且是可直接运行的单策略目录：运行时不得 import、路径注入、动态加载或以任何方式依赖其他 `examples/` 目录，亦不得依赖 `examples` 下的公共包。CLI 配置和离线输入只允许解析到本目录内；`..`、符号链接逃逸与外部绝对路径必须在创建 Cerebro 前以 `CONFIG_PATH` 拒绝。012/013 仅可作为设计参考。真正通用的能力只能落入 `backtrader`、`bt_api_py` 或 `bt_api_ctp` 的明确 owner，并有独立消费方测试。

> **本地实现边界（2026-09-10）**：本目录目前将合成的三腿 1 分钟数据经 Cerebro 和 BackBroker 回放。普通决策只在 `next()` 产生；tick 仅记录特征，不产生普通订单；当前窗口在决定后才吸收样本，边界时刻的晚到 tick 被拒绝。该结果标为 `LOCAL_REPLAY_PASS`，不等于真实 BtApiStore/BtApiFeed/BtApiBroker/CTP 的 minute barrier、账户、费用、订单或实际 PnL 证据。

**独立目录强制约束**：014_2 必须从自身目录直接运行；禁止 runtime import、路径注入、动态加载、读取或任何隐式依赖其他 `examples/` 目录的代码、fixture、状态、账户或审批，也禁止新建 `examples` 公共包。所有 CLI 配置和离线输入路径必须在相对解析及符号链接解析后仍位于本目录；任一外部路径在创建 Cerebro 前拒绝。012/013 只可参考设计。真实共用能力只可进入 `backtrader`、`bt_api_py` 或 `bt_api_ctp` 的明确 owner，并有对应消费者契约；否则必须留在本策略目录且不被其他示例消费。

## 3. 功能需求

下表的“必须”是后续实现验收要求。所有 ID 的设计和验收对应关系见[验收追踪矩阵](./验收文档.md)。

| ID | 需求与可判定边界 |
| --- | --- |
| FR24-01 | 提供上述三文件入口，且目标示例目录可直接运行；运行时不得 import、路径注入、动态加载或依赖任何其他 `examples/` 目录或 `examples` 公共包。配置使用严格 schema、未知键/非有限数/单位错误/重复合约拒绝启动；配置及离线输入在解析后必须仍位于本目录，外部路径/符号链接逃逸拒绝；有效配置冻结并绑定 hash，禁止运行时静默换参数。 |
| FR24-02 | 一次运行恰有一个 Store 管理的公共 `BtApi` 实例；三合约各一个权威 Feed/订阅消费链，所有订单经 `bt.Strategy.buy/sell` 与 Broker，禁止直接调用 native Trader 或另建查询交易客户端。真正通用的实现仅可由 `backtrader`、`bt_api_py` 或 `bt_api_ctp` 的明确 owner 提供，不得以 `examples` 公共模块或跨示例运行时复用代替。 |
| FR24-03 | 启动冻结 `candidate_id`、F/C/P、交易所、交易日历、规则来源、K、到期日、标的映射、行权方式、结算方式、乘数、tick、手数及费用/保证金；缺项或不匹配拒绝开仓。规则更新使旧冻结失效。 |
| FR24-04 | 候选通过欧洲式/美式 capability 分类；美式不得套用未经修正的平价等式。没有经审查的提前行权/转换现金流/费用风险界时，保持只读研究、禁止 SimNow 开仓。首版每腿一手的1:1:1篮子若不满足乘数/交割一致或超预算，结果为 `BLOCKED_CAPITAL`。 |
| FR24-05 | 读取完整账户、持仓、订单、成交、合约与账户费率/保证金查询；每个完成标记、账户指纹、TradingDay、generation 和 scope 必须相符。成功但空的账户、未终结查询、部分响应不能作为零状态。 |
| FR24-06 | 每个合约 tick 具备真实时间/接收时间/单调时间/序列/代际/交易日/盘口/累计及增量成交量/质量字段。缺失、错序、重复、断流、零档量、越限价、离网格或跨代际数据不能进入信号。 |
| FR24-07 | 三腿一分钟 bar 均由各自权威 Feed 的同一 tick 流形成；明确 `[T-60s,T)`、watermark、`available_at`、quality 和源序列。仅已完成且可用的 bar 可参与决策；零成交未形成 bar 时跳过整个三腿分钟，不填造 OHLCV 或沿用旧 bar。 |
| FR24-08 | 三腿以相同 bucket end、TradingDay、规则版本与 generation 对齐后，建立唯一 minute barrier。barrier 等待超限、跨分钟或缺腿则永久跳过该分钟，不晚到补开。 |
| FR24-09 | tick 特征按已封闭分钟的不可变 cutoff 取样：只取 event time `<T` 且在所属 bar 封闭时已收到的事件。后续 tick、晚到历史 tick、下一分钟数据不能修改已有特征或历史信号。执行前新盘口只用于否决、限制价格和风控。 |
| FR24-10 | 计算本设计给出的盘口不平衡、microprice、五秒持续边际、三腿不利盘口压力及分钟残差的历史 median/MAD；均有明确定义、单位、缺样条件和独立数值 oracle。CTP 一档快照不能推断真实主动买卖、撤单量或排队优先级。 |
| FR24-11 | 开仓信号同时满足历史预热、平价方向、历史偏离、三腿可执行净边际、tick 辅助过滤、资金与准入条件。只用过去 60 个合格分钟拟合当前残差；当前样本在计算后入窗，参数调优与 holdout 隔离。 |
| FR24-12 | 新普通决策只能在已完成 minute barrier 对应的 `next()` 中产生并同步尝试提交首腿，每分钟至多一个普通 basket 动作。token 单次消费，未使用也不得延迟到 tick 回调；跨分钟、重连、规则/账户变化立即失效。 |
| FR24-13 | tick/idle 可执行安全撤单、已确认敞口减仓、已授权篮子的有界补腿和对账；每项必须绑定原 basket/订单/剩余额度并证明不增加最坏敞口，不得用“补腿”生成新篮子、改变方向或超原手数。 |
| FR24-14 | 三腿执行非原子；首腿前审核每个可能中间路径，逐腿只按权威确认成交推进。拒单、部分成交、取消与成交竞争、超时及 UNKNOWN 进入公共恢复协议；UNKNOWN不盲重试、不假定撤单成功。执行初值为单腿终态5秒、整篮子未对冲15秒、撤单终态5秒、恢复窗口60秒，按持久意图/最早可能暴露起算，ACK不重置；无行情由idle推进。 |
| FR24-15 | 使用实际买卖盘可执行价格及已验证 L1 数量，限价/手数保守取整并校验涨跌停。价格和持仓方向/开平今昨仓由公开 SDK 合约映射，禁止普遍假设 IOC 或 reduce-only 由 CTP 原生支持。 |
| FR24-16 | 资金总上限 10,000 元，初始工作预算 8,000 元、恢复预留 2,000 元；全账户已冻结义务、活动/未决/UNKNOWN 订单、期权买方权利金、卖方保证金、期货保证金、费用和 VM 冲击均计入路径预算；未确认组合优惠、卖权利金收入、未实现盈利不能扩充预算；按保守归因净PnL历史低点单调收紧本金，跨日/盈利不恢复且不重复累加逐笔亏损，单账户只允许一个写者/一个活跃篮子。 |
| FR24-17 | 建立每日硬损失、单篮子风险、连续亏损、最大尝试次数和写请求限额，数值继承公共基线并可更严格。风控基线只能在完整同代际对账后建立；重启、换日、SimNow 大账户、损失账本写失败均不能重置额度。 |
| FR24-18 | 普通退出在完整三腿持仓至少 60 秒且新 minute barrier 到达时决策；不做常规裸腿 delta 再平衡。900 秒持仓上限、风险损失、临近休市/行权截止、断流及审批到期可由 tick/idle 发起安全退出；计时采用保守成交时间区间。 |
| FR24-19 | 会话休息/夜盘/节假日依据权威日历；开仓剩余可交易时间必须覆盖最大持仓和恢复窗口；跨 session 清空 tick 特征和未用 token。无法在截止前完成时进入恢复/人工接管，不能称为归零成功。 |
| FR24-20 | replay/shadow/simnow/production 权限严格区分，simnow receipt 与候选、代码/包/native、配置、合约、账户、第一套环境及前序 gate 绑定；工程smoke与自然信号用途互不替代，自然写入须本候选G1/G2/G3/G4及R1全部PASS，签名/信任根统一复用公共审批策略。 |
| FR24-21 | `notify_order/notify_trade` 仅投影权威事件；本例保存信号与篮子意图，不另建可独立驱动下单的订单状态账本。停止、重启与 UNKNOWN 均通过公共 journal 和两轮独立完整对账处理，未归零不得退出为成功。 |
| FR24-22 | 输出 quotes/bars/minute_features/signals/basket_intents/risk_events 及统一订单成交证据，记录每个决策的 bar IDs、cutoff、源序列、拒绝码、价格、成本、资金路径、token 和因果关系；证据写失败锁存停止开仓，保留可证明的安全恢复路径。 |
| FR24-23 | 使用通用 TradeLogger 的缓存视图与策略 extension 展示信号、三腿敞口和风险；展示不可发起账户查询，不能把缓存快照代替完整账户对账；hypothetical 与 actual 成交/PnL 必须物理分账和清晰标识。 |
| FR24-24 | 每个候选先预注册，再训练/校准、独立 OOS 与第一套自然 SimNow 观察；报告含完整费用、逐腿持仓风险、成交失败、资金占用和未完成周期。R1/R2 未通过时不得宣称盈利或复制为已验证模板。 |

## 4. 非功能需求

| ID | 验收要求 |
| --- | --- |
| NFR24-01 | 策略和回调单线程拥有可变状态；回调不得同步网络查询或无界 I/O；只读查询结果经 Store 受管队列应用，所有队列有容量与溢出策略。 |
| NFR24-02 | 同输入字节、配置、时钟序列与导入物，决策/拒绝码/业务摘要 hash 可重复；处理顺序由录制序列和受管时钟确定，不能用回放墙钟产生历史信号。 |
| NFR24-03 | 一分钟决策运算 p99 ≤ 100ms，tick 特征回调 p99 ≤ 5ms，idle 调度间隔 ≤ 250ms，测试必须报告机器/样本/负载及尾延迟；这些为工程目标，不是交易所端到端延迟承诺。 |
| NFR24-04 | 在冻结峰值负载的 2 倍、连续 60 分钟下队列/窗口内存有界、关键事件无丢失；若溢出或 deadline 超限，锁存禁止开仓并输出证据不完整状态。 |
| NFR24-05 | 金额、手数、费用、取整使用 Decimal 或整数最小单位；信号统计允许浮点但定义容差；比较边际必须严格大于阈值，禁止 NaN 或无穷穿透。 |
| NFR24-06 | 不新增 metaclass，不破坏框架 API；共用能力只在 `backtrader`、`bt_api_py` 或 `bt_api_ctp` 的明确 owner 层实现并提供第二消费方契约验证。本例目录是独立可运行的单策略入口，不得在运行时依赖其他 `examples/` 目录或 `examples` 公共包；本例只保留参数、信号与接线，不复制 013_3 大型 runner。 |
| NFR24-07 | 凭据仅来自忽略版本控制的本地环境，报告只保留非秘密账户指纹；日志、异常、effective config、manifest 不含原始账号、口令、签名秘密。 |
| NFR24-08 | 输出含实际导入 backtrader/bt_api_py/bt_api_ctp 路径、文件 hash 和 native 身份；仓库源码测试、安装包/native 测试和真实会话验证分层独立，不互相替代。 |
| NFR24-09 | 同一账户并发 writer、重启恢复、进程终止、磁盘错误、日切与断连都可注入测试；任一不确定事件保持停止开仓，恢复权限一次性、可撤销、可审计。 |
| NFR24-10 | 文档、配置 schema、错误码、需求/设计/验收 ID 双向追踪；报告使用 `PASS/FAIL/BLOCKED/NOT_RUN/NO-GO` 并附证据范围，禁止把未执行项写成通过。 |

## 5. 依赖和实现顺序

先完成公共合约资格、option fee/margin、三腿持久执行和账户预算能力，再实现本迭代的三腿 minute barrier 与 frozen tick window，随后接入信号和只读报告。缺少任一公共能力时可继续用本地 fixture 开发和验算，但 G2/G3/G4 必须按缺口保持 `BLOCKED`。

具体当前事实、拟议接口、GAP owner 和交付顺序见[设计文档](./设计文档.md)。本地 replay 仅允许评定 `LOCAL_REPLAY_PASS`；完整 G1 仍为 `INCOMPLETE`，G2 包/native、G3 第一套只读、G4 机械 SimNow、R1 样本外与 R2 自然信号仍为 `NOT_RUN`，并且不解除 production `NO-GO`。
