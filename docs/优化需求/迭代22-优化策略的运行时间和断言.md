1. 运行tests/strategies/中的每个策略，如果运行时间超过了20s，尝试减少数据的长度，减少策略的运行时间在20s以内。
2. 测试用例的断言需要判断bar_num, 年化收益率，夏普率，最大回撤这几个指标，不能使用范围太大的，参考：
assert strat.bar_num == 1000, "bar_num should be 1000， got {strat.bar_num}"
    assert abs(final_value - 49739.00) < 0.01, f"Expected final_value=49739.00, got {final_value}"
    assert abs(sharpe_ratio - (-2.519733167360895)) < 1e-6, f"Expected sharpe_ratio=-2.519733167360895, got {sharpe_ratio}"
    assert abs(annual_return - (-0.002618603816279576)) < 1e-6, f"Expected annual_return=-0.002618603816279576, got {annual_return}"
    assert abs(max_drawdown - 0.8234965704259053) < 1e-6, f"Expected max_drawdown=0.8234965704259053, got {max_drawdown}"
