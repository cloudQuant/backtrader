<!-- bt.observers.TradeLogger 这个现在记录了order, trade, position等数据的日志，希望也能增加一个data.log的日志，可以记录每个bar的时间，开盘价，最高价，最低价，收盘价，成交量，如果这个bar还有其他指标，也增加进去，比如持仓量，还有其他自定义的字段；

这样就方便在每次运行结束之后，分析每个bar的数据与交易记录的对应关系，以及分析策略的表现。

完成之后，优化一下之前的测试用例，确保测试用例也能覆盖当前的情况，并且测试能够通过。 -->

<!-- 1. 现在输出的csv文件是运行结束才会写入还是运行过程中就会写入？需要在运行过程中就能写入；另外希望写入的文件默认是log文件，这样方便追加写入；
2. 希望保存一个current_position.json的文件，能够保存当前bar运行之后的持仓 
3. 希望能在data中增加一些数据列，能够把策略中计算的指标放到data文件中，
4. 希望能够对data,order, position, trade这些文件类型可以配置的，默认是log形式，也可以选择csv -->

1. 我希望写入到日志中的记录，有一个详细的写入时间，放到最开始
2. 分析一下，用于记录运行中的日志的数据表如何创建比较合适？现在有data数据，列并不一定完全一样；data数据不要保存到数据表里面了；order,position,trade这些字段都是一致的，需要保存到数据表里面。根据行业最佳实践，设计比较好的mysql数据表，确保不同策略，同一个策略的不同参数，不同时间运行，这些都能保存下来到数据表中，方便后续区分
3. 根据数据表分析的结果，创建数据库，数据库名字：backtrder_web;数据表根据需要创建，写一个脚本，运行的时候输出root账户的密码，就可以创建具体的数据库，数据库的密码需要手动输入，可以选择，如果不手动输入，默认是backtrader_web_123
4. 写好脚本之后，运行一下这个脚本，让我手动输入密码；如果创建成功，输出创建成功的提示；如果创建失败，输出创建失败的提示
5. 写一个测试用例，确保这个保存到数据表中的功能能够正常实现。