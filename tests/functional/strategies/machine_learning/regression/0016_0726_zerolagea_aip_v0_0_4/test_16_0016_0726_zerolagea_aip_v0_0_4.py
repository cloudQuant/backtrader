"""Inlined regression test for tests/functional/strategies/machine_learning/regression/0016_0726_zerolagea_aip_v0_0_4.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original config.yaml, run.py, strategy_*.py, and expected.json have been
collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own extract_metrics() output captured at
migration time.
"""
from __future__ import annotations
import math
from pathlib import Path
import io
import datetime
import sys
import backtrader as bt
import pandas as pd
import pytest

_REPO = Path(__file__).resolve().parents[6]

_CONFIG = {
    'strategy': {
        'name': 'ZeroLagEA-AIP_v0.0.4',
        'source_ea': 'ea/0726_ZeroLagEA-AIP_v0.0.4',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'feature_cache_dir': 'features',
        'bar_shift_minutes': 15,
    },
    'params': {
        'fast_ema': 2,
        'slow_ema': 34,
        'use_fresh_macd_sig': 1,
        'lots': 2.0,
        'start_hour': 9,
        'end_hour': 15,
        'kill_day': 5,
        'kill_hour': 21,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


def _resolve_repo_paths(node):
    """Replace '{repo}' placeholder in config string values with absolute repo path."""
    if isinstance(node, dict):
        return {k: _resolve_repo_paths(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_resolve_repo_paths(v) for v in node]
    if isinstance(node, str):
        return node.replace('{repo}', str(_REPO))
    return node


def load_config():
    """Inlined config (was config.yaml)."""
    import copy
    return _resolve_repo_paths(copy.deepcopy(_CONFIG))





def load_mt5_csv(filepath, fromdate=None, todate=None, bar_shift_minutes=0):
    with open(filepath, 'r', encoding='utf-8') as f:
        lines = f.read().strip().split('\n')
    cleaned = '\n'.join(line.strip().strip('"') for line in lines)
    df = pd.read_csv(io.StringIO(cleaned), sep='\t')
    df['datetime'] = pd.to_datetime(df['<DATE>'] + ' ' + df['<TIME>'], format='%Y.%m.%d %H:%M:%S')
    df = df.rename(columns={
        '<OPEN>': 'open',
        '<HIGH>': 'high',
        '<LOW>': 'low',
        '<CLOSE>': 'close',
        '<TICKVOL>': 'tick_volume',
        '<VOL>': 'real_volume',
    })
    df['openinterest'] = 0
    df['volume'] = df['tick_volume']
    df = df[['datetime', 'open', 'high', 'low', 'close', 'volume', 'openinterest']]
    df = df.set_index('datetime')
    if bar_shift_minutes:
        df.index = df.index + pd.Timedelta(minutes=bar_shift_minutes)
    if fromdate is not None:
        df = df[df.index >= fromdate]
    if todate is not None:
        df = df[df.index <= todate]
    return df


class Mt5PandasFeed(bt.feeds.PandasData):
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2), ('close', 3), ('volume', 4), ('openinterest', 5),
    )


class ZeroLagMacd(bt.Indicator):
    lines = ('macd', 'signal')
    params = dict(fast=12, slow=26)

    def __init__(self):
        ema_fast = bt.indicators.ExponentialMovingAverage(self.data, period=self.p.fast)
        ema_fast2 = bt.indicators.ExponentialMovingAverage(ema_fast, period=self.p.fast)
        zlema_fast = 2.0 * ema_fast - ema_fast2
        ema_slow = bt.indicators.ExponentialMovingAverage(self.data, period=self.p.slow)
        ema_slow2 = bt.indicators.ExponentialMovingAverage(ema_slow, period=self.p.slow)
        zlema_slow = 2.0 * ema_slow - ema_slow2
        self.lines.macd = zlema_fast - zlema_slow
        self.lines.signal = bt.indicators.ExponentialMovingAverage(self.lines.macd, period=9)


class ZeroLagEAAIPStrategy(bt.Strategy):
    params = dict(
        fast_ema=2,
        slow_ema=34,
        use_fresh_macd_sig=1,
        lots=2.0,
        start_hour=9,
        end_hour=15,
        kill_day=5,
        kill_hour=21,
    )

    def __init__(self):
        self.zlmacd = ZeroLagMacd(self.data.close, fast=self.p.fast_ema, slow=self.p.slow_ema)

        self.bar_num = 0
        self.signal_count = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0
        self.completed_order_count = 0
        self.rejected_order_count = 0

        self.order = None

    def _in_session(self):
        dt = bt.num2date(self.data.datetime[0])
        if dt.hour < int(self.p.start_hour) or dt.hour >= int(self.p.end_hour):
            return False
        if dt.weekday() == int(self.p.kill_day) and dt.hour == int(self.p.kill_hour):
            return False
        return True

    def _kill_time(self):
        dt = bt.num2date(self.data.datetime[0])
        return dt.weekday() == int(self.p.kill_day) and dt.hour == int(self.p.kill_hour)

    def _fresh_signal(self):
        main_prev = float(self.zlmacd.macd[-1])
        signal_prev = float(self.zlmacd.signal[-1])
        main_curr = float(self.zlmacd.macd[0])
        signal_curr = float(self.zlmacd.signal[0])
        return (signal_prev > main_prev and signal_curr < main_curr) or (signal_prev < main_prev and signal_curr > main_curr)

    def next(self):
        self.bar_num += 1
        if len(self) < max(self.p.fast_ema, self.p.slow_ema) + 5:
            return
        if self.order is not None:
            return
        if not self._in_session():
            if self.position:
                self.order = self.close()
            return
        if self._kill_time() and self.position:
            self.order = self.close()
            return
        if int(self.p.use_fresh_macd_sig) == 1 and not self._fresh_signal():
            return
        main_now = float(self.zlmacd.macd[0])
        signal_now = float(self.zlmacd.signal[0])
        buy_sig = signal_now < main_now
        sell_sig = signal_now > main_now
        if self.position:
            if self.position.size > 0 and sell_sig:
                self.order = self.close()
                return
            if self.position.size < 0 and buy_sig:
                self.order = self.close()
                return
            return
        if buy_sig:
            self.signal_count += 1
            self.order = self.buy(size=self.p.lots)
            return
        if sell_sig:
            self.signal_count += 1
            self.order = self.sell(size=self.p.lots)

    def notify_order(self, order):
        if order.status in [bt.Order.Submitted, bt.Order.Accepted]:
            return
        if order.status == bt.Order.Completed:
            self.completed_order_count += 1
            if self.position:
                if order.executed.size > 0:
                    self.buy_count += 1
                elif order.executed.size < 0:
                    self.sell_count += 1
        elif order.status in [bt.Order.Canceled, bt.Order.Margin, bt.Order.Rejected, bt.Order.Expired]:
            self.rejected_order_count += 1
        if self.order is not None and order.ref == self.order.ref and order.status not in [bt.Order.Submitted, bt.Order.Accepted]:
            self.order = None

    def notify_trade(self, trade):
        if not trade.isclosed:
            return
        self.trade_count += 1
        if trade.pnlcomm >= 0:
            self.win_count += 1
        else:
            self.loss_count += 1



BASE_DIR = Path(__file__).resolve().parent

WORKSPACE_DIR = BASE_DIR.parents[2]
LOCAL_BACKTRADER_REPO = WORKSPACE_DIR / 'backtrader'
if LOCAL_BACKTRADER_REPO.exists() and str(LOCAL_BACKTRADER_REPO) not in sys.path:
    sys.path.insert(0, str(LOCAL_BACKTRADER_REPO))



MINUTES_PER_TRADING_YEAR = 24 * 60 * 252



def resolve_data_path(filename):
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def load_backtest_frame(config):
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(resolve_data_path(data_cfg['file']), fromdate=fromdate, todate=todate, bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0))
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f'Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}')
    return {'data': df, 'fromdate': fromdate, 'todate': todate}


def build_cerebro(config, frame):
    bt_cfg = config['backtest']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(commission=bt_cfg['commission'], margin=bt_cfg['margin'], mult=bt_cfg['multiplier'], commtype=comm_type, stocklike=bt_cfg.get('stocklike', False))
    feed = Mt5PandasFeed(dataname=frame['data'][['open', 'high', 'low', 'close', 'volume', 'openinterest']], timeframe=bt.TimeFrame.Minutes, compression=15)
    cerebro.adddata(feed, name='XAUUSD_M15')
    cerebro.addstrategy(ZeroLagEAAIPStrategy, **config.get('params', {}))
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=15, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')
    return cerebro


def extract_metrics(strat, cerebro, frame, config):
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    initial_cash = config['backtest']['initial_cash']
    final_value = cerebro.broker.getvalue()
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    total_trades = trades.get('total', {}).get('closed', won + lost)
    gross_won = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gross_lost = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {
        'fromdate': frame['fromdate'],
        'todate': frame['todate'],
        'bars': len(frame['data']),
        'bar_num': strat.bar_num,
        'signal_count': strat.signal_count,
        'buy_count': strat.buy_count,
        'sell_count': strat.sell_count,
        'trade_count': strat.trade_count,
        'completed_orders': strat.completed_order_count,
        'rejected_orders': strat.rejected_order_count,
        'initial_cash': initial_cash,
        'final_value': final_value,
        'net_pnl': final_value - initial_cash,
        'total_return_pct': (final_value / initial_cash - 1) * 100,
        'total_trades': total_trades,
        'won': won,
        'lost': lost,
        'win_rate': (won / total_trades * 100) if total_trades else 0,
        'profit_factor': (gross_won / gross_lost) if gross_lost else None,
        'sharpe_ratio': sharpe.get('sharperatio'),
        'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sqn': sqn.get('sqn'),
    }


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def _resolve_loader():
    """Locate the data-loading helper (varies by strategy)."""
    for name in ("load_inputs", "load_data", "load_backtest_frame", "prepare_inputs", "prepare_data"):
        fn = globals().get(name)
        if callable(fn):
            return fn
    raise RuntimeError("No inputs loader found in inlined module")


def _build_cerebro_compat(inputs, config):
    """Call build_cerebro with whichever signature the original used."""
    import inspect
    sig = inspect.signature(build_cerebro)
    params = list(sig.parameters.keys())
    if params and params[0].lower() in ("config", "cfg", "configuration"):
        return build_cerebro(config, inputs)
    try:
        return build_cerebro(inputs, config)
    except TypeError:
        return build_cerebro(config, inputs)


def _extract_metrics_compat(strat, cerebro, inputs, config):
    """Call extract_metrics with whichever signature the original used."""
    for args in (
        (strat, cerebro, inputs, config),
        (strat, cerebro, config, inputs),
        (strat, cerebro, inputs),
        (strat, cerebro),
    ):
        try:
            return extract_metrics(*args)
        except TypeError:
            continue
    raise RuntimeError("extract_metrics failed for all argument orderings")


def test_16_0016_0726_zerolagea_aip_v0_0_4() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/machine_learning/0016_0726_zerolagea_aip_v0_0_4.
    """
    config = load_config()
    inputs = _resolve_loader()(config)
    cerebro = _build_cerebro_compat(inputs, config)
    results = cerebro.run(runonce=True)
    metrics = _extract_metrics_compat(results[0], cerebro, inputs, config)

    assert metrics.get('bar_num') == 6055, f"bar_num: expected=6055, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 115, f"buy_count: expected=115, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 80, f"sell_count: expected=80, got={metrics.get('sell_count')!r}"
    assert metrics.get('total_trades') == 195, f"total_trades: expected=195, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 195, f"trade_count: expected=195, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 60, f"won: expected=60, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 135, f"lost: expected=135, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 6129.0, tol=6.129000e-03, key='bars')
    _close(metrics.get('signal_count'), 195.0, tol=1.950000e-04, key='signal_count')
    _close(metrics.get('completed_orders'), 390.0, tol=3.900000e-04, key='completed_orders')
    _close(metrics.get('rejected_orders'), 0.0, tol=1.000000e-06, key='rejected_orders')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_value'), 997379.9999999995, tol=9.973800e-01, key='final_value')
    _close(metrics.get('net_pnl'), -2620.0000000004657, tol=2.620000e-03, key='net_pnl')
    _close(metrics.get('total_return_pct'), -0.2620000000000511, tol=1.000000e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 30.76923076923077, tol=3.076923e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 0.9844127410968171, tol=1.000000e-06, key='profit_factor')
    _close(metrics.get('sharpe_ratio'), -0.24591477913848211, tol=1.000000e-06, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), -14.386399664081095, tol=1.438640e-05, key='annual_return_pct')
    _close(metrics.get('max_drawdown'), 4.698857061775375, tol=4.698857e-06, key='max_drawdown')
    _close(metrics.get('sqn'), -0.05289957347742857, tol=1.000000e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
