"""Inlined regression test for tests/functional/strategies/mean_reversion/regression/0307_1148_combo_right.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original config.yaml, run.py, strategy_*.py, and expected.json have been
collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own extract_metrics() output captured at
migration time.
"""
from __future__ import annotations
import math
from pathlib import Path
import io
import datetime
import sys
import backtrader.feeds as btfeeds
import backtrader as bt
import pandas as pd
import pytest

_REPO = Path(__file__).resolve().parents[4]

_CONFIG = {
    'strategy': {
        'name': 'Combo_Right',
        'source_ea': 'ea/1148_Combo_Right',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
    },
    'params': {
        'tp1': 500,
        'sl1': 500,
        'cci_period': 10,
        'cci_price': 1,
        'x12': 100,
        'x22': 100,
        'x32': 100,
        'x42': 100,
        'tp2': 500,
        'sl2': 500,
        'p2': 20,
        'x13': 100,
        'x23': 100,
        'x33': 100,
        'x43': 100,
        'tp3': 500,
        'sl3': 500,
        'p3': 20,
        'x14': 100,
        'x24': 100,
        'x34': 100,
        'x44': 100,
        'p4': 20,
        'mode_pass': 4,
        'lots': 0.1,
        'shift': 1,
        'point': 0.01,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


def _resolve_repo_paths(node):
    """Replace '{repo}' placeholder in config string values with absolute repo path."""
    if isinstance(node, dict):
        return {k: _resolve_repo_paths(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_resolve_repo_paths(v) for v in node]
    if isinstance(node, str):
        return node.replace('{repo}', str(_REPO))
    return node


def load_config():
    """Inlined config (was config.yaml)."""
    import copy
    return _resolve_repo_paths(copy.deepcopy(_CONFIG))





def load_mt5_csv(filepath, fromdate=None, todate=None, bar_shift_minutes=0):
    with open(filepath, 'r', encoding='utf-8') as f:
        lines = f.read().strip().split('\n')
    cleaned = '\n'.join(line.strip().strip('"') for line in lines)
    df = pd.read_csv(io.StringIO(cleaned), sep='\t')
    df['datetime'] = pd.to_datetime(df['<DATE>'] + ' ' + df['<TIME>'], format='%Y.%m.%d %H:%M:%S')
    df = df.rename(columns={
        '<OPEN>': 'open',
        '<HIGH>': 'high',
        '<LOW>': 'low',
        '<CLOSE>': 'close',
        '<TICKVOL>': 'volume',
        '<VOL>': 'openinterest',
    })
    df = df[['datetime', 'open', 'high', 'low', 'close', 'volume', 'openinterest']]
    df = df.set_index('datetime')
    if bar_shift_minutes:
        df.index = df.index + pd.Timedelta(minutes=bar_shift_minutes)
    if fromdate is not None:
        df = df[df.index >= fromdate]
    if todate is not None:
        df = df[df.index <= todate]
    return df


class Mt5PandasFeed(btfeeds.PandasData):
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2),
        ('close', 3), ('volume', 4), ('openinterest', 5),
    )


class AppliedPriceCCI(bt.Indicator):
    lines = ('cci',)
    params = dict(period=14, factor=0.015)

    def __init__(self):
        self.addminperiod(int(self.p.period) + 1)

    def next(self):
        period = int(self.p.period)
        prices = [float(self.data[-i]) for i in range(period)]
        mean_price = sum(prices) / period
        mean_dev = sum(abs(price - mean_price) for price in prices) / period
        denom = float(self.p.factor) * mean_dev
        if denom == 0:
            self.lines.cci[0] = 0.0
            return
        self.lines.cci[0] = (float(self.data[0]) - mean_price) / denom


class ComboRightStrategy(bt.Strategy):
    params = dict(
        tp1=500,
        sl1=500,
        cci_period=10,
        cci_price=1,
        x12=100,
        x22=100,
        x32=100,
        x42=100,
        tp2=500,
        sl2=500,
        p2=20,
        x13=100,
        x23=100,
        x33=100,
        x43=100,
        tp3=500,
        sl3=500,
        p3=20,
        x14=100,
        x24=100,
        x34=100,
        x44=100,
        p4=20,
        mode_pass=4,
        lots=0.1,
        shift=1,
        point=0.01,
    )

    def __init__(self):
        self.cci = AppliedPriceCCI(self._price_line(self.p.cci_price), period=self.p.cci_period)

        self.bar_num = 0
        self.signal_count = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0

        self.order = None
        self.stop_price = None
        self.take_price = None
        self._position_was_open = False
        self.sl = float(self.p.sl1)
        self.tp = float(self.p.tp1)

        max_shift = max(int(self.p.shift) + int(self.p.p2) * 4, int(self.p.shift) + int(self.p.p3) * 4, int(self.p.shift) + int(self.p.p4) * 4)
        self.addminperiod(max(self.p.cci_period, max_shift) + 5)

    def _price_line(self, price_code):
        code = int(price_code)
        if code == 0:
            return self.data.close
        if code == 1:
            return self.data.open
        if code == 2:
            return self.data.high
        if code == 3:
            return self.data.low
        if code == 4:
            return (self.data.high + self.data.low) / 2.0
        if code == 5:
            return (self.data.high + self.data.low + self.data.close) / 3.0
        if code == 6:
            return (self.data.high + self.data.low + self.data.close + self.data.close) / 4.0
        return self.data.open

    def log(self, text):
        dt = bt.num2date(self.data.datetime[0])
        print(f'{dt.isoformat()}, {text}')

    def _perceptron(self, shifts, weights):
        close_1 = float(self.data.close[-shifts[0]])
        close_2 = float(self.data.close[-shifts[1]])
        close_3 = float(self.data.close[-shifts[2]])
        close_4 = float(self.data.close[-shifts[3]])
        close_5 = float(self.data.close[-shifts[4]])
        a1 = close_1 - close_2
        a2 = close_2 - close_3
        a3 = close_3 - close_4
        a4 = close_4 - close_5
        return weights[0] * a1 + weights[1] * a2 + weights[2] * a3 + weights[3] * a4

    def _basic_signal(self):
        idx = -int(self.p.shift) if int(self.p.shift) > 0 else 0
        return float(self.cci[idx])

    def _supervisor(self):
        basic_sig = self._basic_signal()
        signal = basic_sig
        self.sl = float(self.p.sl1)
        self.tp = float(self.p.tp1)

        pass_value = int(self.p.mode_pass)
        shift = int(self.p.shift)

        sell_weights = [int(self.p.x12) - 100, int(self.p.x22) - 100, int(self.p.x32) - 100, int(self.p.x42) - 100]
        buy_weights = [int(self.p.x13) - 100, int(self.p.x23) - 100, int(self.p.x33) - 100, int(self.p.x43) - 100]
        sum_weights = [int(self.p.x14) - 100, int(self.p.x24) - 100, int(self.p.x34) - 100, int(self.p.x44) - 100]

        sell_shifts = [shift, shift + int(self.p.p2), shift + int(self.p.p2) * 2, shift + int(self.p.p2) * 3, shift + int(self.p.p2) * 4]
        buy_shifts = [shift, shift + int(self.p.p3), shift + int(self.p.p3) * 2, shift + int(self.p.p3) * 3, shift + int(self.p.p3) * 4]
        sum_shifts = [shift, shift + int(self.p.p4), shift + int(self.p.p4) * 2, shift + int(self.p.p4) * 3, shift + int(self.p.p4) * 4]

        if pass_value == 4:
            output1 = self._perceptron(sell_shifts, sell_weights)
            output2 = self._perceptron(buy_shifts, buy_weights)
            output3 = self._perceptron(sum_shifts, sum_weights)
            if output3 > 0 and output2 > 0:
                self.sl = float(self.p.sl3)
                self.tp = float(self.p.tp3)
                return 1.0
            if output3 <= 0 and output1 < 0:
                self.sl = float(self.p.sl2)
                self.tp = float(self.p.tp2)
                return -1.0
            return basic_sig

        if pass_value == 3:
            output2 = self._perceptron(buy_shifts, buy_weights)
            if output2 > 0:
                self.sl = float(self.p.sl3)
                self.tp = float(self.p.tp3)
                return 1.0
            return basic_sig

        if pass_value == 2:
            output1 = self._perceptron(sell_shifts, sell_weights)
            if output1 < 0:
                self.sl = float(self.p.sl2)
                self.tp = float(self.p.tp2)
                return -1.0
            return basic_sig

        return basic_sig

    def _check_exit_levels(self):
        if not self.position or self.order is not None:
            return False
        high = float(self.data.high[0])
        low = float(self.data.low[0])
        if self.position.size > 0:
            if self.stop_price is not None and low <= self.stop_price:
                self.log(f'close long by stop={self.stop_price:.5f}')
                self.order = self.close()
                return True
            if self.take_price is not None and high >= self.take_price:
                self.log(f'close long by take={self.take_price:.5f}')
                self.order = self.close()
                return True
        else:
            if self.stop_price is not None and high >= self.stop_price:
                self.log(f'close short by stop={self.stop_price:.5f}')
                self.order = self.close()
                return True
            if self.take_price is not None and low <= self.take_price:
                self.log(f'close short by take={self.take_price:.5f}')
                self.order = self.close()
                return True
        return False

    def next(self):
        self.bar_num += 1
        if self.order is not None:
            return

        if self._check_exit_levels():
            return
        if self.position:
            return

        signal = self._supervisor()
        if signal > 0 or signal < 0:
            self.signal_count += 1
        lot = max(round(float(self.p.lots), 2), 0.01)
        px = float(self.data.close[0])

        if signal > 0:
            self.stop_price = px - float(self.p.point) * float(self.sl) if self.sl > 0 else None
            self.take_price = px + float(self.p.point) * float(self.tp) if self.tp > 0 else None
            self.log(f'buy signal signal={signal:.5f} sl={self.sl:.1f} tp={self.tp:.1f} lot={lot:.2f}')
            self.order = self.buy(size=lot)
            return

        if signal < 0:
            self.stop_price = px + float(self.p.point) * float(self.sl) if self.sl > 0 else None
            self.take_price = px - float(self.p.point) * float(self.tp) if self.tp > 0 else None
            self.log(f'sell signal signal={signal:.5f} sl={self.sl:.1f} tp={self.tp:.1f} lot={lot:.2f}')
            self.order = self.sell(size=lot)
            return

    def notify_order(self, order):
        if order.status in [order.Submitted, order.Accepted]:
            return
        if order.status in [order.Canceled, order.Margin, order.Rejected]:
            self.log(f'order failed status={order.getstatusname()}')
        self.order = None

    def notify_trade(self, trade):
        if trade.isopen and not self._position_was_open:
            if trade.size > 0:
                self.buy_count += 1
            elif trade.size < 0:
                self.sell_count += 1
            self._position_was_open = True
            return
        if not trade.isclosed:
            return
        self.trade_count += 1
        if trade.pnlcomm >= 0:
            self.win_count += 1
        else:
            self.loss_count += 1
        self._position_was_open = False
        self.stop_price = None
        self.take_price = None
        self.log(f'trade closed pnl={trade.pnlcomm:.2f}')



WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_REPO = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_REPO) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_REPO))



BASE_DIR = Path(__file__).resolve().parent

MINUTES_PER_TRADING_YEAR = 24 * 60 * 252



def resolve_data_path(filename):
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def load_backtest_frame(config):
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(
        resolve_data_path(data_cfg['file']),
        fromdate=fromdate,
        todate=todate,
        bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0),
    )
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f"Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}")
    return {'data': df, 'fromdate': fromdate, 'todate': todate}


def build_cerebro(config, frame):
    bt_cfg = config['backtest']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(
        commission=bt_cfg['commission'],
        margin=bt_cfg['margin'],
        mult=bt_cfg['multiplier'],
        commtype=comm_type,
        stocklike=bt_cfg.get('stocklike', False),
    )
    feed = Mt5PandasFeed(dataname=frame['data'], timeframe=bt.TimeFrame.Minutes, compression=15)
    cerebro.adddata(feed, name=f"{config['data']['symbol']}_{config['data']['timeframe']}")
    cerebro.addstrategy(ComboRightStrategy, **config.get('params', {}))
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=15, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')
    return cerebro


def extract_metrics(strat, cerebro, frame, config):
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    initial_cash = config['backtest']['initial_cash']
    final_value = cerebro.broker.getvalue()
    total_trades = trades.get('total', {}).get('total', 0)
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    gross_won = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gross_lost = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {
        'fromdate': frame['fromdate'],
        'todate': frame['todate'],
        'bars': len(frame['data']),
        'bar_num': strat.bar_num,
        'signal_count': strat.signal_count,
        'buy_count': strat.buy_count,
        'sell_count': strat.sell_count,
        'trade_count': strat.trade_count,
        'win_count': strat.win_count,
        'loss_count': strat.loss_count,
        'initial_cash': initial_cash,
        'final_cash': cerebro.broker.getcash(),
        'final_value': final_value,
        'net_pnl': final_value - initial_cash,
        'total_return_pct': (final_value / initial_cash - 1) * 100,
        'total_trades': total_trades,
        'won': won,
        'lost': lost,
        'win_rate': (won / total_trades * 100) if total_trades else 0,
        'profit_factor': (gross_won / gross_lost) if gross_lost else None,
        'open_position_size': strat.position.size,
        'open_position_price': strat.position.price,
        'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sharpe_ratio': sharpe.get('sharperatio'),
        'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'sqn': sqn.get('sqn'),
    }


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def _resolve_loader():
    """Locate the data-loading helper (varies by strategy)."""
    for name in ("load_inputs", "load_data", "load_backtest_frame", "prepare_inputs", "prepare_data"):
        fn = globals().get(name)
        if callable(fn):
            return fn
    raise RuntimeError("No inputs loader found in inlined module")


def _build_cerebro_compat(inputs, config):
    """Call build_cerebro with whichever signature the original used."""
    import inspect
    sig = inspect.signature(build_cerebro)
    params = list(sig.parameters.keys())
    if params and params[0].lower() in ("config", "cfg", "configuration"):
        return build_cerebro(config, inputs)
    try:
        return build_cerebro(inputs, config)
    except TypeError:
        return build_cerebro(config, inputs)


def _extract_metrics_compat(strat, cerebro, inputs, config):
    """Call extract_metrics with whichever signature the original used."""
    for args in (
        (strat, cerebro, inputs, config),
        (strat, cerebro, config, inputs),
        (strat, cerebro, inputs),
        (strat, cerebro),
    ):
        try:
            return extract_metrics(*args)
        except TypeError:
            continue
    raise RuntimeError("extract_metrics failed for all argument orderings")


def test_308_0307_1148_combo_right() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/mean_reversion/0307_1148_combo_right.
    """
    config = load_config()
    inputs = _resolve_loader()(config)
    cerebro = _build_cerebro_compat(inputs, config)
    results = cerebro.run(runonce=True)
    metrics = _extract_metrics_compat(results[0], cerebro, inputs, config)

    assert metrics.get('bar_num') == 6119, f"bar_num: expected=6119, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 1328, f"buy_count: expected=1328, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 1107, f"sell_count: expected=1107, got={metrics.get('sell_count')!r}"
    assert metrics.get('win_count') == 1209, f"win_count: expected=1209, got={metrics.get('win_count')!r}"
    assert metrics.get('loss_count') == 1225, f"loss_count: expected=1225, got={metrics.get('loss_count')!r}"
    assert metrics.get('total_trades') == 2435, f"total_trades: expected=2435, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 2434, f"trade_count: expected=2434, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 1209, f"won: expected=1209, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 1225, f"lost: expected=1225, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 6129.0, tol=6.129000e-03, key='bars')
    _close(metrics.get('signal_count'), 2435.0, tol=2.435000e-03, key='signal_count')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_cash'), 1001345.299000003, tol=1.001345e+00, key='final_cash')
    _close(metrics.get('final_value'), 1001345.3000000031, tol=1.001345e+00, key='final_value')
    _close(metrics.get('net_pnl'), 1345.3000000030734, tol=1.345300e-03, key='net_pnl')
    _close(metrics.get('total_return_pct'), 0.13453000000029913, tol=1.000000e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 49.65092402464066, tol=4.965092e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 1.013742989802701, tol=1.013743e-06, key='profit_factor')
    _close(metrics.get('open_position_size'), 0.1, tol=1.000000e-06, key='open_position_size')
    _close(metrics.get('open_position_price'), 5175.81, tol=5.175810e-03, key='open_position_price')
    _close(metrics.get('max_drawdown'), 0.6497622946893601, tol=1.000000e-06, key='max_drawdown')
    _close(metrics.get('sharpe_ratio'), 1.6703126344234374, tol=1.670313e-06, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), 8.285136289212854, tol=8.285136e-06, key='annual_return_pct')
    _close(metrics.get('sqn'), 0.21662907316947502, tol=1.000000e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
