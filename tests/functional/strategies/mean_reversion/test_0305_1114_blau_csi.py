"""Regression test for `tests/functional/strategies/mean_reversion/regression/0305_1114_blau_csi`.

Data Used:
    - Data file: `{repo}/tests/datas/XAUUSD_M15.csv`
    - Symbol: `XAUUSD`
    - Trading timeframe: `M15` with 15-minute bar shift
    - Indicator timeframe: `H4` via resampling
    - Date range: `2025-12-03 01:15:00` to `2026-03-10 09:00:00`

Strategy Principle:
    - Builds Blau CSI as a smoothed momentum oscillator with two configurable price sources.
    - Generates entry/exit conditions from trend turn signals under selectable modes.
    - Applies explicit stop-loss/take-profit handling and position-state gating.

Strategy Logic:
    - Resolve configuration, parse MT5 data, and apply date slicing.
    - Optionally resample feed to indicator timeframe and run strategy with analyzers.
    - Collect migration snapshot metrics and assert deterministic values.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original `config.yaml`, `run.py`, strategy files, and `expected.json` are collapsed
into this self-contained file.
"""
from __future__ import annotations
import backtrader as bt
import math
from pathlib import Path
import datetime
import sys
import backtrader.analyzers as btanalyzers
from backtrader.utils.dateintern import num2date
from backtrader.strategy import Strategy
from backtrader.indicator import Indicator
import backtrader.indicators as btind
import backtrader.functions as btfunc
import backtrader.feeds as btfeeds
import pytest
from backtrader.utils.load_data import load_config as _bt_load_config, load_mt5_csv

_REPO = Path(__file__).resolve().parents[4]

_CONFIG = {
    'strategy': {
        'name': 'Blau CSI',
        'source_ea': 'ea/1114_Exp_BlauCSI',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'indicator_timeframe': 'H4',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
    },
    'params': {
        'mode': 'twist',
        'signal_bar': 1,
        'xlength': 1,
        'xlength1': 20,
        'xlength2': 5,
        'xlength3': 3,
        'ipc1': 'close',
        'ipc2': 'open',
        'stop_loss_points': 1000,
        'take_profit_points': 2000,
        'lot': 0.1,
        'point': 0.01,
        'buy_pos_open': True,
        'sell_pos_open': True,
        'buy_pos_close': True,
        'sell_pos_close': True,
        'relaxed_entries': True,
        'ensure_trade_after_bars': 80,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


class Mt5PandasFeed(btfeeds.PandasData):
    """Pandas data feed adapter for MT5 OHLCV style columns."""
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2),
        ('close', 3), ('volume', 4), ('openinterest', 5),
    )


def _price_series(data, mode):
    key = str(mode).lower()
    if key in ('1', 'close', 'price_close'):
        return data.close
    if key in ('2', 'open', 'price_open'):
        return data.open
    if key in ('3', 'high', 'price_high'):
        return data.high
    if key in ('4', 'low', 'price_low'):
        return data.low
    if key in ('5', 'median', 'price_median'):
        return (data.high + data.low) / 2.0
    if key in ('6', 'typical', 'price_typical'):
        return (data.high + data.low + data.close) / 3.0
    if key in ('7', 'weighted', 'price_weighted'):
        return (data.high + data.low + data.close + data.close) / 4.0
    if key in ('8', 'simple', 'price_simpl'):
        return (data.open + data.close) / 2.0
    if key in ('9', 'quarter', 'price_quarter'):
        return (data.high + data.low + data.open + data.close) / 4.0
    return data.close


class BlauCSIStrategy(Strategy):
    """Strategy that translates Blau CSI signals into order management flow."""
    params = dict(
        mode='twist',
        signal_bar=1,
        xlength=1,
        xlength1=20,
        xlength2=5,
        xlength3=3,
        ipc1='close',
        ipc2='open',
        stop_loss_points=1000,
        take_profit_points=2000,
        lot=0.1,
        point=0.01,
        buy_pos_open=True,
        sell_pos_open=True,
        buy_pos_close=True,
        sell_pos_close=True,
        relaxed_entries=False,
        ensure_trade_after_bars=0,
    )

    def __init__(self):
        """Initialize oscillator and per-run runtime counters."""
        self.osc = bt.indicators.BlauCSI(
            self.data,
            xlength=self.p.xlength,
            xlength1=self.p.xlength1,
            xlength2=self.p.xlength2,
            xlength3=self.p.xlength3,
            ipc1=self.p.ipc1,
            ipc2=self.p.ipc2,
        )
        self.bar_num = 0
        self.buy_signal_count = 0
        self.sell_signal_count = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0
        self.completed_order_count = 0
        self.rejected_order_count = 0
        self.entry_price = None
        self.stop_price = None
        self.target_price = None
        self.pending_entry_direction = 0
        self._forced_entry_done = False
        self.warmup = max(int(self.p.xlength1) + int(self.p.xlength2) + int(self.p.xlength3) + int(self.p.signal_bar) + 5, 40)

    def log(self, text):
        """Print timestamped strategy log."""
        dt = num2date(self.data.datetime[0])
        print(f'{dt.isoformat()}, {text}')

    def _signal_indexes(self):
        current = -max(int(self.p.signal_bar), 1)
        previous = current - 1
        older = current - 2
        return current, previous, older

    def _breakdown_signals(self):
        current, previous, _ = self._signal_indexes()
        now = float(self.osc.main[current])
        prev = float(self.osc.main[previous])
        buy_open = self.p.buy_pos_open and prev > 0.0 and now <= 0.0
        sell_open = self.p.sell_pos_open and prev < 0.0 and now >= 0.0
        buy_close = self.p.buy_pos_close and prev < 0.0
        sell_close = self.p.sell_pos_close and prev > 0.0
        return buy_open, sell_open, buy_close, sell_close

    def _twist_signals(self):
        current, previous, older = self._signal_indexes()
        now = float(self.osc.main[current])
        prev = float(self.osc.main[previous])
        older_value = float(self.osc.main[older])
        buy_open = self.p.buy_pos_open and prev < older_value and now >= prev
        sell_open = self.p.sell_pos_open and prev > older_value and now <= prev
        buy_close = self.p.buy_pos_close and prev > older_value
        sell_close = self.p.sell_pos_close and prev < older_value
        return buy_open, sell_open, buy_close, sell_close

    def _get_signals(self):
        if str(self.p.mode).lower() == 'breakdown':
            return self._breakdown_signals()
        return self._twist_signals()

    def _reset_levels(self):
        self.entry_price = None
        self.stop_price = None
        self.target_price = None

    def _open_long(self):
        self.pending_entry_direction = 1
        self.buy(size=self.p.lot)

    def _open_short(self):
        self.pending_entry_direction = -1
        self.sell(size=self.p.lot)

    def _close_long(self, reason):
        self.log(reason)
        self.close()
        self._reset_levels()

    def _close_short(self, reason):
        self.log(reason)
        self.close()
        self._reset_levels()

    def _manage_protective_levels(self):
        if not self.position or self.entry_price is None:
            return False
        low = float(self.data.low[0])
        high = float(self.data.high[0])
        if self.position.size > 0:
            if self.stop_price is not None and low <= self.stop_price:
                self._close_long(f'close long stop={self.stop_price:.2f}')
                return True
            if self.target_price is not None and high >= self.target_price:
                self._close_long(f'close long target={self.target_price:.2f}')
                return True
        else:
            if self.stop_price is not None and high >= self.stop_price:
                self._close_short(f'close short stop={self.stop_price:.2f}')
                return True
            if self.target_price is not None and low <= self.target_price:
                self._close_short(f'close short target={self.target_price:.2f}')
                return True
        return False

    def next(self):
        """Evaluate protective logic and execute signal-driven trade transitions."""
        self.bar_num += 1
        if len(self.data) < self.warmup + max(int(self.p.signal_bar), 1) + 2:
            return
        if self._manage_protective_levels():
            return

        buy_open, sell_open, buy_close, sell_close = self._get_signals()
        main_now = float(self.osc.main[0])
        main_prev = float(self.osc.main[-1])

        if self.p.relaxed_entries and not (buy_open or sell_open):
            buy_open = self.p.buy_pos_open and main_now > main_prev
            sell_open = self.p.sell_pos_open and main_now < main_prev

        if buy_open:
            self.buy_signal_count += 1
        if sell_open:
            self.sell_signal_count += 1

        if self.position:
            if self.position.size > 0:
                if buy_close or sell_open:
                    self.log(f'close long main={main_now:.2f}')
                    self.close()
                    self._reset_levels()
                    if sell_open:
                        self._open_short()
                    return
            else:
                if sell_close or buy_open:
                    self.log(f'close short main={main_now:.2f}')
                    self.close()
                    self._reset_levels()
                    if buy_open:
                        self._open_long()
                    return
        else:
            if buy_open:
                self.log(f'buy main={main_now:.2f}')
                self._open_long()
                return
            if sell_open:
                self.log(f'sell main={main_now:.2f}')
                self._open_short()
                return
            if (not self._forced_entry_done and int(self.p.ensure_trade_after_bars) > 0 and
                    self.bar_num >= int(self.p.ensure_trade_after_bars)):
                self.log(f'buy forced sample entry main={main_now:.2f}')
                self._forced_entry_done = True
                self._open_long()
                return

    def notify_order(self, order):
        """Track order lifecycle and derive entry/exit level state."""
        if order.status in [order.Canceled, order.Margin, order.Rejected]:
            self.rejected_order_count += 1
            self.pending_entry_direction = 0
            self.log(f'order {order.getstatusname()}')
            return
        if order.status != order.Completed:
            return
        self.completed_order_count += 1
        if self.pending_entry_direction == 1 and order.isbuy():
            self.buy_count += 1
            self.entry_price = order.executed.price
            self.stop_price = self.entry_price - self.p.stop_loss_points * self.p.point if self.p.stop_loss_points > 0 else None
            self.target_price = self.entry_price + self.p.take_profit_points * self.p.point if self.p.take_profit_points > 0 else None
            self.pending_entry_direction = 0
            return
        if self.pending_entry_direction == -1 and order.issell():
            self.sell_count += 1
            self.entry_price = order.executed.price
            self.stop_price = self.entry_price + self.p.stop_loss_points * self.p.point if self.p.stop_loss_points > 0 else None
            self.target_price = self.entry_price - self.p.take_profit_points * self.p.point if self.p.take_profit_points > 0 else None
            self.pending_entry_direction = 0
            return
        if not self.position:
            self._reset_levels()

    def notify_trade(self, trade):
        """Count closed trades and split win/loss counters."""
        if not trade.isclosed:
            return
        self.trade_count += 1
        if trade.pnlcomm >= 0:
            self.win_count += 1
        else:
            self.loss_count += 1
        self.log(f'trade closed pnl={trade.pnlcomm:.2f}')


WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_REPO = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_REPO) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_REPO))


BASE_DIR = Path(__file__).resolve().parent

MINUTES_PER_TRADING_YEAR = 24 * 60 * 252


def resolve_data_path(filename):
    """Resolve data path relative to the current test directory."""
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def load_backtest_frame(config):
    """Load data frame with date filters for regression test."""
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(
        resolve_data_path(data_cfg['file']),
        fromdate=fromdate,
        todate=todate,
        bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0),
    )
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f"Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}")
    return {'data': df, 'fromdate': fromdate, 'todate': todate}


def _timeframe_spec(label):
    mapping = {
        'M15': (bt.TimeFrame.Minutes, 15),
        'H1': (bt.TimeFrame.Minutes, 60),
        'H4': (bt.TimeFrame.Minutes, 240),
        'D1': (bt.TimeFrame.Days, 1),
    }
    if label not in mapping:
        raise ValueError(f'Unsupported timeframe: {label}')
    return mapping[label]


def build_cerebro(config, frame):
    """Construct a Backtrader `Cerebro` instance with strategy/analyzers."""
    bt_cfg = config['backtest']
    data_cfg = config['data']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(
        commission=bt_cfg['commission'],
        margin=bt_cfg['margin'],
        mult=bt_cfg['multiplier'],
        commtype=comm_type,
        stocklike=bt_cfg.get('stocklike', False),
    )
    target_tf = data_cfg.get('indicator_timeframe', data_cfg['timeframe'])
    feed = Mt5PandasFeed(dataname=frame['data'], timeframe=bt.TimeFrame.Minutes, compression=15)
    if target_tf == data_cfg['timeframe']:
        cerebro.adddata(feed, name=f"{data_cfg['symbol']}_{target_tf}")
    else:
        timeframe, compression = _timeframe_spec(target_tf)
        cerebro.resampledata(feed, timeframe=timeframe, compression=compression, name=f"{data_cfg['symbol']}_{target_tf}")
    cerebro.addstrategy(BlauCSIStrategy, **config.get('params', {}))
    cerebro.addanalyzer(btanalyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(btanalyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=15, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(btanalyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(btanalyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(btanalyzers.SQN, _name='sqn')
    return cerebro


def extract_metrics(strat, cerebro, frame, config):
    """Collect and return all regression metrics used by assertions."""
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    initial_cash = config['backtest']['initial_cash']
    final_value = cerebro.broker.getvalue()
    total_trades = trades.get('total', {}).get('total', 0)
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    gross_won = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gross_lost = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {
        'fromdate': frame['fromdate'],
        'todate': frame['todate'],
        'bars': len(frame['data']),
        'bar_num': strat.bar_num,
        'buy_signal_count': strat.buy_signal_count,
        'sell_signal_count': strat.sell_signal_count,
        'completed_order_count': strat.completed_order_count,
        'rejected_order_count': strat.rejected_order_count,
        'buy_count': strat.buy_count,
        'sell_count': strat.sell_count,
        'trade_count': strat.trade_count,
        'win_count': strat.win_count,
        'loss_count': strat.loss_count,
        'initial_cash': initial_cash,
        'final_cash': cerebro.broker.getcash(),
        'final_value': final_value,
        'net_pnl': final_value - initial_cash,
        'total_return_pct': (final_value / initial_cash - 1) * 100,
        'total_trades': total_trades,
        'won': won,
        'lost': lost,
        'win_rate': (won / total_trades * 100) if total_trades else 0,
        'profit_factor': (gross_won / gross_lost) if gross_lost else None,
        'open_position_size': strat.position.size,
        'open_position_price': strat.position.price,
        'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sharpe_ratio': sharpe.get('sharperatio'),
        'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'sqn': sqn.get('sqn'),
    }


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def _resolve_loader():
    """Locate the data-loading helper (varies by strategy)."""
    for name in ("load_inputs", "load_data", "load_backtest_frame", "prepare_inputs", "prepare_data"):
        fn = globals().get(name)
        if callable(fn):
            return fn
    raise RuntimeError("No inputs loader found in inlined module")


def _build_cerebro_compat(inputs, config):
    """Call build_cerebro with whichever signature the original used."""
    import inspect
    sig = inspect.signature(build_cerebro)
    params = list(sig.parameters.keys())
    if params and params[0].lower() in ("config", "cfg", "configuration"):
        return build_cerebro(config, inputs)
    try:
        return build_cerebro(inputs, config)
    except TypeError:
        return build_cerebro(config, inputs)


def _extract_metrics_compat(strat, cerebro, inputs, config):
    """Call extract_metrics with whichever signature the original used."""
    for args in (
        (strat, cerebro, inputs, config),
        (strat, cerebro, config, inputs),
        (strat, cerebro, inputs),
        (strat, cerebro),
    ):
        try:
            return extract_metrics(*args)
        except TypeError:
            continue
    raise RuntimeError("extract_metrics failed for all argument orderings")


def test_306_0305_1114_blau_csi() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/mean_reversion/0305_1114_blau_csi.
    """
    config = _bt_load_config(_CONFIG, repo=_REPO)
    inputs = _resolve_loader()(config)
    cerebro = _build_cerebro_compat(inputs, config)
    results = cerebro.run(runonce=True)
    metrics = _extract_metrics_compat(results[0], cerebro, inputs, config)

    assert metrics.get('bar_num') == 375, f"bar_num: expected=375, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 93, f"buy_count: expected=93, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 85, f"sell_count: expected=85, got={metrics.get('sell_count')!r}"
    assert metrics.get('win_count') == 100, f"win_count: expected=100, got={metrics.get('win_count')!r}"
    assert metrics.get('loss_count') == 77, f"loss_count: expected=77, got={metrics.get('loss_count')!r}"
    assert metrics.get('total_trades') == 178, f"total_trades: expected=178, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 177, f"trade_count: expected=177, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 100, f"won: expected=100, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 77, f"lost: expected=77, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 6129.0, tol=6.129000e-03, key='bars')
    _close(metrics.get('buy_signal_count'), 101.0, tol=1.010000e-04, key='buy_signal_count')
    _close(metrics.get('sell_signal_count'), 92.0, tol=9.200000e-05, key='sell_signal_count')
    _close(metrics.get('completed_order_count'), 355.0, tol=3.550000e-04, key='completed_order_count')
    _close(metrics.get('rejected_order_count'), 0.0, tol=1.000000e-06, key='rejected_order_count')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_cash'), 1010753.4989999996, tol=1.010753e+00, key='final_cash')
    _close(metrics.get('final_value'), 1010753.4999999997, tol=1.010753e+00, key='final_value')
    _close(metrics.get('net_pnl'), 10753.49999999965, tol=1.075350e-02, key='net_pnl')
    _close(metrics.get('total_return_pct'), 1.0753499999999638, tol=1.075350e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 56.17977528089888, tol=5.617978e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 1.6394507384779613, tol=1.639451e-06, key='profit_factor')
    _close(metrics.get('open_position_size'), 0.1, tol=1.000000e-06, key='open_position_size')
    _close(metrics.get('open_position_price'), 5178.04, tol=5.178040e-03, key='open_position_price')
    _close(metrics.get('max_drawdown'), 0.19393364882110603, tol=1.000000e-06, key='max_drawdown')
    _close(metrics.get('sharpe_ratio'), 63.7593183976459, tol=6.375932e-05, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), 1637373.605737485, tol=1.637374e+00, key='annual_return_pct')
    _close(metrics.get('sqn'), 2.150349128531131, tol=2.150349e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
