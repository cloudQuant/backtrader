# 0354 Exp_ColorX2MA_X2

## 策略来源

- MT5 源码：`ea/0354_Exp_ColorX2MA_X2/Exp_ColorX2MA_X2.mq5`
- 指标源码：`ea/0354_Exp_ColorX2MA_X2/ColorX2MA.mq5`
- 当前实现：`examples/0354_exp_colorx2ma_x2/`

## 策略逻辑

- 使用两个 `ColorX2MA` 层：
  - 慢层颜色决定趋势过滤方向。
  - 快层颜色翻转决定入场时机。
- 当慢层颜色为上升色时，只接受快层从上升色翻出后的多头触发。
- 当慢层颜色为下降色时，只接受快层从下降色翻出后的空头触发。
- 保留慢层方向平仓开关，以及固定 `SL/TP` 主流程。

## 与源码一致/差异说明

- 当前版本按源码结构保留了“慢层趋势过滤 + 快层颜色翻转触发”的双时间框架主流程。
- `TradeAlgorithms` 库文件未直接收录在当前目录中；当前迁移按同仓库内同类模板的单品种单仓语义处理开仓。
- 原 `ColorX2MA` 指标由两层平滑均线和颜色缓冲组成；当前版本用 `SMA` 链式近似实现默认参数流程。
- 原文示例为 `USDJPY` 上 `H6` 慢层、`M30` 快层；当前使用 `XAUUSD_M15.csv` 重采样近似运行环境。

## 首轮回测结果

- 数据区间：`2025-12-03 01:15:00` 至 `2026-03-10 09:00:00`
- 平仓笔数：`82`
- 期末权益：`103628.40`
- 最大回撤：`0.79%`
- 多头开仓：`56`
- 空头开仓：`26`

## 运行方式

```bash
python run.py
```

如需画图：

```bash
python run.py --plot
```
