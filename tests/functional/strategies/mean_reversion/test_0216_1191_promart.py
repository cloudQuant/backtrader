"""Inlined regression test for tests/functional/strategies/mean_reversion/regression/0216_1191_promart.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original config.yaml, run.py, strategy_*.py, and expected.json have been
collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own extract_metrics() output captured at
migration time.

Data Used:
    Symbol XAUUSD (gold spot) on the 15-minute (M15) timeframe, loaded from the
    MT5 export ``tests/datas/XAUUSD_M15.csv`` and spanning 2025-12-03 01:15 to
    2026-03-10 09:00. Each bar's timestamp is shifted forward by 15 minutes
    (``bar_shift_minutes``) so it is stamped at its close. A single data feed is
    used; no resampling.

Strategy Principle:
    This is the "ProMart" strategy, a dual-MACD entry system with a martingale
    money-management overlay. A fast MACD (computed on the median price) provides
    the turning-point entry trigger while a slower MACD confirms the trend
    direction. After a losing trade the strategy reverses direction and doubles
    the lot (up to ``doubling_count``), sizing positions from available cash and
    a margin-per-lot budget. Each position is protected by fixed point-based
    stop-loss and take-profit distances.

Strategy Logic:
    load_backtest_frame loads the M15 frame; build_cerebro wires the pandas feed,
    a fixed-commission futures broker, the strategy, and the analyzers. The
    strategy builds the entry and trend MACDs in __init__ and, each bar after
    warm-up, first manages stop/take-profit exits, then—when flat—picks a
    direction (MACD signal, or a reversal after a loss), sizes the martingale lot,
    and opens the trade. notify_order sets entry/stop/take prices on fills, while
    notify_trade tracks direction, win/loss counts, and the loss streak on close.
    extract_metrics consolidates analyzer output and martingale state, and the
    test forces runonce=True, runs the module via the loader/build/extract
    compatibility helpers, and asserts each metric against migration-time
    expectations.
"""
from __future__ import annotations
import backtrader as bt
import math
from pathlib import Path
import sys
import datetime
import backtrader.feeds as btfeeds
import pytest
from backtrader.utils.load_data import load_config as _bt_load_config, load_mt5_csv

_REPO = Path(__file__).resolve().parents[4]

_CONFIG = {
    'strategy': {
        'name': 'ProMart',
        'source_ea': 'ea/1191_ProMart',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'feature_cache_dir': 'features',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
    },
    'params': {
        'dml': 1000.0,
        'doubling_count': 1,
        'stop_points': 500,
        'take_points': 1500,
        'macd1_fast': 5,
        'macd1_slow': 20,
        'macd2_fast': 10,
        'macd2_slow': 15,
        'macd_signal': 3,
        'base_lot_scale': 0.001,
        'volume_min': 0.1,
        'volume_step': 0.1,
        'volume_max': 5.0,
        'margin_per_lot': 250.0,
        'point': 0.01,
        'price_digits': 2,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_SRC = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_SRC) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_SRC))


class Mt5PandasFeed(btfeeds.PandasData):
    """PandasData adapter for MT5-exported OHLCV bars."""
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2),
        ('close', 3), ('volume', 4), ('openinterest', 5),
    )


class ProMartStrategy(bt.Strategy):
    """ProMart strategy with dual MACD triggers and lot scaling after losses."""
    params = dict(
        dml=1000.0,
        doubling_count=1,
        stop_points=500,
        take_points=1500,
        macd1_fast=5,
        macd1_slow=20,
        macd2_fast=10,
        macd2_slow=15,
        macd_signal=3,
        base_lot_scale=0.001,
        volume_min=0.1,
        volume_step=0.1,
        volume_max=100.0,
        margin_per_lot=250.0,
        point=0.01,
        price_digits=2,
    )

    def __init__(self):
        """Initialize indicators, counters, and state used across trades."""
        self.median_price = (self.data.high + self.data.low) / 2.0
        self.macd_entry = bt.indicators.MACD(
            self.median_price,
            period_me1=self.p.macd1_fast,
            period_me2=self.p.macd1_slow,
            period_signal=self.p.macd_signal,
        )
        self.macd_trend = bt.indicators.MACD(
            self.median_price,
            period_me1=self.p.macd2_fast,
            period_me2=self.p.macd2_slow,
            period_signal=self.p.macd_signal,
        )
        self.bar_num = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0
        self._position_was_open = False
        self.order = None
        self.entry_price = None
        self.stop_price = None
        self.take_price = None
        self.loss_streak = 0
        self.last_lot = 0.0
        self.current_trade_direction = 0
        self.last_trade_direction = 0
        self.last_trade_was_loss = False

    def log(self, text):
        """Print a timestamped textual log message for diagnostics."""
        dt = bt.num2date(self.data.datetime[0])
        print(f'{dt.isoformat()}, {text}')

    def _round_volume_down(self, value):
        if value <= 0:
            return 0.0
        steps = int(value / self.p.volume_step)
        return round(steps * self.p.volume_step, 8)

    def _calc_base_lot(self):
        cash = float(self.broker.getcash())
        base_lot = cash / max(self.p.dml, 1.0) * self.p.base_lot_scale
        base_lot = self._round_volume_down(base_lot)
        if base_lot < self.p.volume_min:
            base_lot = self.p.volume_min
        base_lot = min(base_lot, self.p.volume_max)
        return round(base_lot, 8)

    def _calc_lot(self):
        cash = float(self.broker.getcash())
        base_lot = self._calc_base_lot()
        multiplier = 2 ** min(self.loss_streak, self.p.doubling_count)
        lot = self._round_volume_down(base_lot * multiplier)
        lot = min(lot, self.p.volume_max)
        while lot >= self.p.volume_min and cash < lot * self.p.margin_per_lot:
            lot = self._round_volume_down(lot - self.p.volume_step)
        if lot < self.p.volume_min:
            return 0.0
        return round(lot, 8)

    def _manage_position(self):
        if not self.position:
            return False
        if self.position.size > 0:
            if self.stop_price is not None and float(self.data.low[0]) <= self.stop_price:
                self.log(f'close long stop={self.stop_price:.2f}')
                self.order = self.close()
                return True
            if self.take_price is not None and float(self.data.high[0]) >= self.take_price:
                self.log(f'close long take={self.take_price:.2f}')
                self.order = self.close()
                return True
            return False
        if self.stop_price is not None and float(self.data.high[0]) >= self.stop_price:
            self.log(f'close short stop={self.stop_price:.2f}')
            self.order = self.close()
            return True
        if self.take_price is not None and float(self.data.low[0]) <= self.take_price:
            self.log(f'close short take={self.take_price:.2f}')
            self.order = self.close()
            return True
        return False

    def _signal_direction(self):
        entry0 = float(self.macd_entry.macd[0])
        entry1 = float(self.macd_entry.macd[-1])
        entry2 = float(self.macd_entry.macd[-2])
        trend0 = float(self.macd_trend.macd[0])
        trend1 = float(self.macd_trend.macd[-1])
        buy_sig = entry0 > entry1 and entry1 < entry2 and trend0 > trend1
        sell_sig = entry0 < entry1 and entry1 > entry2 and trend0 < trend1
        return buy_sig, sell_sig, entry2, entry1, entry0, trend1, trend0

    def next(self):
        """Process indicators, risk exits, and possible new entries every bar."""
        self.bar_num += 1
        warmup = max(self.p.macd1_slow, self.p.macd2_slow) + self.p.macd_signal + 5
        if len(self.data) < warmup:
            return
        if self.order:
            return
        if self._manage_position():
            return
        if self.position:
            return

        buy_sig, sell_sig, entry2, entry1, entry0, trend1, trend0 = self._signal_direction()
        direction = 0
        reason = ''
        if self.last_trade_was_loss and self.last_trade_direction != 0:
            direction = -self.last_trade_direction
            reason = 'loss reversal'
        else:
            if buy_sig:
                direction = 1
                reason = 'macd buy'
            elif sell_sig:
                direction = -1
                reason = 'macd sell'

        if direction == 0:
            return

        lot = self._calc_lot()
        if lot < self.p.volume_min:
            return
        self.last_lot = lot
        if direction > 0:
            self.log(
                f'buy reason={reason} lot={lot:.2f} macd1=({entry2:.4f},{entry1:.4f},{entry0:.4f}) '
                f'macd2=({trend1:.4f},{trend0:.4f})'
            )
            self.order = self.buy(size=lot)
            return
        self.log(
            f'sell reason={reason} lot={lot:.2f} macd1=({entry2:.4f},{entry1:.4f},{entry0:.4f}) '
            f'macd2=({trend1:.4f},{trend0:.4f})'
        )
        self.order = self.sell(size=lot)

    def notify_order(self, order):
        """Track filled/cancelled orders and clear risk/entry scaffolding."""
        if order.status in [order.Submitted, order.Accepted]:
            return
        if order.status == order.Completed:
            if self.position:
                self.entry_price = float(order.executed.price)
                if self.position.size > 0:
                    self.stop_price = round(self.entry_price - self.p.stop_points * self.p.point, self.p.price_digits)
                    self.take_price = round(self.entry_price + self.p.take_points * self.p.point, self.p.price_digits)
                    self.log(f'long filled price={self.entry_price:.2f} size={abs(self.position.size):.2f} stop={self.stop_price:.2f} take={self.take_price:.2f}')
                else:
                    self.stop_price = round(self.entry_price + self.p.stop_points * self.p.point, self.p.price_digits)
                    self.take_price = round(self.entry_price - self.p.take_points * self.p.point, self.p.price_digits)
                    self.log(f'short filled price={self.entry_price:.2f} size={abs(self.position.size):.2f} stop={self.stop_price:.2f} take={self.take_price:.2f}')
            else:
                self.entry_price = None
                self.stop_price = None
                self.take_price = None
        elif order.status in [order.Canceled, order.Margin, order.Rejected]:
            self.log(f'order {order.getstatusname()}')
            if not self.position:
                self.entry_price = None
                self.stop_price = None
                self.take_price = None
        self.order = None

    def notify_trade(self, trade):
        """Update trade counters, streak status, and trade-direction state."""
        if trade.isopen and not self._position_was_open:
            if trade.size > 0:
                self.buy_count += 1
                self.current_trade_direction = 1
            elif trade.size < 0:
                self.sell_count += 1
                self.current_trade_direction = -1
            self._position_was_open = True
            return
        if not trade.isclosed:
            return
        self.trade_count += 1
        self.last_trade_direction = self.current_trade_direction
        if trade.pnlcomm >= 0:
            self.win_count += 1
            self.loss_streak = 0
            self.last_trade_was_loss = False
        else:
            self.loss_count += 1
            self.loss_streak = min(self.loss_streak + 1, self.p.doubling_count)
            self.last_trade_was_loss = True
        self.current_trade_direction = 0
        self._position_was_open = False
        self.log(
            f'trade closed pnl={trade.pnlcomm:.2f} loss_streak={self.loss_streak} '
            f'last_trade_direction={self.last_trade_direction} last_trade_was_loss={self.last_trade_was_loss}'
        )


WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_SRC = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_SRC) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_SRC))


BASE_DIR = Path(__file__).resolve().parent

MINUTES_PER_TRADING_YEAR = 24 * 60 * 252


def resolve_data_path(filename):
    """Resolve a data path in the test directory.

    Args:
        filename (str): Relative data filename.

    Returns:
        Path: Absolute path.

    Raises:
        FileNotFoundError: If the file is not present.
    """
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def load_backtest_frame(config):
    """Load and clip data using configured date boundaries.

    Args:
        config (dict): Test configuration dictionary.

    Returns:
        dict: Loaded frame and from/to dates.

    Raises:
        ValueError: If the loaded dataset is empty.
    """
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(
        resolve_data_path(data_cfg['file']),
        fromdate=fromdate,
        todate=todate,
        bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0),
    )
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f"Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}")
    return {'data': df, 'fromdate': fromdate, 'todate': todate}


def build_cerebro(config, frame):
    """Create and configure cerebro with strategy, data feed and analyzers.

    Args:
        config (dict): Backtest configuration.
        frame (dict): Loaded OHLCV frame and bounds.

    Returns:
        bt.Cerebro: Prepared backtest engine.
    """
    bt_cfg = config['backtest']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(
        commission=bt_cfg['commission'],
        margin=bt_cfg['margin'],
        mult=bt_cfg['multiplier'],
        commtype=comm_type,
        stocklike=bt_cfg.get('stocklike', False),
    )
    feed = Mt5PandasFeed(dataname=frame['data'], timeframe=bt.TimeFrame.Minutes, compression=15)
    cerebro.adddata(feed, name=f"{config['data']['symbol']}_{config['data']['timeframe']}")
    cerebro.addstrategy(ProMartStrategy, **config.get('params', {}))
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=15, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')
    return cerebro


def extract_metrics(strat, cerebro, frame, config):
    """Consolidate analyzer and strategy counters for assertion checks.

    Args:
        strat (bt.Strategy): Completed strategy instance.
        cerebro (bt.Cerebro): Backtest engine instance.
        frame (dict): Data frame and metadata used by the test.
        config (dict): Execution configuration.

    Returns:
        dict: Strategy and analyzer metrics used by regression checks.
    """
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    initial_cash = config['backtest']['initial_cash']
    final_value = cerebro.broker.getvalue()
    total_trades = trades.get('total', {}).get('total', 0)
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    gross_won = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gross_lost = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {
        'fromdate': frame['fromdate'],
        'todate': frame['todate'],
        'bars': len(frame['data']),
        'bar_num': strat.bar_num,
        'buy_count': strat.buy_count,
        'sell_count': strat.sell_count,
        'trade_count': strat.trade_count,
        'win_count': strat.win_count,
        'loss_count': strat.loss_count,
        'loss_streak': strat.loss_streak,
        'last_trade_direction': strat.last_trade_direction,
        'last_trade_was_loss': strat.last_trade_was_loss,
        'initial_cash': initial_cash,
        'final_value': final_value,
        'net_pnl': final_value - initial_cash,
        'total_return_pct': (final_value / initial_cash - 1) * 100,
        'total_trades': total_trades,
        'won': won,
        'lost': lost,
        'win_rate': (won / total_trades * 100) if total_trades else 0,
        'profit_factor': (gross_won / gross_lost) if gross_lost else None,
        'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sharpe_ratio': sharpe.get('sharperatio'),
        'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'sqn': sqn.get('sqn'),
    }


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def _resolve_loader():
    """Locate the data-loading helper (varies by strategy)."""
    for name in ("load_inputs", "load_data", "load_backtest_frame", "prepare_inputs", "prepare_data"):
        fn = globals().get(name)
        if callable(fn):
            return fn
    raise RuntimeError("No inputs loader found in inlined module")


def _build_cerebro_compat(inputs, config):
    """Call build_cerebro with whichever signature the original used."""
    import inspect
    sig = inspect.signature(build_cerebro)
    params = list(sig.parameters.keys())
    if params and params[0].lower() in ("config", "cfg", "configuration"):
        return build_cerebro(config, inputs)
    try:
        return build_cerebro(inputs, config)
    except TypeError:
        return build_cerebro(config, inputs)


def _extract_metrics_compat(strat, cerebro, inputs, config):
    """Call extract_metrics with whichever signature the original used."""
    for args in (
        (strat, cerebro, inputs, config),
        (strat, cerebro, config, inputs),
        (strat, cerebro, inputs),
        (strat, cerebro),
    ):
        try:
            return extract_metrics(*args)
        except TypeError:
            continue
    raise RuntimeError("extract_metrics failed for all argument orderings")


def test_217_0216_1191_promart() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/mean_reversion/0216_1191_promart.
    """
    config = _bt_load_config(_CONFIG, repo=_REPO)
    inputs = _resolve_loader()(config)
    cerebro = _build_cerebro_compat(inputs, config)
    results = cerebro.run(runonce=True)
    metrics = _extract_metrics_compat(results[0], cerebro, inputs, config)

    assert metrics.get('bar_num') == 6108, f"bar_num: expected=6108, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 538, f"buy_count: expected=538, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 521, f"sell_count: expected=521, got={metrics.get('sell_count')!r}"
    assert metrics.get('win_count') == 379, f"win_count: expected=379, got={metrics.get('win_count')!r}"
    assert metrics.get('loss_count') == 680, f"loss_count: expected=680, got={metrics.get('loss_count')!r}"
    assert metrics.get('total_trades') == 1059, f"total_trades: expected=1059, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 1059, f"trade_count: expected=1059, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 379, f"won: expected=379, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 680, f"lost: expected=680, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 6129.0, tol=6.129000e-03, key='bars')
    _close(metrics.get('loss_streak'), 0.0, tol=1.000000e-06, key='loss_streak')
    _close(metrics.get('last_trade_direction'), 1.0, tol=1.000000e-06, key='last_trade_direction')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_value'), 1048978.800000006, tol=1.048979e+00, key='final_value')
    _close(metrics.get('net_pnl'), 48978.8000000061, tol=4.897880e-02, key='net_pnl')
    _close(metrics.get('total_return_pct'), 4.897880000000621, tol=4.897880e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 35.78847969782814, tol=3.578848e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 1.0616385741583856, tol=1.061639e-06, key='profit_factor')
    _close(metrics.get('max_drawdown'), 6.257155862323441, tol=6.257156e-06, key='max_drawdown')
    _close(metrics.get('sharpe_ratio'), 5.610284155830207, tol=5.610284e-06, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), 1596.4289390464146, tol=1.596429e-03, key='annual_return_pct')
    _close(metrics.get('sqn'), 0.6891756413258218, tol=1.000000e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
