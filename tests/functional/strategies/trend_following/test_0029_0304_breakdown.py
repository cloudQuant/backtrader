"""Regression test for ``Breakdown`` in trend-following strategy suite.

Auto-generated by ``scripts/inline_regression_tests.py`` — do not edit manually.
The original ``config.yaml``, ``run.py``, ``strategy_*.py``, and
``expected.json`` have been collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own ``extract_metrics()`` output captured at
migration time.

Data Used:
    Uses XAUUSD M15 bar data from ``tests/datas/XAUUSD_M15.csv``.
    Time window is ``2025-12-03 01:15:00`` through ``2026-03-10 09:00:00``.
    Input is shifted by ``bar_shift_minutes`` for feed alignment.

Strategy Principle:
    Daily-breakdown style system submits paired long/short stop entries at prior
    day breakout levels, then manages stop-loss / take-profit / trailing stops.
    It also enforces one active breakout setup per session.

Strategy Logic:
    1) Load and parse MT5 data, add spread plus OHLCV fields.
    2) Compute previous-day levels and place OCO breakout orders each session.
    3) Track fills, exits, and trade lifecycle events.
    4) Aggregate analyzers and strategy counters into deterministic regression metrics.
"""
from __future__ import annotations
import math
from pathlib import Path
import io
import argparse
import datetime
import backtrader as bt
import pandas as pd
import pytest

_REPO = Path(__file__).resolve().parents[4]

_CONFIG = {
    'strategy': {
        'name': 'Breakdown',
        'source_ea': 'ea/0304_breakdown/breakdown.mq5',
    },
    'data': {
        'symbol': 'XAUUSD',
        'base_timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
        'execution_compression_minutes': 15,
    },
    'params': {
        'fixed_lot': 0.1,
        'point_size': 0.01,
        'stoploss_pips': 50,
        'takeprofit_pips': 50,
        'trailing_stop_pips': 5,
        'trailing_step_pips': 5,
        'min_distance_pips': 25,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


def _resolve_repo_paths(node):
    """Replace '{repo}' placeholder in config string values with absolute repo path."""
    if isinstance(node, dict):
        return {k: _resolve_repo_paths(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_resolve_repo_paths(v) for v in node]
    if isinstance(node, str):
        return node.replace('{repo}', str(_REPO))
    return node


def load_config(*args, **kwargs):
    """Inlined config (was config.yaml). Accepts any args for compatibility with strategies that pass a path."""
    import copy
    return _resolve_repo_paths(copy.deepcopy(_CONFIG))





def load_mt5_csv(filepath, fromdate=None, todate=None, bar_shift_minutes=0):
    """Load MT5 TSV data into an OHLCV DataFrame with optional date filtering.

    Args:
        filepath (str): Source file path.
        fromdate (datetime.datetime | None): Start timestamp inclusive.
        todate (datetime.datetime | None): End timestamp inclusive.
        bar_shift_minutes (int): Bar timestamp shift in minutes.

    Returns:
        pandas.DataFrame: Sorted DataFrame with datetime index and OHLCV/spread.
    """
    with open(filepath, 'r', encoding='utf-8') as f:
        lines = f.read().strip().split('\n')
    cleaned = '\n'.join(line.strip().strip('"') for line in lines if line.strip())
    df = pd.read_csv(io.StringIO(cleaned), sep='\t')
    df['datetime'] = pd.to_datetime(df['<DATE>'] + ' ' + df['<TIME>'], format='%Y.%m.%d %H:%M:%S')
    df = df.rename(columns={
        '<OPEN>': 'open',
        '<HIGH>': 'high',
        '<LOW>': 'low',
        '<CLOSE>': 'close',
        '<TICKVOL>': 'volume',
        '<VOL>': 'openinterest',
        '<SPREAD>': 'spread',
    })
    df = df[['datetime', 'open', 'high', 'low', 'close', 'volume', 'openinterest', 'spread']]
    df = df.set_index('datetime').sort_index()
    if bar_shift_minutes:
        df.index = df.index + pd.Timedelta(minutes=bar_shift_minutes)
    if fromdate is not None:
        df = df[df.index >= fromdate]
    if todate is not None:
        df = df[df.index <= todate]
    return df


class Mt5PandasFeed(bt.feeds.PandasData):
    """Pandas feed extension exposing spread as an additional line."""

    lines = ('spread',)
    params = (
        ('datetime', None),
        ('open', 0),
        ('high', 1),
        ('low', 2),
        ('close', 3),
        ('volume', 4),
        ('openinterest', 5),
        ('spread', 6),
    )


class BreakdownStrategy(bt.Strategy):
    """Breakdown entry/exist strategy with session-level pending breakout levels."""
    params = dict(
        fixed_lot=0.1,
        point_size=0.01,
        stoploss_pips=50,
        takeprofit_pips=50,
        trailing_stop_pips=5,
        trailing_step_pips=5,
        min_distance_pips=25,
    )

    def __init__(self):
        """Initialize state for pending/active orders and session bookkeeping."""
        self.data0_feed = self.datas[0]
        self.entry_order = None
        self.close_order = None
        self.buy_stop_order = None
        self.sell_stop_order = None
        self.active_side = None
        self.entry_price = None
        self.stop_price = None
        self.limit_price = None
        self.current_session_date = None
        self.pending_levels_date = None

    def log(self, text):
        """Log a message with bar timestamp."""
        dt = bt.num2date(self.data0_feed.datetime[0])
        print(f'{dt.isoformat()}, {text}')

    def _minimum_bars(self):
        return 2 * 24 * 4 + 10

    def _reset_exit_levels(self):
        self.stop_price = None
        self.limit_price = None

    def _cancel_pending_orders(self):
        if self.buy_stop_order is not None:
            self.cancel(self.buy_stop_order)
            self.buy_stop_order = None
        if self.sell_stop_order is not None:
            self.cancel(self.sell_stop_order)
            self.sell_stop_order = None

    def _initialize_exit_levels(self):
        if not self.position or self.entry_price is None:
            return
        if self.position.size > 0:
            self.stop_price = self.entry_price - self.p.stoploss_pips * self.p.point_size if self.p.stoploss_pips > 0 else None
            self.limit_price = self.entry_price + self.p.takeprofit_pips * self.p.point_size if self.p.takeprofit_pips > 0 else None
        else:
            self.stop_price = self.entry_price + self.p.stoploss_pips * self.p.point_size if self.p.stoploss_pips > 0 else None
            self.limit_price = self.entry_price - self.p.takeprofit_pips * self.p.point_size if self.p.takeprofit_pips > 0 else None

    def _submit_close(self, reason):
        if not self.position or self.close_order is not None:
            return
        self.close_order = self.close()
        self.log(f'CLOSE side={self.active_side} reason={reason}')

    def _check_exit_thresholds(self):
        if not self.position or self.entry_price is None or self.close_order is not None:
            return False
        bar_high = float(self.data0_feed.high[0])
        bar_low = float(self.data0_feed.low[0])
        if self.position.size > 0:
            if self.stop_price is not None and bar_low <= self.stop_price:
                self._submit_close(f'stop loss hit @{self.stop_price:.5f}')
                return True
            if self.limit_price is not None and bar_high >= self.limit_price:
                self._submit_close(f'take profit hit @{self.limit_price:.5f}')
                return True
        else:
            if self.stop_price is not None and bar_high >= self.stop_price:
                self._submit_close(f'stop loss hit @{self.stop_price:.5f}')
                return True
            if self.limit_price is not None and bar_low <= self.limit_price:
                self._submit_close(f'take profit hit @{self.limit_price:.5f}')
                return True
        return False

    def _update_trailing(self):
        if not self.position or self.entry_price is None or self.p.trailing_stop_pips <= 0:
            return
        close_price = float(self.data0_feed.close[0])
        trail_distance = self.p.trailing_stop_pips * self.p.point_size
        trail_gate = (self.p.trailing_stop_pips + self.p.trailing_step_pips) * self.p.point_size
        if self.position.size > 0:
            if close_price - self.entry_price > trail_gate:
                candidate = close_price - trail_distance
                if self.stop_price is None or candidate > self.stop_price + 1e-12:
                    self.stop_price = candidate
                    self.log(f'UPDATE LONG TRAIL stop={self.stop_price:.5f}')
        else:
            if self.entry_price - close_price > trail_gate:
                candidate = close_price + trail_distance
                if self.stop_price is None or candidate < self.stop_price - 1e-12:
                    self.stop_price = candidate
                    self.log(f'UPDATE SHORT TRAIL stop={self.stop_price:.5f}')

    def _previous_day_levels(self):
        current_dt = bt.num2date(self.data0_feed.datetime[0])
        prev_day = current_dt.date() - pd.Timedelta(days=1)
        prev_high = None
        prev_low = None
        max_lookback = min(len(self.data0_feed) - 1, 24 * 4 * 7)
        for i in range(1, max_lookback + 1):
            dt_i = bt.num2date(self.data0_feed.datetime[-i])
            day_i = dt_i.date()
            if day_i == current_dt.date():
                continue
            if day_i == prev_day:
                high_i = float(self.data0_feed.high[-i])
                low_i = float(self.data0_feed.low[-i])
                prev_high = high_i if prev_high is None else max(prev_high, high_i)
                prev_low = low_i if prev_low is None else min(prev_low, low_i)
            elif prev_high is not None:
                break
        return prev_high, prev_low

    def _ensure_daily_pending_orders(self):
        if self.position or self.entry_order is not None or self.close_order is not None:
            return
        current_dt = bt.num2date(self.data0_feed.datetime[0])
        session_date = current_dt.date()
        if self.current_session_date != session_date:
            self._cancel_pending_orders()
            self.current_session_date = session_date
            self.pending_levels_date = None
        if self.pending_levels_date == session_date:
            return
        if self.buy_stop_order is not None or self.sell_stop_order is not None:
            return
        prev_high, prev_low = self._previous_day_levels()
        if prev_high is None or prev_low is None:
            return
        size = max(0.01, float(self.p.fixed_lot))
        min_distance = self.p.min_distance_pips * self.p.point_size
        buy_price = prev_high + min_distance
        sell_price = prev_low - min_distance
        buy_sl = buy_price - self.p.stoploss_pips * self.p.point_size if self.p.stoploss_pips > 0 else None
        buy_tp = buy_price + self.p.takeprofit_pips * self.p.point_size if self.p.takeprofit_pips > 0 else None
        sell_sl = sell_price + self.p.stoploss_pips * self.p.point_size if self.p.stoploss_pips > 0 else None
        sell_tp = sell_price - self.p.takeprofit_pips * self.p.point_size if self.p.takeprofit_pips > 0 else None
        buy_kwargs = dict(size=size, exectype=bt.Order.Stop, price=buy_price)
        sell_kwargs = dict(size=size, exectype=bt.Order.Stop, price=sell_price)
        if buy_sl is not None:
            buy_kwargs['plimit'] = None
        if sell_sl is not None:
            sell_kwargs['plimit'] = None
        self.buy_stop_order = self.buy(**buy_kwargs)
        self.sell_stop_order = self.sell(**sell_kwargs)
        self.pending_levels_date = session_date
        self.log(f'PLACE OCO BUY_STOP={buy_price:.5f} SELL_STOP={sell_price:.5f}')

    def next(self):
        """Evaluate exits, trailing stops, and scheduled breakout order maintenance."""
        if len(self.data0_feed) < self._minimum_bars():
            return
        if self._check_exit_thresholds():
            return
        self._update_trailing()
        self._ensure_daily_pending_orders()

    def notify_order(self, order):
        """Update order state for entries, stop orders, and close orders."""
        if order.status in [order.Submitted, order.Accepted]:
            return
        if order.status == order.Completed:
            if order == self.buy_stop_order:
                self.entry_order = None
                self.buy_stop_order = None
                self.active_side = 'long'
                self.entry_price = order.executed.price
                self._cancel_pending_orders()
                self.log(f'ENTRY FILLED side=long price={order.executed.price:.5f} size={order.executed.size}')
                self._initialize_exit_levels()
            elif order == self.sell_stop_order:
                self.entry_order = None
                self.sell_stop_order = None
                self.active_side = 'short'
                self.entry_price = order.executed.price
                self._cancel_pending_orders()
                self.log(f'ENTRY FILLED side=short price={order.executed.price:.5f} size={order.executed.size}')
                self._initialize_exit_levels()
            elif order == self.close_order:
                self.log(f'CLOSE FILLED price={order.executed.price:.5f} size={order.executed.size}')
                self.close_order = None
                self.active_side = None
                self.entry_price = None
                self._reset_exit_levels()
            else:
                if order.isbuy() and order.exectype == bt.Order.Stop:
                    self.buy_stop_order = None
                elif order.issell() and order.exectype == bt.Order.Stop:
                    self.sell_stop_order = None
        elif order.status in [order.Canceled, order.Margin, order.Rejected]:
            if order == self.buy_stop_order:
                self.buy_stop_order = None
            elif order == self.sell_stop_order:
                self.sell_stop_order = None
            elif order == self.close_order:
                self.close_order = None

    def notify_trade(self, trade):
        """Reset state when a trade has fully closed."""
        if not trade.isclosed:
            return
        self.log(f'TRADE CLOSED side={self.active_side or ("long" if trade.long else "short")} pnl={trade.pnlcomm:.2f} net={self.broker.getvalue():.2f}')
        if not self.position:
            self.active_side = None
            self.entry_price = None
            self._reset_exit_levels()





BASE_DIR = Path(__file__).resolve().parent

MINUTES_PER_TRADING_YEAR = 24 * 60 * 252



def resolve_data_path(filename):
    """Resolve a path relative to the current test module directory.

    Args:
        filename (str): Relative data file path.

    Returns:
        pathlib.Path: Absolute path to the resource.
    """
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def parse_dt(value):
    """Parse ISO datetime strings used by strategy config."""
    if not value:
        return None
    return datetime.datetime.fromisoformat(value)


def load_backtest_frame(config):
    """Load and filter data frame for strategy backtest execution.

    Args:
        config (dict): Strategy configuration.

    Returns:
        dict: Frame payload for cerebro builder.
    """
    data_cfg = config['data']
    fromdate = parse_dt(data_cfg.get('fromdate'))
    todate = parse_dt(data_cfg.get('todate'))
    df = load_mt5_csv(resolve_data_path(data_cfg['file']), fromdate=fromdate, todate=todate, bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0))
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f'Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}')
    return {'data': df}


def add_default_analyzers(cerebro):
    """Attach analyzers used by the regression metric checks."""
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=60, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')


def build_cerebro(config, frame):
    """Create and configure Backtrader engine for the breakdown strategy.

    Args:
        config (dict): Strategy and backtest config.
        frame (dict): Loaded market frame and metadata.

    Returns:
        bt.Cerebro: Configured engine.
    """
    bt_cfg = config['backtest']
    data_cfg = config['data']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(commission=bt_cfg['commission'], margin=bt_cfg['margin'], mult=bt_cfg['multiplier'], commtype=comm_type, stocklike=bt_cfg.get('stocklike', False))
    feed = Mt5PandasFeed(dataname=frame['data'], timeframe=bt.TimeFrame.Minutes, compression=data_cfg.get('execution_compression_minutes', 15))
    cerebro.adddata(feed, name=f"{data_cfg['symbol']}_{data_cfg['base_timeframe']}")
    cerebro.addstrategy(BreakdownStrategy, **config.get('params', {}))
    add_default_analyzers(cerebro)
    return cerebro


def finite_or_none(value):
    """Return finite values unchanged, otherwise ``None``."""
    if value is None:
        return None
    if isinstance(value, (int, float)) and not math.isfinite(value):
        return None
    return value


def summarize(results, start_value):
    """Print a compact backtest summary from analyzers and trade statistics."""
    strat = results[0]
    end_value = strat.broker.getvalue()
    drawdown = strat.analyzers.drawdown.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    sharpe = strat.analyzers.sharpe.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    total_closed = trades.get('total', {}).get('closed', 0)
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    strike_rate = (won / total_closed * 100.0) if total_closed else 0.0
    print('\n# Backtest Summary')
    print(f'- Start Value: {start_value:.2f}')
    print(f'- End Value: {end_value:.2f}')
    print(f'- Net PnL: {end_value - start_value:.2f}')
    print(f'- Max Drawdown: {drawdown.get("max", {}).get("drawdown", 0.0):.2f}%')
    print(f'- Total Closed Trades: {total_closed}')
    print(f'- Won Trades: {won}')
    print(f'- Lost Trades: {lost}')
    print(f'- Strike Rate: {strike_rate:.2f}%')
    print(f'- Sharpe: {finite_or_none(sharpe.get("sharperatio"))}')
    print(f'- SQN: {finite_or_none(sqn.get("sqn"))}')
    print(f'- Total Return: {returns.get("rtot", 0.0):.6f}')


def main():
    """Load, build, run, and summarize the Breakdown strategy backtest."""
    parser = argparse.ArgumentParser(description='Run Breakdown backtest')
    parser.add_argument('--plot', action='store_true', help='Plot result chart')
    args = parser.parse_args()
    config = load_config()
    frame = load_backtest_frame(config)
    cerebro = build_cerebro(config, frame)
    start_value = cerebro.broker.getvalue()
    results = cerebro.run(runonce=False)
    summarize(results, start_value)
    if args.plot:
        cerebro.plot(style='candlestick')


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def test_29_0029_0304_breakdown() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/trend_following/0029_0304_breakdown.
    """
    captured = {}

    import sys as _sys
    _mod = _sys.modules[__name__]

    # Hook any plausible metrics-extraction function (returns a dict).
    _hook_targets = []
    _metric_names = (
        "extract_metrics", "build_metrics", "compute_metrics",
        "calculate_metrics", "collect_metrics", "gather_metrics", "extract_results",
    )
    for _name in _metric_names:
        _orig = getattr(_mod, _name, None)
        if callable(_orig):
            def _make_hook(orig):
                def _hook(*a, **kw):
                    m = orig(*a, **kw)
                    if isinstance(m, dict) and m and "extracted" not in captured:
                        captured["extracted"] = m
                    return m
                return _hook
            setattr(_mod, _name, _make_hook(_orig))
            _hook_targets.append((_name, _orig))

    # Hook cerebro.run() to (a) force runonce=True and (b) capture results
    # so we can derive metrics directly from analyzers when no extractor returns a dict.
    import backtrader as _bt
    _orig_run = _bt.Cerebro.run
    def _hooked_cerebro_run(self, *args, **kwargs):
        kwargs["runonce"] = True
        _r = _orig_run(self, *args, **kwargs)
        captured["cerebro"] = self
        captured["results"] = _r
        try:
            captured["initial_cash"] = float(self.broker.startingcash)
        except Exception:
            pass
        return _r
    _bt.Cerebro.run = _hooked_cerebro_run

    # Strip pytest argv so argparse-based main() functions don't see them.
    _saved_argv = _sys.argv
    _sys.argv = [_sys.argv[0]]

    try:
        try:
            if hasattr(_mod, "main") and callable(_mod.main):
                _mod.main()
            elif hasattr(_mod, "run") and callable(_mod.run):
                result = _mod.run()
                if isinstance(result, dict) and "extracted" not in captured:
                    captured["extracted"] = result
                elif isinstance(result, (list, tuple)):
                    for item in result:
                        if isinstance(item, dict) and "extracted" not in captured:
                            captured["extracted"] = item
                            break
            else:
                raise RuntimeError("Neither main() nor run() found in inlined module")
        except SystemExit:
            pass
        except Exception:
            if "cerebro" not in captured:
                raise
    finally:
        _bt.Cerebro.run = _orig_run
        for _name, _orig in _hook_targets:
            setattr(_mod, _name, _orig)
        _sys.argv = _saved_argv

    metrics = captured.get("extracted")
    if metrics is None:
        # Derive from cerebro/analyzers
        cerebro = captured.get("cerebro")
        results = captured.get("results") or []
        assert cerebro is not None and results, "no metrics or cerebro captured"
        strat = results[0] if not isinstance(results[0], list) else results[0][0]
        metrics = {}
        metrics["final_value"] = float(cerebro.broker.getvalue())
        if "initial_cash" in captured:
            metrics["initial_cash"] = captured["initial_cash"]
        analyzers = getattr(strat, "analyzers", None)
        if analyzers is not None:
            for name in dir(analyzers):
                if name.startswith("_"):
                    continue
                try:
                    an = getattr(analyzers, name)
                    analysis = an.get_analysis()
                except Exception:
                    continue
                if "sharperatio" in analysis and "sharpe_ratio" not in metrics:
                    metrics["sharpe_ratio"] = analysis.get("sharperatio")
                if "rnorm" in analysis and "annual_return" not in metrics:
                    metrics["annual_return"] = analysis.get("rnorm")
                if "rtot" in analysis and "return_rate" not in metrics:
                    metrics["return_rate"] = analysis.get("rtot")
                if "max" in analysis and isinstance(analysis["max"], dict) and "drawdown" in analysis["max"] and "max_drawdown" not in metrics:
                    metrics["max_drawdown"] = analysis["max"]["drawdown"]
                if "sqn" in analysis and "sqn" not in metrics:
                    metrics["sqn"] = analysis.get("sqn")
                if "total" in analysis and isinstance(analysis["total"], dict) and "total_trades" not in metrics:
                    metrics["total_trades"] = analysis["total"].get("closed", analysis["total"].get("total", 0))
                    metrics["trade_num"] = metrics.get("total_trades", 0)
                if "won" in analysis and isinstance(analysis["won"], dict) and "win_count" not in metrics:
                    metrics["win_count"] = analysis["won"].get("total", 0)
                if "lost" in analysis and isinstance(analysis["lost"], dict) and "loss_count" not in metrics:
                    metrics["loss_count"] = analysis["lost"].get("total", 0)
        for attr in ("bar_num", "buy_count", "sell_count", "rebalance_count"):
            if hasattr(strat, attr) and attr not in metrics:
                metrics[attr] = getattr(strat, attr)

    assert metrics, "no metrics derived"

    assert metrics.get('win_count') == 30, f"win_count: expected=30, got={metrics.get('win_count')!r}"
    assert metrics.get('loss_count') == 19, f"loss_count: expected=19, got={metrics.get('loss_count')!r}"
    assert metrics.get('trade_num') == 49, f"trade_num: expected=49, got={metrics.get('trade_num')!r}"
    assert metrics.get('total_trades') == 49, f"total_trades: expected=49, got={metrics.get('total_trades')!r}"
    _close(metrics.get('final_value'), 1003870.8999999999, tol=1.003871e+00, key='final_value')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('max_drawdown'), 0.01840167324285528, tol=1.000000e-06, key='max_drawdown')
    _close(metrics.get('annual_return'), 1.4005023379420452, tol=1.400502e-06, key='annual_return')
    _close(metrics.get('return_rate'), 0.0038634273443224356, tol=1.000000e-06, key='return_rate')
    _close(metrics.get('sharpe_ratio'), 22.849953087660424, tol=2.284995e-05, key='sharpe_ratio')
    _close(metrics.get('sqn'), 3.2784145734419208, tol=3.278415e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
