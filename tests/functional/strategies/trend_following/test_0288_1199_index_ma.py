"""Inlined regression test for tests/functional/strategies/trend_following/regression/0288_1199_index_ma.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original config.yaml, run.py, strategy_*.py, and expected.json have been
collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own extract_metrics() output captured at
migration time.
"""
from __future__ import annotations
import math
from pathlib import Path
import io
import sys
import argparse, datetime
import backtrader.feeds as btfeeds
import backtrader as bt
import pandas as pd
import pytest

_REPO = Path(__file__).resolve().parents[4]

_CONFIG = {
    'strategy': {
        'name': 'Indexed Moving Average',
        'source_ea': 'ea/1199_一个EA交易_-_索引移动平均',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
    },
    'params': {
        'ma_period': 5,
        'take': 50,
        'drop': 1000,
        'signal_level': 0.5,
        'risk': 0.01,
        'max_lots': 1.0,
        'volume_min': 0.1,
        'volume_step': 0.1,
        'volume_max': 100.0,
        'margin_per_lot': 250.0,
        'point': 0.01,
        'price_digits': 2,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


def _resolve_repo_paths(node):
    """Replace '{repo}' placeholder in config string values with absolute repo path."""
    if isinstance(node, dict):
        return {k: _resolve_repo_paths(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_resolve_repo_paths(v) for v in node]
    if isinstance(node, str):
        return node.replace('{repo}', str(_REPO))
    return node


def load_config(*args, **kwargs):
    """Inlined config (was config.yaml). Accepts any args for compatibility with strategies that pass a path."""
    import copy
    return _resolve_repo_paths(copy.deepcopy(_CONFIG))



WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_SRC = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_SRC) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_SRC))



def load_mt5_csv(filepath, fromdate=None, todate=None, bar_shift_minutes=0):
    with open(filepath, 'r', encoding='utf-8') as f:
        lines = f.read().strip().split('\n')
    cleaned = '\n'.join(line.strip().strip('"') for line in lines)
    df = pd.read_csv(io.StringIO(cleaned), sep='\t')
    df['datetime'] = pd.to_datetime(df['<DATE>'] + ' ' + df['<TIME>'], format='%Y.%m.%d %H:%M:%S')
    df = df.rename(columns={
        '<OPEN>': 'open', '<HIGH>': 'high', '<LOW>': 'low',
        '<CLOSE>': 'close', '<TICKVOL>': 'volume', '<VOL>': 'openinterest',
    })
    df = df[['datetime', 'open', 'high', 'low', 'close', 'volume', 'openinterest']]
    df = df.set_index('datetime')
    if bar_shift_minutes:
        df.index = df.index + pd.Timedelta(minutes=bar_shift_minutes)
    if fromdate is not None:
        df = df[df.index >= fromdate]
    if todate is not None:
        df = df[df.index <= todate]
    return df


class Mt5PandasFeed(btfeeds.PandasData):
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2),
        ('close', 3), ('volume', 4), ('openinterest', 5),
    )


class IndexedMovingAverage(bt.Indicator):
    lines = ('ima',)
    params = dict(period=5)

    def __init__(self):
        self.ma = bt.indicators.SimpleMovingAverage(self.data.close, period=self.p.period)
        self.addminperiod(self.p.period + 1)

    def next(self):
        ma = float(self.ma[0])
        close = float(self.data.close[0])
        self.lines.ima[0] = 0.0 if abs(ma) < 1e-12 else (close / ma) - 1.0


class IndexedMovingAverageStrategy(bt.Strategy):
    params = dict(
        ma_period=5,
        take=50,
        drop=1000,
        signal_level=0.5,
        risk=0.01,
        max_lots=1.0,
        volume_min=0.1,
        volume_step=0.1,
        volume_max=100.0,
        margin_per_lot=250.0,
        point=0.01,
        price_digits=2,
    )

    def __init__(self):
        self.ima = IndexedMovingAverage(self.data, period=self.p.ma_period)
        self.bar_num = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0
        self._position_was_open = False
        self.order = None
        self.entry_price = None
        self.stop_price = None
        self.last_signal_k = 0.0

    def log(self, text):
        dt = bt.num2date(self.data.datetime[0])
        print(f'{dt.isoformat()}, {text}')

    def _round_volume_down(self, value):
        if value <= 0:
            return 0.0
        steps = int(value / self.p.volume_step)
        return round(steps * self.p.volume_step, 8)

    def _calc_lot(self):
        cash = float(self.broker.getcash())
        lot = cash * self.p.risk / max(self.p.drop, 1)
        lot = self._round_volume_down(lot)
        if self.p.max_lots > 0:
            lot = min(lot, self.p.max_lots)
        lot = min(lot, self.p.volume_max)
        while lot >= self.p.volume_min and cash < lot * self.p.margin_per_lot:
            lot = self._round_volume_down(lot - self.p.volume_step)
        if lot < self.p.volume_min:
            return 0.0
        return round(lot, 8)

    def _loss_points(self):
        if not self.position or self.entry_price is None:
            return 0.0
        close = float(self.data.close[0])
        if self.position.size > 0:
            return max(0.0, (self.entry_price - close) / self.p.point)
        return max(0.0, (close - self.entry_price) / self.p.point)

    def _profit_points(self):
        if not self.position or self.entry_price is None:
            return 0.0
        close = float(self.data.close[0])
        if self.position.size > 0:
            return max(0.0, (close - self.entry_price) / self.p.point)
        return max(0.0, (self.entry_price - close) / self.p.point)

    def _update_trailing_stop(self):
        profit_points = self._profit_points()
        if profit_points <= self.p.take:
            return
        close = float(self.data.close[0])
        if self.position.size > 0:
            new_stop = round(close - self.p.take * self.p.point, self.p.price_digits)
            if self.stop_price is None or new_stop > self.stop_price:
                self.stop_price = new_stop
        elif self.position.size < 0:
            new_stop = round(close + self.p.take * self.p.point, self.p.price_digits)
            if self.stop_price is None or new_stop < self.stop_price:
                self.stop_price = new_stop

    def _check_stop_hit(self):
        if self.stop_price is None or not self.position:
            return False
        high = float(self.data.high[0])
        low = float(self.data.low[0])
        if self.position.size > 0 and low <= self.stop_price:
            self.log(f'close long trailing_stop={self.stop_price:.2f}')
            self.order = self.close()
            return True
        if self.position.size < 0 and high >= self.stop_price:
            self.log(f'close short trailing_stop={self.stop_price:.2f}')
            self.order = self.close()
            return True
        return False

    def next(self):
        self.bar_num += 1
        warmup = self.p.ma_period + 3
        if len(self.data) < warmup:
            return
        if self.order:
            return

        if self.position:
            if self._check_stop_hit():
                return
            loss_points = self._loss_points()
            if loss_points >= self.p.drop:
                self.log(f'close loss drop_points={loss_points:.2f}')
                self.order = self.close()
                return
            self._update_trailing_stop()
            return

        ima0 = float(self.ima.ima[0])
        ima1 = float(self.ima.ima[-1])
        if abs(ima1) < 1e-12:
            self.last_signal_k = 0.0
            return
        k1 = (ima0 - ima1) / abs(ima1)
        self.last_signal_k = k1
        lot = self._calc_lot()
        if lot < self.p.volume_min:
            return
        if k1 >= self.p.signal_level:
            self.log(f'buy signal k={k1:.4f} lot={lot:.2f}')
            self.order = self.buy(size=lot)
            return
        if k1 <= -self.p.signal_level:
            self.log(f'sell signal k={k1:.4f} lot={lot:.2f}')
            self.order = self.sell(size=lot)
            return

    def notify_order(self, order):
        if order.status in [order.Submitted, order.Accepted]:
            return
        if order.status == order.Completed:
            if self.position:
                self.entry_price = float(order.executed.price)
                self.stop_price = None
                side = 'long' if self.position.size > 0 else 'short'
                self.log(f'{side} filled price={self.entry_price:.2f} size={abs(self.position.size):.2f}')
            else:
                self.entry_price = None
                self.stop_price = None
        elif order.status in [order.Canceled, order.Margin, order.Rejected]:
            if not self.position:
                self.entry_price = None
                self.stop_price = None
            self.log(f'order {order.getstatusname()}')
        self.order = None

    def notify_trade(self, trade):
        if trade.isopen and not self._position_was_open:
            if trade.size > 0:
                self.buy_count += 1
            elif trade.size < 0:
                self.sell_count += 1
            self._position_was_open = True
            return
        if not trade.isclosed:
            return
        self.trade_count += 1
        if trade.pnlcomm >= 0:
            self.win_count += 1
        else:
            self.loss_count += 1
        self._position_was_open = False
        self.log(f'trade closed pnl={trade.pnlcomm:.2f}')



WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_SRC = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_SRC) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_SRC))


BASE_DIR = Path(__file__).resolve().parent

MINUTES_PER_TRADING_YEAR = 24 * 60 * 252

def resolve_data_path(filename):
    path = (BASE_DIR / filename).resolve()
    if not path.exists(): raise FileNotFoundError(f'Data file not found: {path}')
    return path

def load_backtest_frame(config):
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(resolve_data_path(data_cfg['file']), fromdate=fromdate, todate=todate,
                      bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0))
    if df.empty: raise ValueError('Loaded data frame is empty')
    print(f"Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}")
    return {'data': df, 'fromdate': fromdate, 'todate': todate}

def build_cerebro(config, frame):
    bt_cfg = config['backtest']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(commission=bt_cfg['commission'], margin=bt_cfg['margin'],
        mult=bt_cfg['multiplier'], commtype=comm_type, stocklike=bt_cfg.get('stocklike', False))
    feed = Mt5PandasFeed(dataname=frame['data'], timeframe=bt.TimeFrame.Minutes, compression=15)
    cerebro.adddata(feed, name=f"{config['data']['symbol']}_{config['data']['timeframe']}")
    cerebro.addstrategy(IndexedMovingAverageStrategy, **config.get('params', {}))
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=15, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')
    return cerebro

def extract_metrics(strat, cerebro, frame, config):
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    ic = config['backtest']['initial_cash']; fv = cerebro.broker.getvalue()
    tt = trades.get('total', {}).get('total', 0); w = trades.get('won', {}).get('total', 0)
    l = trades.get('lost', {}).get('total', 0)
    gw = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gl = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {'fromdate': frame['fromdate'], 'todate': frame['todate'], 'bars': len(frame['data']),
        'bar_num': strat.bar_num, 'buy_count': strat.buy_count, 'sell_count': strat.sell_count,
        'trade_count': strat.trade_count, 'win_count': strat.win_count, 'loss_count': strat.loss_count,
        'initial_cash': ic, 'final_value': fv, 'net_pnl': fv - ic, 'total_return_pct': (fv / ic - 1) * 100,
        'total_trades': tt, 'won': w, 'lost': l, 'win_rate': (w / tt * 100) if tt else 0,
        'profit_factor': (gw / gl) if gl else None, 'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sharpe_ratio': sharpe.get('sharperatio'), 'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'sqn': sqn.get('sqn')}

def run(plot=False):
    config = load_config(); frame = load_backtest_frame(config); cerebro = build_cerebro(config, frame)
    print('\nStarting backtest...'); results = cerebro.run(); strat = results[0]
    metrics = extract_metrics(strat, cerebro, frame, config); print_report(metrics)
    if plot: cerebro.plot()
    return results, metrics, cerebro


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def test_287_0288_1199_index_ma() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/trend_following/0288_1199_index_ma.
    """
    # Capture metrics by hooking extract_metrics() (or similar) and invoking the
    # original main()/run(). This reuses whatever loader / build_cerebro /
    # metrics-extraction signatures the strategy used internally.
    captured = {}

    import sys as _sys
    _mod = _sys.modules[__name__]

    # Hook any plausible metrics-extraction function.
    _hook_targets = []
    _metric_names = (
        "extract_metrics", "summarize", "build_metrics", "compute_metrics",
        "calculate_metrics", "collect_metrics", "gather_metrics", "extract_results",
    )
    for _name in _metric_names:
        _orig = getattr(_mod, _name, None)
        if callable(_orig):
            def _make_hook(orig):
                def _hook(*a, **kw):
                    m = orig(*a, **kw)
                    if isinstance(m, dict) and m and "metrics" not in captured:
                        captured["metrics"] = m
                    return m
                return _hook
            setattr(_mod, _name, _make_hook(_orig))
            _hook_targets.append((_name, _orig))

    # Force runonce=True for the cerebro.run() call inside main().
    import backtrader as _bt
    _orig_run = _bt.Cerebro.run
    def _forced_runonce(self, *args, **kwargs):
        kwargs["runonce"] = True
        return _orig_run(self, *args, **kwargs)
    _bt.Cerebro.run = _forced_runonce

    # Strip pytest argv so argparse-based main() functions don't see them.
    _saved_argv = _sys.argv
    _sys.argv = [_sys.argv[0]]

    try:
        try:
            if hasattr(_mod, "main") and callable(_mod.main):
                _mod.main()
            elif hasattr(_mod, "run") and callable(_mod.run):
                result = _mod.run()
                if isinstance(result, dict) and "metrics" not in captured:
                    captured["metrics"] = result
                elif isinstance(result, (list, tuple)):
                    for item in result:
                        if isinstance(item, dict) and "metrics" not in captured:
                            captured["metrics"] = item
                            break
            else:
                raise RuntimeError("Neither main() nor run() found in inlined module")
        except SystemExit:
            pass
        except Exception:
            if "metrics" not in captured:
                raise
    finally:
        _bt.Cerebro.run = _orig_run
        for _name, _orig in _hook_targets:
            setattr(_mod, _name, _orig)
        _sys.argv = _saved_argv

    metrics = captured.get("metrics")
    assert metrics is not None, "no metrics captured during run"

    assert metrics.get('bar_num') == 6124, f"bar_num: expected=6124, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 670, f"buy_count: expected=670, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 645, f"sell_count: expected=645, got={metrics.get('sell_count')!r}"
    assert metrics.get('win_count') == 681, f"win_count: expected=681, got={metrics.get('win_count')!r}"
    assert metrics.get('loss_count') == 633, f"loss_count: expected=633, got={metrics.get('loss_count')!r}"
    assert metrics.get('total_trades') == 1315, f"total_trades: expected=1315, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 1314, f"trade_count: expected=1314, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 681, f"won: expected=681, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 633, f"lost: expected=633, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 6129.0, tol=6.129000e-03, key='bars')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_value'), 1034197.9999999981, tol=1.034198e+00, key='final_value')
    _close(metrics.get('net_pnl'), 34197.99999999814, tol=3.419800e-02, key='net_pnl')
    _close(metrics.get('total_return_pct'), 3.4197999999998174, tol=3.419800e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 51.78707224334601, tol=5.178707e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 1.043231851126196, tol=1.043232e-06, key='profit_factor')
    _close(metrics.get('max_drawdown'), 7.1539420207024, tol=7.153942e-06, key='max_drawdown')
    _close(metrics.get('sharpe_ratio'), 3.9663251803279, tol=3.966325e-06, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), 632.2200096709539, tol=6.322200e-04, key='annual_return_pct')
    _close(metrics.get('sqn'), 0.47401141050533374, tol=1.000000e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
