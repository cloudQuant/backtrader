"""Inlined regression test for tests/functional/strategies/trend_following/regression/0059_0669_hercules_atc_2006.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original config.yaml, run.py, strategy_*.py, and expected.json have been
collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own extract_metrics() output captured at
migration time.
"""
from __future__ import annotations
import math
from pathlib import Path
import io
import argparse
import datetime
import sys
import backtrader as bt
import pandas as pd
import pytest

_REPO = Path(__file__).resolve().parents[6]

_CONFIG = {
    'strategy': {
        'name': 'Hercules_A.T.C._2006',
        'source_ea': 'ea/0669_Hercules_A.T.C._2006/hercules_a.t.c._2006.mq5',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2026-01-15 00:00:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
        'base_compression': 15,
    },
    'params': {
        'lots': 0.01,
        'money_management': False,
        'risk': 2.5,
        'trigger': 10,
        'trailing_stop': 90,
        'take_profit_1': 210,
        'take_profit_2': 280,
        'ma1_period': 1,
        'ma2_period': 72,
        'rsi_upper': 50.0,
        'rsi_lower': 50.0,
        'strict_breakout_filters': False,
        'relaxed_trend_entry': True,
        'blackout_period_hours': 144,
        'point': 0.01,
        'price_digits': 2,
        'contract_multiplier': 100.0,
        'base_compression': 15,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


def _resolve_repo_paths(node):
    """Replace '{repo}' placeholder in config string values with absolute repo path."""
    if isinstance(node, dict):
        return {k: _resolve_repo_paths(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_resolve_repo_paths(v) for v in node]
    if isinstance(node, str):
        return node.replace('{repo}', str(_REPO))
    return node


def load_config(*args, **kwargs):
    """Inlined config (was config.yaml). Accepts any args for compatibility with strategies that pass a path."""
    import copy
    return _resolve_repo_paths(copy.deepcopy(_CONFIG))





def load_mt5_csv(filepath, fromdate=None, todate=None, bar_shift_minutes=0):
    with open(filepath, 'r', encoding='utf-8') as f:
        lines = f.read().strip().split('\n')
    cleaned = '\n'.join(line.strip().strip('"') for line in lines)
    df = pd.read_csv(io.StringIO(cleaned), sep='\t')
    df['datetime'] = pd.to_datetime(df['<DATE>'] + ' ' + df['<TIME>'], format='%Y.%m.%d %H:%M:%S')
    df = df.rename(columns={
        '<OPEN>': 'open',
        '<HIGH>': 'high',
        '<LOW>': 'low',
        '<CLOSE>': 'close',
        '<TICKVOL>': 'tick_volume',
        '<VOL>': 'real_volume',
    })
    df['openinterest'] = 0
    df['volume'] = df['tick_volume']
    df = df[['datetime', 'open', 'high', 'low', 'close', 'volume', 'openinterest']]
    df = df.set_index('datetime')
    if bar_shift_minutes:
        df.index = df.index + pd.Timedelta(minutes=bar_shift_minutes)
    if fromdate is not None:
        df = df[df.index >= fromdate]
    if todate is not None:
        df = df[df.index <= todate]
    return df


class Mt5PandasFeed(bt.feeds.PandasData):
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2), ('close', 3), ('volume', 4), ('openinterest', 5),
    )


class PercentEnvelope(bt.Indicator):
    lines = ('top', 'bot')
    params = dict(period=14, perc=1.0)

    def __init__(self):
        self.ma = bt.indicators.SimpleMovingAverage(self.data, period=int(self.p.period))
        self.addminperiod(int(self.p.period))

    def next(self):
        offset = float(self.p.perc) / 100.0
        ma = float(self.ma[0])
        self.lines.top[0] = ma * (1.0 + offset)
        self.lines.bot[0] = ma * (1.0 - offset)


class HerculesAtc2006Strategy(bt.Strategy):
    params = dict(
        lots=0.01,
        money_management=True,
        risk=2.5,
        trigger=38,
        trailing_stop=90,
        take_profit_1=210,
        take_profit_2=280,
        ma1_period=1,
        ma2_period=72,
        rsi_upper=55.0,
        rsi_lower=45.0,
        strict_breakout_filters=False,
        relaxed_trend_entry=True,
        blackout_period_hours=144,
        point=0.01,
        price_digits=2,
        contract_multiplier=100.0,
        base_compression=15,
    )

    def __init__(self):
        self.base = self.datas[0]
        self.h1 = self.datas[1]
        self.h4 = self.datas[2]
        self.d1 = self.datas[3]

        self.ma_fast = bt.indicators.ExponentialMovingAverage(self.base.close, period=self.p.ma1_period)
        self.ma_slow = bt.indicators.SimpleMovingAverage(self.base.open, period=self.p.ma2_period)
        self.rsi_h1 = bt.indicators.RSI((self.h1.high + self.h1.low + self.h1.close) / 3.0, period=10)
        self.env_d1 = PercentEnvelope(self.d1.close, period=24, perc=0.99)
        self.env_h4 = PercentEnvelope(self.h4.close, period=96, perc=0.10)

        self.bar_num = 0
        self.signal_count = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0
        self.completed_order_count = 0
        self.rejected_order_count = 0

        self.orders = []
        self.legs = []
        self.last_base_len = 0
        self.cross_price = None
        self.cross_dir = None
        self.cross_len = None
        self.blackout_until = None

    def _dt(self):
        return bt.num2date(self.base.datetime[0])

    def _point_value(self):
        return float(self.p.point)

    def _dynamic_lot(self):
        if not self.p.money_management:
            return float(self.p.lots)
        equity = float(self.broker.getvalue())
        lots = (equity * float(self.p.risk) / 100.0) / max(float(self.p.contract_multiplier), 1.0)
        return max(float(self.p.lots), round(lots, 2))

    def _bars_count_for_h10(self):
        return max(1, int(round(120 / float(self.p.base_compression))))

    def _high10h(self):
        count = min(self._bars_count_for_h10(), len(self.base) - 1)
        highs = [float(self.base.high[-i]) for i in range(1, count + 1)]
        return max(highs) if highs else float(self.base.high[-1])

    def _low10h(self):
        count = min(self._bars_count_for_h10(), len(self.base) - 1)
        lows = [float(self.base.low[-i]) for i in range(1, count + 1)]
        return min(lows) if lows else float(self.base.low[-1])

    def _detect_cross(self):
        if len(self.base) < 4:
            return
        fast1 = float(self.ma_fast[-1])
        fast2 = float(self.ma_fast[-2])
        fast3 = float(self.ma_fast[-3])
        slow1 = float(self.ma_slow[-1])
        slow2 = float(self.ma_slow[-2])
        slow3 = float(self.ma_slow[-3])
        if fast1 > slow1 and fast2 < slow2:
            self.cross_dir = 'long'
            self.cross_price = (fast1 + fast2 + slow1 + slow2) / 4.0
            self.cross_len = len(self.base) - 1
            return
        if fast2 > slow2 and fast3 < slow3:
            self.cross_dir = 'long'
            self.cross_price = (fast2 + fast3 + slow2 + slow3) / 4.0
            self.cross_len = len(self.base) - 2
            return
        if fast1 < slow1 and fast2 > slow2:
            self.cross_dir = 'short'
            self.cross_price = (fast1 + fast2 + slow1 + slow2) / 4.0
            self.cross_len = len(self.base) - 1
            return
        if fast2 < slow2 and fast3 > slow3:
            self.cross_dir = 'short'
            self.cross_price = (fast2 + fast3 + slow2 + slow3) / 4.0
            self.cross_len = len(self.base) - 2

    def _countdown_active(self):
        if self.cross_len is None:
            return False
        return (len(self.base) - self.cross_len) <= 2

    def _can_trade(self):
        return self.blackout_until is None or self._dt() >= self.blackout_until

    def _trigger_price(self):
        if self.cross_price is None or self.cross_dir is None:
            return None
        if self.cross_dir == 'long':
            return self.cross_price + float(self.p.trigger) * self._point_value()
        return self.cross_price - float(self.p.trigger) * self._point_value()

    def _clear_orders(self):
        self.orders = [o for o in self.orders if o.alive()]

    def _net_leg_size(self, side):
        return sum(leg['size'] for leg in self.legs if leg['side'] == side)

    def _open_two_legs(self, side):
        size = self._dynamic_lot()
        price = float(self.base.close[0])
        if side == 'long':
            stop = float(self.base.low[-4]) if len(self.base) > 4 else float(self.base.low[0])
            tp1 = round(price + float(self.p.take_profit_1) * self._point_value(), int(self.p.price_digits))
            tp2 = round(price + float(self.p.take_profit_2) * self._point_value(), int(self.p.price_digits))
            self.orders.append(self.buy(size=size))
            self.orders.append(self.buy(size=size))
        else:
            stop = float(self.base.high[-4]) if len(self.base) > 4 else float(self.base.high[0])
            tp1 = round(price - float(self.p.take_profit_1) * self._point_value(), int(self.p.price_digits))
            tp2 = round(price - float(self.p.take_profit_2) * self._point_value(), int(self.p.price_digits))
            self.orders.append(self.sell(size=size))
            self.orders.append(self.sell(size=size))
        self.signal_count += 1
        self.legs.extend([
            {'side': side, 'size': size, 'entry_hint': price, 'stop': round(stop, int(self.p.price_digits)), 'tp': tp1, 'opened': False},
            {'side': side, 'size': size, 'entry_hint': price, 'stop': round(stop, int(self.p.price_digits)), 'tp': tp2, 'opened': False},
        ])
        self.blackout_until = self._dt() + pd.Timedelta(hours=float(self.p.blackout_period_hours))

    def _apply_trailing(self):
        if not self.legs:
            return
        trail = float(self.p.trailing_stop) * self._point_value()
        if trail <= 0:
            return
        high = float(self.base.high[0])
        low = float(self.base.low[0])
        for leg in self.legs:
            if not leg['opened'] or leg['size'] <= 0:
                continue
            if leg['side'] == 'long':
                stopcal = round(high - trail, int(self.p.price_digits))
                if leg['stop'] is None or stopcal > float(leg['stop']):
                    leg['stop'] = stopcal
            else:
                stopcal = round(low + trail, int(self.p.price_digits))
                if leg['stop'] is None or stopcal < float(leg['stop']):
                    leg['stop'] = stopcal

    def _manage_legs(self):
        if not self.legs:
            return
        self._apply_trailing()
        high = float(self.base.high[0])
        low = float(self.base.low[0])
        new_legs = []
        close_orders = []
        for leg in self.legs:
            if not leg['opened'] or leg['size'] <= 0:
                new_legs.append(leg)
                continue
            exit_now = False
            if leg['side'] == 'long':
                if leg['tp'] is not None and high >= float(leg['tp']):
                    exit_now = True
                elif leg['stop'] is not None and low <= float(leg['stop']):
                    exit_now = True
                if exit_now:
                    close_orders.append(self.sell(size=leg['size']))
                else:
                    new_legs.append(leg)
            else:
                if leg['tp'] is not None and low <= float(leg['tp']):
                    exit_now = True
                elif leg['stop'] is not None and high >= float(leg['stop']):
                    exit_now = True
                if exit_now:
                    close_orders.append(self.buy(size=leg['size']))
                else:
                    new_legs.append(leg)
        if close_orders:
            self.orders.extend(close_orders)
        self.legs = new_legs

    def next(self):
        self.bar_num += 1
        self._clear_orders()
        self._manage_legs()
        if len(self.base) == self.last_base_len:
            return
        self.last_base_len = len(self.base)
        if len(self.base) < 100 or len(self.h1) < 12 or len(self.h4) < 100 or len(self.d1) < 30:
            return
        if self.legs or self.orders:
            return
        rsi = float(self.rsi_h1[0])
        if bool(self.p.relaxed_trend_entry):
            fast = float(self.ma_fast[0])
            slow = float(self.ma_slow[0])
            if fast >= slow:
                self._open_two_legs('long')
            else:
                self._open_two_legs('short')
            return
        self._detect_cross()
        if not self._can_trade():
            return
        if not self._countdown_active():
            if bool(self.p.relaxed_trend_entry):
                fast = float(self.ma_fast[0])
                slow = float(self.ma_slow[0])
                if fast > slow and rsi > float(self.p.rsi_upper):
                    self._open_two_legs('long')
                elif fast < slow and rsi < float(self.p.rsi_lower):
                    self._open_two_legs('short')
            return
        trigger_price = self._trigger_price()
        if trigger_price is None:
            return
        enu = float(self.env_d1.top[0])
        enl = float(self.env_d1.bot[0])
        enu2 = float(self.env_h4.top[0])
        enl2 = float(self.env_h4.bot[0])
        high10h = self._high10h()
        low10h = self._low10h()
        ask = float(self.base.close[0])
        bid = float(self.base.close[0])
        if self.cross_dir == 'long' and ask >= trigger_price:
            long_ok = rsi > float(self.p.rsi_upper)
            if bool(self.p.strict_breakout_filters):
                long_ok = long_ok and ask > high10h and ask > enu and ask > enu2
            if long_ok:
                self._open_two_legs('long')
        elif self.cross_dir == 'short' and bid <= trigger_price:
            short_ok = rsi < float(self.p.rsi_lower)
            if bool(self.p.strict_breakout_filters):
                short_ok = short_ok and bid < low10h and bid < enl and bid < enl2
            if short_ok:
                self._open_two_legs('short')

    def notify_order(self, order):
        if order.status in [bt.Order.Submitted, bt.Order.Accepted]:
            return
        if order.status == bt.Order.Completed:
            self.completed_order_count += 1
            if order.executed.size > 0:
                self.buy_count += 1
                for leg in self.legs:
                    if leg['side'] == 'long' and not leg['opened']:
                        leg['opened'] = True
                        leg['entry_hint'] = order.executed.price
                        break
            elif order.executed.size < 0:
                self.sell_count += 1
                for leg in self.legs:
                    if leg['side'] == 'short' and not leg['opened']:
                        leg['opened'] = True
                        leg['entry_hint'] = order.executed.price
                        break
        elif order.status in [bt.Order.Canceled, bt.Order.Margin, bt.Order.Rejected, bt.Order.Expired]:
            self.rejected_order_count += 1

    def notify_trade(self, trade):
        if not trade.isclosed:
            return
        self.trade_count += 1
        if trade.pnlcomm >= 0:
            self.win_count += 1
        else:
            self.loss_count += 1



BASE_DIR = Path(__file__).resolve().parent

WORKSPACE_DIR = BASE_DIR.parents[2]
LOCAL_BACKTRADER_REPO = WORKSPACE_DIR / 'backtrader'
if LOCAL_BACKTRADER_REPO.exists() and str(LOCAL_BACKTRADER_REPO) not in sys.path:
    sys.path.insert(0, str(LOCAL_BACKTRADER_REPO))



MINUTES_PER_TRADING_YEAR = 24 * 60 * 252



def resolve_data_path(filename):
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def load_backtest_frame(config):
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(
        resolve_data_path(data_cfg['file']),
        fromdate=fromdate,
        todate=todate,
        bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0),
    )
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f'Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}')
    return {'data': df, 'fromdate': fromdate, 'todate': todate}


def build_cerebro(config, frame):
    bt_cfg = config['backtest']
    data_cfg = config['data']
    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(
        commission=bt_cfg['commission'],
        margin=bt_cfg['margin'],
        mult=bt_cfg['multiplier'],
        commtype=comm_type,
        stocklike=bt_cfg.get('stocklike', False),
    )

    base_feed = Mt5PandasFeed(
        dataname=frame['data'][['open', 'high', 'low', 'close', 'volume', 'openinterest']],
        timeframe=bt.TimeFrame.Minutes,
        compression=data_cfg.get('base_compression', 15),
    )
    h1_source = Mt5PandasFeed(
        dataname=frame['data'][['open', 'high', 'low', 'close', 'volume', 'openinterest']],
        timeframe=bt.TimeFrame.Minutes,
        compression=data_cfg.get('base_compression', 15),
    )
    h4_source = Mt5PandasFeed(
        dataname=frame['data'][['open', 'high', 'low', 'close', 'volume', 'openinterest']],
        timeframe=bt.TimeFrame.Minutes,
        compression=data_cfg.get('base_compression', 15),
    )
    d1_source = Mt5PandasFeed(
        dataname=frame['data'][['open', 'high', 'low', 'close', 'volume', 'openinterest']],
        timeframe=bt.TimeFrame.Minutes,
        compression=data_cfg.get('base_compression', 15),
    )

    cerebro.adddata(base_feed, name=f"{data_cfg['symbol']}_{data_cfg['timeframe']}")
    cerebro.resampledata(h1_source, timeframe=bt.TimeFrame.Minutes, compression=60, name='HTF_H1')
    cerebro.resampledata(h4_source, timeframe=bt.TimeFrame.Minutes, compression=240, name='HTF_H4')
    cerebro.resampledata(d1_source, timeframe=bt.TimeFrame.Days, compression=1, name='HTF_D1')
    cerebro.addstrategy(HerculesAtc2006Strategy, **config.get('params', {}))
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=data_cfg.get('base_compression', 15), tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')
    return cerebro


def extract_metrics(strat, cerebro, frame, config):
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    initial_cash = config['backtest']['initial_cash']
    final_value = cerebro.broker.getvalue()
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    total_trades = trades.get('total', {}).get('closed', won + lost)
    gross_won = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gross_lost = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {
        'fromdate': frame['fromdate'],
        'todate': frame['todate'],
        'bars': len(frame['data']),
        'bar_num': strat.bar_num,
        'signal_count': strat.signal_count,
        'buy_count': strat.buy_count,
        'sell_count': strat.sell_count,
        'trade_count': strat.trade_count,
        'completed_orders': strat.completed_order_count,
        'rejected_orders': strat.rejected_order_count,
        'initial_cash': initial_cash,
        'final_value': final_value,
        'net_pnl': final_value - initial_cash,
        'total_return_pct': (final_value / initial_cash - 1) * 100,
        'total_trades': total_trades,
        'won': won,
        'lost': lost,
        'win_rate': (won / total_trades * 100) if total_trades else 0,
        'profit_factor': (gross_won / gross_lost) if gross_lost else None,
        'sharpe_ratio': sharpe.get('sharperatio'),
        'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sqn': sqn.get('sqn'),
    }



def run(plot=False):
    config = load_config()
    frame = load_backtest_frame(config)
    cerebro = build_cerebro(config, frame)
    print('\nStarting backtest...')
    results = cerebro.run()
    strat = results[0]
    metrics = extract_metrics(strat, cerebro, frame, config)

    if plot:
        cerebro.plot()
    return results, metrics, cerebro


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def _invoke_strategy_main():
    """Call main() or run() depending on what the original script defined."""
    import sys as _sys
    _mod = _sys.modules[__name__]
    if hasattr(_mod, "main") and callable(_mod.main):
        return _mod.main()
    if hasattr(_mod, "run") and callable(_mod.run):
        return _mod.run()
    raise RuntimeError("Neither main() nor run() found in inlined module")


def test_59_0059_0669_hercules_atc_2006() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/trend_following/0059_0669_hercules_atc_2006.
    """
    # Capture metrics by hooking extract_metrics() and invoking the original
    # main() (or run()). This reuses whatever loader / build_cerebro /
    # extract_metrics signatures the strategy used internally.
    captured = {}
    _orig_extract = extract_metrics
    def _capture_em(*a, **kw):
        m = _orig_extract(*a, **kw)
        if isinstance(m, dict):
            captured["metrics"] = m
        return m

    import sys as _sys
    _mod = _sys.modules[__name__]
    _mod.extract_metrics = _capture_em

    # Force runonce=True for the run inside main().
    import backtrader as _bt
    _orig_run = _bt.Cerebro.run
    def _forced_runonce(self, *args, **kwargs):
        kwargs["runonce"] = True
        return _orig_run(self, *args, **kwargs)
    _bt.Cerebro.run = _forced_runonce

    # Strip pytest argv so that argparse-based main() functions don't see them.
    _saved_argv = _sys.argv
    _sys.argv = [_sys.argv[0]]

    try:
        try:
            _invoke_strategy_main()
        except SystemExit:
            pass
        except Exception:
            if "metrics" not in captured:
                raise
    finally:
        _bt.Cerebro.run = _orig_run
        _mod.extract_metrics = _orig_extract
        _sys.argv = _saved_argv

    metrics = captured.get("metrics")
    assert metrics is not None, "extract_metrics() was not called"

    assert metrics.get('bar_num') == 1302, f"bar_num: expected=1302, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 748, f"buy_count: expected=748, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 748, f"sell_count: expected=748, got={metrics.get('sell_count')!r}"
    assert metrics.get('total_trades') == 374, f"total_trades: expected=374, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 374, f"trade_count: expected=374, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 166, f"won: expected=166, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 208, f"lost: expected=208, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 3489.0, tol=3.489000e-03, key='bars')
    _close(metrics.get('signal_count'), 375.0, tol=3.750000e-04, key='signal_count')
    _close(metrics.get('completed_orders'), 1496.0, tol=1.496000e-03, key='completed_orders')
    _close(metrics.get('rejected_orders'), 0.0, tol=1.000000e-06, key='rejected_orders')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_value'), 999581.5199999991, tol=9.995815e-01, key='final_value')
    _close(metrics.get('net_pnl'), -418.4800000009127, tol=4.184800e-04, key='net_pnl')
    _close(metrics.get('total_return_pct'), -0.0418480000000887, tol=1.000000e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 44.38502673796791, tol=4.438503e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 0.858554721827889, tol=1.000000e-06, key='profit_factor')
    _close(metrics.get('sharpe_ratio'), -9.414433283607242, tol=9.414433e-06, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), -4.258796767716796, tol=4.258797e-06, key='annual_return_pct')
    _close(metrics.get('max_drawdown'), 0.06823652807487497, tol=1.000000e-06, key='max_drawdown')
    _close(metrics.get('sqn'), -0.9188133355710207, tol=1.000000e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
