"""Inlined regression test for tests/functional/strategies/trend_following/regression/0124_1172_ea_marsi.

Auto-generated by scripts/inline_regression_tests.py — do not edit manually.
The original config.yaml, run.py, strategy_*.py, and expected.json have been
collapsed into this single self-contained file.

Runs with runonce=True only (no parametrization).
Asserts directly on the strategy's own extract_metrics() output captured at
migration time.
"""
from __future__ import annotations
import math
from pathlib import Path
import io
import sys
import argparse
import datetime
import backtrader.feeds as btfeeds
import backtrader as bt
import numpy as np
import pandas as pd
import pytest

_REPO = Path(__file__).resolve().parents[4]

_CONFIG = {
    'strategy': {
        'name': 'EA_MARSI',
        'source_ea': 'ea/1172_EA_MARSI',
    },
    'data': {
        'symbol': 'XAUUSD',
        'timeframe': 'M15',
        'file': '{repo}/tests/datas/XAUUSD_M15.csv',
        'fromdate': '2025-12-03 01:15:00',
        'todate': '2026-03-10 09:00:00',
        'bar_shift_minutes': 15,
    },
    'params': {
        'lots': 0.1,
        'tp_points': 0.0,
        'sl_points': 0.0,
        'use_multpl': False,
        'max_drawdown': 10000.0,
        'slow_rsi_period': 310,
        'slow_ema_periods': 40,
        'slow_price': 0,
        'fast_rsi_period': 200,
        'fast_ema_periods': 50,
        'fast_price': 0,
        'signal_bar': 1,
        'point': 0.01,
        'max_lot': None,
    },
    'backtest': {
        'initial_cash': 1000000,
        'commission': 0.0,
        'margin': 0.01,
        'multiplier': 100.0,
        'commission_type': 'fixed',
        'stocklike': False,
    },
}


def _resolve_repo_paths(node):
    """Replace '{repo}' placeholder in config string values with absolute repo path."""
    if isinstance(node, dict):
        return {k: _resolve_repo_paths(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_resolve_repo_paths(v) for v in node]
    if isinstance(node, str):
        return node.replace('{repo}', str(_REPO))
    return node


def load_config(*args, **kwargs):
    """Inlined config (was config.yaml). Accepts any args for compatibility with strategies that pass a path."""
    import copy
    return _resolve_repo_paths(copy.deepcopy(_CONFIG))



WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_REPO = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_REPO) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_REPO))



def load_mt5_csv(filepath, fromdate=None, todate=None, bar_shift_minutes=0):
    with open(filepath, 'r', encoding='utf-8') as f:
        lines = f.read().strip().split('\n')
    cleaned = '\n'.join(line.strip().strip('"') for line in lines)
    df = pd.read_csv(io.StringIO(cleaned), sep='\t')
    df['datetime'] = pd.to_datetime(df['<DATE>'] + ' ' + df['<TIME>'], format='%Y.%m.%d %H:%M:%S')
    df = df.rename(columns={
        '<OPEN>': 'open', '<HIGH>': 'high', '<LOW>': 'low',
        '<CLOSE>': 'close', '<TICKVOL>': 'volume', '<VOL>': 'openinterest',
    })
    df = df[['datetime', 'open', 'high', 'low', 'close', 'volume', 'openinterest']]
    df = df.set_index('datetime')
    if bar_shift_minutes:
        df.index = df.index + pd.Timedelta(minutes=bar_shift_minutes)
    if fromdate is not None:
        df = df[df.index >= fromdate]
    if todate is not None:
        df = df[df.index <= todate]
    return df


class Mt5PandasFeed(btfeeds.PandasData):
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2),
        ('close', 3), ('volume', 4), ('openinterest', 5),
    )


class EaMarsiSignalFeed(btfeeds.PandasData):
    lines = ('slow_line', 'fast_line')
    params = (
        ('datetime', None), ('open', 0), ('high', 1), ('low', 2),
        ('close', 3), ('volume', 4), ('openinterest', 5),
        ('slow_line', 6), ('fast_line', 7),
    )


def applied_price(df, code):
    open_ = df['open'].astype(float)
    high = df['high'].astype(float)
    low = df['low'].astype(float)
    close = df['close'].astype(float)
    code = int(code)
    if code == 0:
        return close
    if code == 1:
        return open_
    if code == 2:
        return high
    if code == 3:
        return low
    if code == 4:
        return (high + low) / 2.0
    if code == 5:
        return (high + low + close) / 3.0
    if code == 6:
        return (high + low + 2.0 * close) / 4.0
    return close


def rsi_wilder(series, period):
    period = int(period)
    delta = series.diff()
    gain = delta.clip(lower=0.0)
    loss = (-delta).clip(lower=0.0)
    avg_gain = gain.ewm(alpha=1.0 / period, adjust=False, min_periods=period).mean()
    avg_loss = loss.ewm(alpha=1.0 / period, adjust=False, min_periods=period).mean()
    rs = avg_gain / avg_loss.replace(0.0, np.nan)
    rsi = 100.0 - (100.0 / (1.0 + rs))
    rsi = rsi.where(avg_loss != 0.0, 100.0)
    rsi = rsi.where(~((avg_gain == 0.0) & (avg_loss == 0.0)), 50.0)
    return rsi


def ema_rsi_va(series, rsi_period, ema_periods):
    price_series = series.astype(float)
    rsi_period = int(rsi_period)
    ema_periods = float(ema_periods)
    rsi = rsi_wilder(price_series, rsi_period)
    values = np.full(len(price_series), np.nan, dtype=float)
    seed_index = rsi_period * 2 - 1
    if seed_index >= len(price_series):
        return pd.Series(values, index=price_series.index)
    values[seed_index] = float(price_series.iloc[seed_index])
    for i in range(seed_index + 1, len(price_series)):
        rsi_value = float(rsi.iloc[i]) if pd.notna(rsi.iloc[i]) else math.nan
        if not math.isfinite(rsi_value):
            values[i] = values[i - 1]
            continue
        rsvoltl = abs(rsi_value - 50.0) + 1.0
        multi = (5.0 + 100.0 / rsi_period) / (0.06 + 0.92 * rsvoltl + 0.02 * (rsvoltl ** 2))
        pdsx = max(multi * ema_periods, 1.0)
        alpha = 2.0 / (pdsx + 1.0)
        values[i] = float(price_series.iloc[i]) * alpha + values[i - 1] * (1.0 - alpha)
    return pd.Series(values, index=price_series.index)


def build_signal_frame(df, slow_rsi_period, slow_ema_periods, slow_price, fast_rsi_period, fast_ema_periods, fast_price):
    signal_df = df.copy()
    signal_df['slow_line'] = ema_rsi_va(applied_price(signal_df, slow_price), slow_rsi_period, slow_ema_periods)
    signal_df['fast_line'] = ema_rsi_va(applied_price(signal_df, fast_price), fast_rsi_period, fast_ema_periods)
    return signal_df


class EaMarsiStrategy(bt.Strategy):
    params = dict(
        lots=0.1,
        tp_points=0.0,
        sl_points=0.0,
        use_multpl=False,
        max_drawdown=10000.0,
        point=0.01,
        signal_bar=1,
        max_lot=None,
        slow_rsi_period=310,
        slow_ema_periods=40,
        slow_price=0,
        fast_rsi_period=200,
        fast_ema_periods=50,
        fast_price=0,
    )

    def __init__(self):
        self.base = self.datas[0]
        self.signal = self.datas[1]
        self.order = None
        self.pending_reverse = None
        self.stop_price = None
        self.take_price = None
        self.bar_num = 0
        self.signal_count = 0
        self.buy_count = 0
        self.sell_count = 0
        self.trade_count = 0
        self.win_count = 0
        self.loss_count = 0
        self._position_was_open = False

    def log(self, text):
        dt = bt.num2date(self.base.datetime[0])
        print(f'{dt.isoformat()}, {text}')

    def _position_size(self):
        size = float(self.p.lots)
        if bool(self.p.use_multpl):
            base_value = float(self.broker.getvalue())
            max_drawdown = max(float(self.p.max_drawdown), 1e-9)
            size = round(float(self.p.lots) * base_value / max_drawdown, 2)
        max_lot = self.p.max_lot
        if max_lot is not None:
            size = min(size, float(max_lot))
        return max(size, 0.0)

    def _set_exit_levels(self, entry_price, direction):
        point = float(self.p.point)
        sl_distance = float(self.p.sl_points) * point if float(self.p.sl_points) > 0 else None
        tp_distance = float(self.p.tp_points) * point if float(self.p.tp_points) > 0 else None
        if direction > 0:
            self.stop_price = entry_price - sl_distance if sl_distance is not None else None
            self.take_price = entry_price + tp_distance if tp_distance is not None else None
        else:
            self.stop_price = entry_price + sl_distance if sl_distance is not None else None
            self.take_price = entry_price - tp_distance if tp_distance is not None else None

    def _clear_exit_levels(self):
        self.stop_price = None
        self.take_price = None

    def _enter(self, direction):
        if self.order is not None:
            return
        size = self._position_size()
        if size <= 0:
            return
        if direction > 0:
            self.log(f'open long size={size:.2f}')
            self.order = self.buy(size=size)
        else:
            self.log(f'open short size={size:.2f}')
            self.order = self.sell(size=size)

    def _check_exit_levels(self):
        if not self.position or self.order is not None:
            return False
        high = float(self.base.high[0])
        low = float(self.base.low[0])
        if self.position.size > 0:
            hit_stop = self.stop_price is not None and low <= self.stop_price
            hit_take = self.take_price is not None and high >= self.take_price
            if hit_stop or hit_take:
                reason = 'stop loss' if hit_stop else 'take profit'
                self.log(f'close long by {reason}')
                self.pending_reverse = None
                self.order = self.close()
                return True
        elif self.position.size < 0:
            hit_stop = self.stop_price is not None and high >= self.stop_price
            hit_take = self.take_price is not None and low <= self.take_price
            if hit_stop or hit_take:
                reason = 'stop loss' if hit_stop else 'take profit'
                self.log(f'close short by {reason}')
                self.pending_reverse = None
                self.order = self.close()
                return True
        return False

    def next(self):
        self.bar_num += 1
        if len(self.base) < 2 or len(self.signal) < 2:
            return
        if self.order is not None:
            return
        if self.pending_reverse is not None and not self.position:
            direction = self.pending_reverse
            self.pending_reverse = None
            self._enter(direction)
            return
        if self._check_exit_levels():
            return
        recent_ago = max(int(self.p.signal_bar), 1) - 1
        prev_ago = recent_ago + 1
        if len(self.signal) <= prev_ago:
            return
        slow_now = float(self.signal.slow_line[-recent_ago]) if recent_ago else float(self.signal.slow_line[0])
        fast_now = float(self.signal.fast_line[-recent_ago]) if recent_ago else float(self.signal.fast_line[0])
        slow_prev = float(self.signal.slow_line[-prev_ago])
        fast_prev = float(self.signal.fast_line[-prev_ago])
        if not all(math.isfinite(v) for v in [slow_now, fast_now, slow_prev, fast_prev]):
            return
        bullish_cross = slow_prev > fast_prev and slow_now <= fast_now
        bearish_cross = slow_prev < fast_prev and slow_now >= fast_now
        if not bullish_cross and not bearish_cross:
            return
        self.signal_count += 1
        if not self.position:
            if bullish_cross:
                self._enter(1)
            elif bearish_cross:
                self._enter(-1)
            return
        if self.position.size > 0 and bearish_cross:
            self.log('reverse long to short')
            self.pending_reverse = -1
            self.order = self.close()
            return
        if self.position.size < 0 and bullish_cross:
            self.log('reverse short to long')
            self.pending_reverse = 1
            self.order = self.close()

    def notify_order(self, order):
        if order.status in [order.Submitted, order.Accepted]:
            return
        if order.status == order.Completed:
            self.order = None
            if self.position:
                direction = 1 if self.position.size > 0 else -1
                self._set_exit_levels(float(order.executed.price), direction)
            else:
                self._clear_exit_levels()
            return
        if order.status in [order.Canceled, order.Margin, order.Rejected]:
            self.log(f'order failed status={order.getstatusname()}')
            self.order = None
            self.pending_reverse = None

    def notify_trade(self, trade):
        if trade.isopen and not self._position_was_open:
            if trade.size > 0:
                self.buy_count += 1
            elif trade.size < 0:
                self.sell_count += 1
            self._position_was_open = True
            return
        if not trade.isclosed:
            return
        self.trade_count += 1
        if trade.pnlcomm >= 0:
            self.win_count += 1
        else:
            self.loss_count += 1
        self._position_was_open = False
        self._clear_exit_levels()
        self.log(f'trade closed pnl={trade.pnlcomm:.2f}')



WORKSPACE_ROOT = Path(__file__).resolve().parents[3]
BACKTRADER_REPO = WORKSPACE_ROOT / 'backtrader'
if str(BACKTRADER_REPO) not in sys.path:
    sys.path.insert(0, str(BACKTRADER_REPO))



BASE_DIR = Path(__file__).resolve().parent

MINUTES_PER_TRADING_YEAR = 24 * 60 * 252



def resolve_data_path(filename):
    path = (BASE_DIR / filename).resolve()
    if not path.exists():
        raise FileNotFoundError(f'Data file not found: {path}')
    return path


def load_backtest_frame(config):
    data_cfg = config['data']
    fromdate = datetime.datetime.fromisoformat(data_cfg['fromdate'])
    todate = datetime.datetime.fromisoformat(data_cfg['todate'])
    df = load_mt5_csv(
        resolve_data_path(data_cfg['file']),
        fromdate=fromdate,
        todate=todate,
        bar_shift_minutes=data_cfg.get('bar_shift_minutes', 0),
    )
    if df.empty:
        raise ValueError('Loaded data frame is empty')
    print(f"Loaded {len(df)} bars: {df.index[0]} -> {df.index[-1]}")
    return {'data': df, 'fromdate': fromdate, 'todate': todate}


def build_cerebro(config, frame):
    bt_cfg = config['backtest']
    params = dict(config.get('params', {}))
    signal_frame = build_signal_frame(
        frame['data'],
        slow_rsi_period=params.pop('slow_rsi_period', 310),
        slow_ema_periods=params.pop('slow_ema_periods', 40),
        slow_price=params.pop('slow_price', 0),
        fast_rsi_period=params.pop('fast_rsi_period', 200),
        fast_ema_periods=params.pop('fast_ema_periods', 50),
        fast_price=params.pop('fast_price', 0),
    )
    if signal_frame.empty:
        raise ValueError('Signal frame is empty after preprocessing')

    cerebro = bt.Cerebro(stdstats=True)
    cerebro.broker.setcash(bt_cfg['initial_cash'])
    comm_type = bt.CommInfoBase.COMM_FIXED if bt_cfg.get('commission_type', 'fixed') == 'fixed' else bt.CommInfoBase.COMM_PERC
    cerebro.broker.setcommission(
        commission=bt_cfg['commission'],
        margin=bt_cfg['margin'],
        mult=bt_cfg['multiplier'],
        commtype=comm_type,
        stocklike=bt_cfg.get('stocklike', False),
    )

    base_feed = Mt5PandasFeed(dataname=frame['data'], timeframe=bt.TimeFrame.Minutes, compression=15)
    signal_feed = EaMarsiSignalFeed(dataname=signal_frame, timeframe=bt.TimeFrame.Minutes, compression=15)
    cerebro.adddata(base_feed, name=f"{config['data']['symbol']}_{config['data']['timeframe']}")
    cerebro.adddata(signal_feed, name=f"{config['data']['symbol']}_{config['data']['timeframe']}_signal")
    cerebro.addstrategy(EaMarsiStrategy, **params)
    cerebro.addanalyzer(bt.analyzers.SharpeRatio, _name='sharpe', timeframe=bt.TimeFrame.Minutes, factor=MINUTES_PER_TRADING_YEAR, annualize=True, riskfreerate=0)
    cerebro.addanalyzer(bt.analyzers.Returns, _name='returns', timeframe=bt.TimeFrame.Minutes, compression=15, tann=MINUTES_PER_TRADING_YEAR)
    cerebro.addanalyzer(bt.analyzers.DrawDown, _name='drawdown')
    cerebro.addanalyzer(bt.analyzers.TradeAnalyzer, _name='trades')
    cerebro.addanalyzer(bt.analyzers.SQN, _name='sqn')
    return cerebro


def extract_metrics(strat, cerebro, frame, config):
    sharpe = strat.analyzers.sharpe.get_analysis()
    returns = strat.analyzers.returns.get_analysis()
    drawdown = strat.analyzers.drawdown.get_analysis()
    trades = strat.analyzers.trades.get_analysis()
    sqn = strat.analyzers.sqn.get_analysis()
    initial_cash = config['backtest']['initial_cash']
    final_value = cerebro.broker.getvalue()
    total_trades = trades.get('total', {}).get('total', 0)
    won = trades.get('won', {}).get('total', 0)
    lost = trades.get('lost', {}).get('total', 0)
    gross_won = trades.get('won', {}).get('pnl', {}).get('total', 0) or 0
    gross_lost = abs(trades.get('lost', {}).get('pnl', {}).get('total', 0) or 0)
    return {
        'fromdate': frame['fromdate'],
        'todate': frame['todate'],
        'bars': len(frame['data']),
        'bar_num': strat.bar_num,
        'signal_count': strat.signal_count,
        'buy_count': strat.buy_count,
        'sell_count': strat.sell_count,
        'trade_count': strat.trade_count,
        'win_count': strat.win_count,
        'loss_count': strat.loss_count,
        'initial_cash': initial_cash,
        'final_value': final_value,
        'net_pnl': final_value - initial_cash,
        'total_return_pct': (final_value / initial_cash - 1) * 100,
        'total_trades': total_trades,
        'won': won,
        'lost': lost,
        'win_rate': (won / total_trades * 100) if total_trades else 0,
        'profit_factor': (gross_won / gross_lost) if gross_lost else None,
        'max_drawdown': drawdown.get('max', {}).get('drawdown', 0),
        'sharpe_ratio': sharpe.get('sharperatio'),
        'annual_return_pct': (returns.get('rnorm') or 0) * 100,
        'sqn': sqn.get('sqn'),
    }



def run(plot=False):
    config = load_config()
    frame = load_backtest_frame(config)
    cerebro = build_cerebro(config, frame)
    print('\nStarting backtest...')
    results = cerebro.run()
    strat = results[0]
    metrics = extract_metrics(strat, cerebro, frame, config)

    if plot:
        cerebro.plot()
    return results, metrics, cerebro


def _close(actual, expected, *, tol, key):
    """Assert ``actual`` is finite and within ``tol`` of ``expected``."""
    assert actual is not None, f"{key}: expected={expected}, got=None"
    a = float(actual)
    assert math.isfinite(a), f"{key}: expected={expected}, got non-finite {actual}"
    assert abs(a - float(expected)) <= tol, (
        f"{key}: expected={expected}, got={a} (tol={tol})"
    )


def _invoke_strategy_main():
    """Call main() or run() depending on what the original script defined."""
    import sys as _sys
    _mod = _sys.modules[__name__]
    if hasattr(_mod, "main") and callable(_mod.main):
        return _mod.main()
    if hasattr(_mod, "run") and callable(_mod.run):
        return _mod.run()
    raise RuntimeError("Neither main() nor run() found in inlined module")


def test_124_0124_1172_ea_marsi() -> None:
    """Migrated regression test (runonce=True only).

    Originally located at tests/functional/strategies_regression/trend_following/0124_1172_ea_marsi.
    """
    # Capture metrics by hooking extract_metrics() and invoking the original
    # main() (or run()). This reuses whatever loader / build_cerebro /
    # extract_metrics signatures the strategy used internally.
    captured = {}
    _orig_extract = extract_metrics
    def _capture_em(*a, **kw):
        m = _orig_extract(*a, **kw)
        if isinstance(m, dict):
            captured["metrics"] = m
        return m

    import sys as _sys
    _mod = _sys.modules[__name__]
    _mod.extract_metrics = _capture_em

    # Force runonce=True for the run inside main().
    import backtrader as _bt
    _orig_run = _bt.Cerebro.run
    def _forced_runonce(self, *args, **kwargs):
        kwargs["runonce"] = True
        return _orig_run(self, *args, **kwargs)
    _bt.Cerebro.run = _forced_runonce

    # Strip pytest argv so that argparse-based main() functions don't see them.
    _saved_argv = _sys.argv
    _sys.argv = [_sys.argv[0]]

    try:
        try:
            _invoke_strategy_main()
        except SystemExit:
            pass
        except Exception:
            if "metrics" not in captured:
                raise
    finally:
        _bt.Cerebro.run = _orig_run
        _mod.extract_metrics = _orig_extract
        _sys.argv = _saved_argv

    metrics = captured.get("metrics")
    assert metrics is not None, "extract_metrics() was not called"

    assert metrics.get('bar_num') == 6129, f"bar_num: expected=6129, got={metrics.get('bar_num')!r}"
    assert metrics.get('buy_count') == 26, f"buy_count: expected=26, got={metrics.get('buy_count')!r}"
    assert metrics.get('sell_count') == 26, f"sell_count: expected=26, got={metrics.get('sell_count')!r}"
    assert metrics.get('win_count') == 31, f"win_count: expected=31, got={metrics.get('win_count')!r}"
    assert metrics.get('loss_count') == 20, f"loss_count: expected=20, got={metrics.get('loss_count')!r}"
    assert metrics.get('total_trades') == 52, f"total_trades: expected=52, got={metrics.get('total_trades')!r}"
    assert metrics.get('trade_count') == 51, f"trade_count: expected=51, got={metrics.get('trade_count')!r}"
    assert metrics.get('won') == 31, f"won: expected=31, got={metrics.get('won')!r}"
    assert metrics.get('lost') == 20, f"lost: expected=20, got={metrics.get('lost')!r}"
    _close(metrics.get('bars'), 6129.0, tol=6.129000e-03, key='bars')
    _close(metrics.get('signal_count'), 52.0, tol=5.200000e-05, key='signal_count')
    _close(metrics.get('initial_cash'), 1000000.0, tol=1.000000e+00, key='initial_cash')
    _close(metrics.get('final_value'), 990110.0999999988, tol=9.901101e-01, key='final_value')
    _close(metrics.get('net_pnl'), -9889.900000001187, tol=9.889900e-03, key='net_pnl')
    _close(metrics.get('total_return_pct'), -0.9889900000001228, tol=1.000000e-06, key='total_return_pct')
    _close(metrics.get('win_rate'), 59.61538461538461, tol=5.961538e-05, key='win_rate')
    _close(metrics.get('profit_factor'), 0.5555264066879151, tol=1.000000e-06, key='profit_factor')
    _close(metrics.get('max_drawdown'), 1.265145841523397, tol=1.265146e-06, key='max_drawdown')
    _close(metrics.get('sharpe_ratio'), -8.700322233926839, tol=8.700322e-06, key='sharpe_ratio')
    _close(metrics.get('annual_return_pct'), -44.48220281073637, tol=4.448220e-05, key='annual_return_pct')
    _close(metrics.get('sqn'), -1.3144825773260933, tol=1.314483e-06, key='sqn')
    _total_trades = metrics.get("total_trades") or metrics.get("trade_num") or metrics.get("trade_count") or 0
    _activity = (
        _total_trades
        or (metrics.get("buy_count") or 0)
        or (metrics.get("sell_count") or 0)
        or (metrics.get("rebalance_count") or 0)
    )
    assert _activity > 0, f"strategy must have non-zero activity, got metrics={metrics!r}"
