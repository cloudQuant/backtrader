"""Self-contained Iteration 25 CTP-options tick replay example.

The package deliberately has no dependency on any other directory below
``examples/``.  It is importable for its focused unit tests and can also be
started directly through :mod:`run`.
"""
