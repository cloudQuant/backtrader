# This replay example does not load credentials and never opens a network connection.
# Keep any future local-only credentials in an ignored .env file; do not copy them
# into config.yaml, fixtures, reports, or command lines.

