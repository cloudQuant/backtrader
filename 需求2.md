### 继续重构完全去除元编程
1. 修复当前测试不通过的测试文件
2. 修复完成源代码之后，pip install -U .  进行编译安装
3. 修复相应的bug，需要确保pytest tests -n 8 全部运行成功
4. pytest tests -n 8 运行耗费的时间需要控制在25s以下，超过25s,认为这次修复失败，回退代码
5. 不允许修改F:\source_code\backtrader\tests\original_tests和F:\source_code\backtrader\tests\funding_rate_examples这两个文件夹中的测试用例
6. 需要确保最后完成的时候，pytest tests -n 8 全部运行成功
7. 如果希望计算这些测试用例产生的真实的期望值，需要你把backtrader切换到master，pip install -U . 安装之后，运行来获取真实的bakctrader产生的期望值。记得获取真实的值之后，切换回当前分支之后，重新pip install -U . 进行安装