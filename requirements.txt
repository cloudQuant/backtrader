pandas>=1.3.0
# numpy: pin <2.0 on Python <3.13 (strategy regression tests assert exact
# order/trade counts calibrated on numpy 1.x; numpy 2.x drifts a few via
# reduction/sort/dtype numerics). Python 3.13 has no numpy 1.x wheel (1.26.x
# source builds segfault on win/py3.13), so it uses numpy>=2.1 (has 3.13 wheels,
# regression suite also passes there).
numpy>=1.20.0,<2.0.0; python_version < "3.13"
numpy>=2.1.0; python_version >= "3.13"
matplotlib>=3.3.0
plotly>=5.0.0
python-dateutil>=2.8.0
pytz>=2021.1
tzlocal>=2.1
future>=0.18.0
cython>=0.29.0
setuptools>=40.0
requests>=2.25.0
scipy>=1.5.0
pyecharts>=1.9.0
tqdm>=4.50.0
# numba>=0.50.0  # Optional, may have platform-specific issues
# spdlog  # Optional C++ binding, may fail on some platforms
# python-rapidjson  # Optional, may fail on some platforms
# Type stubs for mypy (dev)
types-pytz>=2021.1
types-python-dateutil>=2.8.0
types-PyMySQL>=1.0.0
pytest>=6.0.0
pytest-benchmark>=3.4.0
pytest-sugar>=0.9.0
pytest-cov>=2.12.0
pytest-picked>=0.4.0
pytest-xdist>=2.3.0
pytest-asyncio>=0.15.0
pytest-html>=3.1.0
pytest-ordering>=0.6.0
pytest-mock>=3.6.0
pytest-timeout>=2.1.0
requests-mock>=1.9.0
dash>=2.0.0
# ctpbee  # Optional China market data, requires C++ build
# akshare  # Optional, may have network issues in CI
# backports.tarfile  # Not needed for Python 3.12+
# backtrader  # Don't install self from PyPI
# bt_api_py  # Optional external package
statsmodels>=0.12.0