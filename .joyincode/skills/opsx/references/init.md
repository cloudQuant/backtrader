# 初始化

按以下步骤执行项目初始化：

1. 在当前项目根目录下创建以下文件夹
```
├── openspec/
│   └── changes/
│       └── archive/
│   └── specs/
│   └── config.yaml
└── docs/
```

2. 将以下内容写入`config.yaml`
```
schema: spec-driven

```

3. 安装OpenSpec（已安装则跳过）
`npm install -g @fission-ai/openspec@latest`
